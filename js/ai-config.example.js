// ─────────────────────────────────────────────────────────────
// js/ai-config.example.js — ต้นแบบ ไม่มีคีย์จริง
//
// วิธีใช้: คัดลอกไฟล์นี้เป็นชื่อ js/ai-config.js (ไฟล์นั้นถูก .gitignore ไว้
// จะไม่ถูก push ขึ้น GitHub) แล้วแทนค่าด้านล่างด้วยคีย์ OpenRouter จริงของคุณ
// ─────────────────────────────────────────────────────────────

window.AI_CONFIG = {
  apiKey: "ใส่คีย์ OpenRouter ของคุณ (sk-or-...)",
  model: "google/gemini-2.5-flash-lite"
};
