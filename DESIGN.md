# DESIGN.md — War Point Design System

- **วันที่สร้าง:** 2026-08-21
- **สถานะ:** Confirmed (โทนสีและกฎ contrast ยืนยันด้วยการวัดจริงแล้ว)
- **อ้างอิงต้นทาง:** [[docs/01-requirements/01-spec/20260821-00-problem-statement|00-problem-statement]], [[docs/01-requirements/feature-list|feature-list]], [[docs/01-requirements/user-journey|user-journey]]

> ไฟล์นี้เป็น **แหล่งเดียว** ของ design token, UI component pattern และกฎ UX ของโปรเจกต์
> งาน UI ทุกชิ้นต้องอ่านไฟล์นี้ก่อนเขียนโค้ด และคัดลอกค่า token มาประกาศเป็น CSS custom properties ที่ `:root` แล้วอ้างผ่าน `var(--token)` เท่านั้น
> **ห้าม hardcode ค่า hex ใน rule ของ component และห้ามเดาสีเอง**

---

## 1. Brand Identity & CI

### 1.1 แนวคิดหลัก

War Point คือระบบที่ทำให้เด็กประถม **อยากทบทวนบทเรียนด้วยตัวเอง** โดยไม่ต้องรอผู้ใหญ่มากำกับ กลไกคือการเลี้ยงสัตว์เลี้ยงที่เติบโตจากคะแนน แล้วนำไปแข่งขันรายสัปดาห์

### 1.2 บุคลิกของแบรนด์

| ควรรู้สึก | ไม่ควรรู้สึก |
|---|---|
| สดใส มีพลัง อยากกดต่อ | ฉูดฉาดจนลายตา |
| ตื่นเต้นแบบเวทีแข่งขัน | ดุดัน ก้าวร้าว รุนแรง |
| เป็นเจ้าของสัตว์เลี้ยงของตัวเอง | เหมือนแบบฟอร์มของโรงเรียน |
| ปลอดภัย ไม่มีใครมาว่า | กดดัน ถูกจับผิด ถูกลงโทษ |

### 1.3 ระบบ 2 โซน (หัวใจของ CI)

โปรเจกต์นี้ใช้ **โทนสองโซน** เพื่อให้ได้ทั้งความอ่านง่ายและความตื่นเต้น:

| โซน | ใช้ที่ไหน | พื้นหลัง | ความรู้สึกที่ต้องการ |
|---|---|---|---|
| **โซนเรียน** (`.zone-learn`) | บทเรียน แบบทดสอบ โปรไฟล์ หน้าครูทุกหน้า | สว่าง ครีมอบอุ่น | สงบ อ่านง่าย ไม่ล้าตา |
| **โซนอารีน่า** (`.zone-arena`) | สัตว์เลี้ยง วิวัฒนาการ การแข่งขัน ผลการต่อสู้ | เข้ม น้ำเงินม่วง | ตื่นเต้น เหมือนไฟสปอตไลต์ส่องเวที |

**เหตุผลเชิงออกแบบ:** การเปลี่ยนโซนคือ **รางวัลทางอารมณ์** — เด็กทำแบบทดสอบเสร็จในโซนสว่าง แล้ว "เข้าอารีน่า" ที่มืดลงและสีจัดขึ้น ทำให้รู้สึกว่าก้าวเข้าสู่อีกโลก · และในทางเทคนิค การแยกโซนทำให้เรื่อง contrast ง่ายขึ้นมาก เพราะพื้นสว่างใช้อักษรเข้ม พื้นเข้มใช้อักษรสว่าง ไม่ต้องประนีประนอม

**ห้ามใช้ 2 โซนสลับกันในหน้าจอเดียว** ยกเว้นการ์ดสัตว์เลี้ยงที่ฝังในหน้าโซนเรียนได้ (ดูข้อ 3.4)

### 1.4 ตัวอักษร

ต้องใช้ **ฟอนต์ระบบเท่านั้น** เพราะ prototype ต้องเปิดออฟไลน์ได้ **ห้ามอ้างอิงฟอนต์จาก CDN หรือ Google Fonts**

```
--font-base: -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans Thai",
             "Leelawadee UI", Tahoma, "Helvetica Neue", sans-serif;
```

`Tahoma` อยู่ท้ายสุดในฐานะตัวสำรองที่รองรับภาษาไทยได้แน่นอนบน Windows ทุกเครื่อง

---

## 2. Design Tokens

**ทุกค่าด้านล่างวัด contrast แล้วจริงเมื่อ 2026-08-21** ตัวเลขในวงเล็บคืออัตราส่วนที่วัดได้ ดูตารางเต็มในข้อ 2.6

### 2.1 พื้นหลังและพื้นผิว

```css
:root {
  /* โซนเรียน (สว่าง) */
  --bg-learn:          #FFFDF7;
  --surface-learn:     #FFFFFF;
  --surface-learn-alt: #FFF1DC;
  --border-learn:      #E6DAC4;

  /* โซนอารีน่า (เข้ม) */
  --bg-arena:          #141733;
  --surface-arena:     #1E2247;
  --surface-arena-alt: #2B3163;
  --border-arena:      #3C4480;
}
```

### 2.2 Ink token (สีตัวอักษร) — แยกตามโซน

> **นี่คือ token ที่โปรเจกต์ก่อนไม่มีตั้งแต่ต้นแล้วต้องกลับมาไล่แก้ทั้งระบบ** ห้ามใช้สีสดเป็นตัวอักษรโดยตรง ให้ใช้ ink token เท่านั้น

```css
:root {
  /* บนพื้นสว่าง */
  --ink-learn:         #171B2A;  /* 16.83 บน bg-learn */
  --ink-learn-muted:   #4B5568;  /*  7.38 บน bg-learn */

  /* บนพื้นเข้ม */
  --ink-arena:         #F6F8FF;  /* 16.49 บน bg-arena */
  --ink-arena-muted:   #AEB7DE;  /*  8.86 บน bg-arena */

  /* อักษรที่วางทับพื้นสีสด — ใช้เมื่อปุ่มเป็นสีสด */
  --ink-on-bright:     #171B2A;
}
```

### 2.3 สีแอคชันและสีสด

สีสดทุกตัวใน `--bright-*` **ใช้เป็นพื้น (fill) เท่านั้น ห้ามใช้เป็นตัวอักษรบนพื้นสว่าง** ถ้าต้องการใช้สีเป็นตัวอักษรบนพื้นสว่าง ให้ใช้ `--deep-*`

```css
:root {
  /* สีสด — ใช้เป็นพื้นปุ่ม/บล็อก/แถบ พร้อม --ink-on-bright ทับ */
  --bright-orange:    #FF6A13;  /* อักษรเข้มทับได้  5.97 */
  --bright-yellow:    #FFD21E;  /* อักษรเข้มทับได้ 11.81 */
  --bright-teal:      #00B3C8;  /* อักษรเข้มทับได้  6.75 */
  --bright-green:     #21C46A;  /* อักษรเข้มทับได้  7.47 */
  --bright-lavender:  #C4A5FF;  /* อักษรเข้มทับได้  8.29 */
  --bright-pink:      #FF7BAC;  /* อักษรเข้มทับได้  7.08 */

  /* สีเข้ม — ใช้เป็นตัวอักษร/ไอคอนบนพื้นสว่าง หรือพื้นปุ่มที่มีอักษรสว่างทับ */
  --deep-orange:      #C43D00;  /* เป็นอักษรบนพื้นสว่าง 5.14 */
  --deep-amber:       #8A6300;  /* เป็นอักษรบนพื้นสว่าง 5.34 */
  --deep-teal:        #00707F;  /* เป็นอักษรบนพื้นสว่าง 5.70 */
  --deep-green:       #0F7A3D;  /* เป็นอักษรบนพื้นสว่าง 5.33 */
  --deep-violet:      #5B21B6;  /* เป็นอักษรบนพื้นสว่าง 8.83 */
  --deep-red:         #B3231C;  /* เป็นอักษรบนพื้นสว่าง 6.50 */
}
```

**สีที่ห้ามใช้:** `#8B5CF6` (ม่วงกลาง) — วัดแล้วได้ 4.04 / 4.16 / 4.13 **ตกทุกทาง** ใช้เป็นตัวอักษรไม่ได้และเป็นพื้นปุ่มก็ไม่ได้ ถูกแทนด้วย `--bright-lavender` และ `--deep-violet`

### 2.4 สีสถานะ — ต้องมีคู่ทั้ง 2 โซน

```css
:root {
  --state-success-on-learn: #0F7A3D;  /* 5.33 */
  --state-success-on-arena: #21C46A;  /* 7.64 */
  --state-warn-on-learn:    #8A6300;  /* 5.34 */
  --state-warn-on-arena:    #FFD21E;  /* 12.07 */
  --state-error-on-learn:   #B3231C;  /* 6.50 */
  --state-error-on-arena:   #FF8A84;  /* 7.68 */
  --state-info-on-learn:    #00707F;  /* 5.70 */
  --state-info-on-arena:    #3ED8EB;  /* 10.19 */
}
```

### 2.5 สีประจำสายพันธุ์สัตว์เลี้ยง (3 สาย)

~~ค่าตั้งต้นของโปรเจกต์คือ **5 สายพันธุ์**~~ **[แก้ 2026-08-23]** ค่าตั้งต้นของโปรเจกต์คือ **3 สายพันธุ์** ([[docs/01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|spec 02]] หัวข้อ ซ.4) — ชื่อที่มีคือ**ลักษณะไข่** ได้แก่ **ไข่มีปีก / ไข่มีขา / ไข่มีหาง** (ชื่อสายพันธุ์ทางการยังไม่กำหนด ที่นี่กำหนดเฉพาะสีและชื่อลักษณะไข่)

```css
:root {
  --species-wing: #FF6A13;  /* เดิม --species-1 · อักษรเข้มทับ 5.97 · บนพื้นอารีน่า 6.10 · = "ไข่มีปีก" */
  --species-leg:  #00B3C8;  /* เดิม --species-2 · อักษรเข้มทับ 6.75 · บนพื้นอารีน่า 6.90 · = "ไข่มีขา" */
  --species-tail: #21C46A;  /* เดิม --species-3 · อักษรเข้มทับ 7.47 · บนพื้นอารีน่า 7.64 · = "ไข่มีหาง" */
}
```

~~`--species-4: #C4A5FF;` (อักษรเข้มทับ 8.29 · บนพื้นอารีน่า 8.48)~~ `/* เลิกใช้ 2026-08-23 — ลดเหลือ 3 สาย */` — คงค่าเดิมไว้เป็นประวัติ ไม่ได้ลบทิ้ง
~~`--species-5: #FF7BAC;` (อักษรเข้มทับ 7.08 · บนพื้นอารีน่า 7.23)~~ `/* เลิกใช้ 2026-08-23 — ลดเหลือ 3 สาย */` — คงค่าเดิมไว้เป็นประวัติ ไม่ได้ลบทิ้ง

**ห้ามใช้สีเป็นตัวบอกความหมายอย่างเดียว** ทุกที่ที่ใช้สีประจำสายต้องมีชื่อหรือสัญลักษณ์กำกับด้วย เพราะเด็กตาบอดสีจะแยกไม่ออก (ดูข้อ 4.4)

### 2.6 ตารางผลวัด contrast (วัดจริง 2026-08-21)

| คู่สี | อัตราส่วน | เกณฑ์ | ผล |
|---|---|---|---|
| `--ink-learn` บน `--bg-learn` | 16.83 | 4.5 | ✅ |
| `--ink-learn` บน `--surface-learn-alt` | 15.38 | 4.5 | ✅ |
| `--ink-learn-muted` บน `--bg-learn` | 7.38 | 4.5 | ✅ |
| `--ink-arena` บน `--bg-arena` | 16.49 | 4.5 | ✅ |
| `--ink-arena` บน `--surface-arena-alt` | 11.49 | 4.5 | ✅ |
| `--ink-arena-muted` บน `--bg-arena` | 8.86 | 4.5 | ✅ |
| `--ink-on-bright` บนสีสดทุกตัว | 5.97 – 11.81 | 4.5 | ✅ |
| `--deep-*` เป็นอักษรบนพื้นสว่าง | 5.14 – 8.83 | 4.5 | ✅ |
| สีสดเป็นอักษรบนพื้นอารีน่า | 6.10 – 12.07 | 4.5 | ✅ |
| สีสดเป็นอักษรบนพื้นสว่าง | 1.42 – 3.63 | 4.5 | ❌ **ห้ามใช้** |
| `#8B5CF6` ทุกกรณี | 4.04 – 4.16 | 4.5 | ❌ **ห้ามใช้** |

**หมายเหตุ [เพิ่ม 2026-08-23]:** การลดสายพันธุ์จาก 5 เหลือ 3 สายและเปลี่ยนชื่อ token เป็น `--species-wing` / `--species-leg` / `--species-tail` **ไม่กระทบตารางนี้** เพราะค่า hex ของ 3 สีที่เหลือไม่เปลี่ยนเลย ตัวเลข contrast ที่วัดไว้เดิมยังใช้ได้ครบ — ไม่มีแถวใดในตารางนี้อ้างอิง `--species-4`/`--species-5` โดยตรงอยู่แล้ว (ทั้งสองสีถูกรวมอยู่ในแถวรวม "สีสดเป็นอักษรบนพื้นอารีน่า/พื้นสว่าง" ข้างต้น จึงไม่ต้องขีดฆ่าแถวเพิ่ม)

### 2.7 Spacing, radius, ขนาดตัวอักษร

```css
:root {
  --space-1: 4px;  --space-2: 8px;  --space-3: 12px;
  --space-4: 16px; --space-5: 24px; --space-6: 32px; --space-7: 48px;

  --radius-sm: 8px;   --radius-md: 14px;
  --radius-lg: 22px;  --radius-pill: 999px;

  --text-xs: 13px; --text-sm: 15px; --text-md: 17px;
  --text-lg: 21px; --text-xl: 27px; --text-2xl: 34px; --text-3xl: 44px;

  --tap-min: 48px;   /* ขั้นต่ำของพื้นที่กดสำหรับเด็กประถม */
}
```

**ขนาดตัวอักษรฐานคือ `--text-md` 17px ไม่ใช่ 14-16px** เพราะกลุ่มผู้ใช้เป็นเด็กประถมที่อ่านยังไม่คล่อง และ [[docs/01-requirements/01-spec/20260821-01-lesson-and-quiz-system|spec 01]] ระบุว่าเด็กบางคนอ่านหรือมองเห็นได้ไม่ดีนัก

### 2.8 เงาและเอฟเฟกต์

```css
:root {
  --shadow-learn: 0 2px 8px rgba(23, 27, 42, .10);
  --shadow-lift:  0 6px 18px rgba(23, 27, 42, .16);
  --glow-arena:   0 0 20px rgba(255, 210, 30, .45);
}
```

`--glow-arena` ใช้ได้เฉพาะในโซนอารีน่า เพื่อให้เกิดความรู้สึกไฟเวที **ห้ามใช้ในโซนเรียน**

---

## 3. UI Components & Patterns

### 3.1 ปุ่ม

| ชนิด | พื้น | อักษร | ใช้เมื่อ |
|---|---|---|---|
| Primary | `--bright-orange` | `--ink-on-bright` | แอคชันหลัก 1 ปุ่มต่อหน้าจอ |
| Secondary | โปร่งใส + ขอบ `--deep-teal` | `--deep-teal` | แอคชันรอง |
| Ghost | ไม่มีพื้น | `--ink-learn-muted` | ยกเลิก ย้อนกลับ |
| Arena | `--bright-yellow` + `--glow-arena` | `--ink-on-bright` | ปุ่มในโซนอารีน่าเท่านั้น |

ทุกปุ่มสูงอย่างน้อย `--tap-min` (48px) · `border-radius: var(--radius-pill)` · **ห้ามมีปุ่ม primary เกิน 1 ปุ่มในหน้าจอเดียว**

### 3.2 การ์ดบทเรียนและแบบทดสอบ (โซนเรียน)

พื้น `--surface-learn` · ขอบ `--border-learn` · `--radius-lg` · เงา `--shadow-learn`
แถบสถานะด้านซ้ายหนา 6px ใช้สีสด (ยังไม่ทำ / ทำแล้ว / เลยกำหนด) **พร้อมข้อความกำกับเสมอ**

### 3.3 หลอดความก้าวหน้าสู่ขั้นถัดไป

พื้นราง `--surface-learn-alt` (โซนเรียน) หรือ `--surface-arena-alt` (โซนอารีน่า) · แถบเติม `--bright-green`
**ต้องมีตัวเลขกำกับข้างหลอดเสมอ** เช่น "12 / 20 แต้ม" เพราะเด็กประเมินจากความยาวแถบเพียงอย่างเดียวไม่ได้

### 3.4 การ์ดสัตว์เลี้ยง

เป็น component เดียวที่ใช้ได้ทั้ง 2 โซน ในโซนเรียนให้ใช้พื้น `--surface-arena` เป็นบล็อกเข้มฝังในหน้าสว่างได้ (เหมือนหน้าต่างมองเข้าไปในอารีน่า) และตัวอักษรในบล็อกนั้นต้องเปลี่ยนไปใช้ `--ink-arena`

องค์ประกอบบังคับ: รูปสัตว์เลี้ยง · ชื่อที่เด็กตั้ง · ขั้นที่ (เช่น "ขั้น 2 / 4") · แถบสีประจำสาย + ชื่อสาย

### 3.5 ป้ายบอกค่าสเตตัสและพลังพิเศษ

ป้ายรูปแคปซูล พื้น `--surface-arena-alt` อักษร `--ink-arena`

~~สถานะพิเศษที่ spec ระบุไว้มี 3 อย่างเท่านั้น: ไฟไหม้ / ติดพิษ / มึนงง ([[docs/01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|spec 02]]) ห้ามคิดสถานะเพิ่มเอง~~ **[แก้ 2026-08-23]** ข้อความเดิมจัดประเภทผิด แก้ใหม่ดังนี้:

- ไฟไหม้ / ติดพิษ / มึนงง คือ **พลังพิเศษประจำสาย ไม่ใช่ค่าสเตตัส** (เดิมเอกสารจัดประเภทผิด — ดู [[docs/01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|spec 02]] หัวข้อ ช.)
- ทั้ง 3 อย่างเป็นเพียง **ตัวอย่าง เพิ่มได้เมื่อผู้ใช้ระบุ** แต่ยังคงหลักการเดิมไว้ว่า **ห้ามผู้ช่วยคิดรายการเพิ่มเอง**
- **ค่าสเตตัส** คือ ความเร็ว / พละกำลัง / ความแม่นยำ (**เป็น placeholder จำนวนค่ายังไม่ล็อก**)

คงกฎเดิม: แต่ละป้ายค่าสเตตัส/พลังพิเศษต้องใช้ไอคอน inline SVG + ข้อความกำกับเสมอ ห้ามใช้สีเพียงอย่างเดียว

### 3.6 สัญลักษณ์เตือนบนแถวนักเรียน (หน้าครู)

[[docs/01-requirements/01-spec/20260821-04-teacher-class-overview|spec 04]] กำหนดให้มีสัญลักษณ์เตือนบนแถวของนักเรียนที่ครูควรดูแล
พื้น `--bright-yellow` อักษร `--ink-on-bright` **ห้ามใช้การไฮไลต์สีทั้งแถว** เพราะจะทำให้ตัวอักษรในแถวนั้น contrast ตก

### 3.7 กราฟิกสัตว์เลี้ยงใน prototype

**ห้ามอ้างอิงไฟล์รูปภายนอก** ให้ใช้ inline SVG หรือ CSS shape เท่านั้น
ในรอบ prototype ยังไม่มีอาร์ตเวิร์กจริง ให้วาดเป็นรูปทรงเรขาคณิตง่ายๆ ที่ต่างกันตามขั้นและสี และ **ต้องมีป้ายกำกับว่าเป็นภาพสมมติ** (ดูข้อ 4.7)

### 3.8 แถบ HP (HP bar) **[เพิ่มใหม่ 2026-08-23]**

ใช้ token ที่มีอยู่แล้วเท่านั้น ไม่เพิ่ม token ใหม่: รางใช้ `--surface-arena-alt` · แถบเติมใช้ `--state-success-on-arena` ตามปกติ และเปลี่ยนเป็น `--state-warn-on-arena` เมื่อ HP เหลือน้อย
**ต้องมีตัวเลขกำกับเป็นข้อความเสมอ** เช่น "HP 15 / 50" **ห้ามสื่อด้วยความยาวแถบหรือสีเพียงอย่างเดียว** (ข้อ 4.4)
**ค่า HP ยังเป็นค่าสมมติ** — [[docs/01-requirements/01-spec/20260823-06-battle-support-items|spec 06]] ยังไม่มีสูตรคำนวณ HP จึงต้องมีป้ายกำกับว่าเป็นค่าสมมติทุกครั้งที่แสดง (ข้อ 4.7)

### 3.9 ป้ายไอเทม (item chip) **[เพิ่มใหม่ 2026-08-23]**

พื้น `--surface-arena-alt` อักษร `--ink-arena` มีไอคอน inline SVG + ชื่อไอเทม + จำนวนครั้งที่ใช้ได้ (เช่น "ป้องกันการโจมตีถึงตาย · เหลือ 1 ครั้งต่อแมตช์")
ต้องมีสถานะ **"ใช้แล้ว"** ที่แยกได้โดยไม่พึ่งสีเพียงอย่างเดียว — ใช้ไอคอนที่เปลี่ยนไป (เช่น เครื่องหมายถูกจาง) + ข้อความ "ใช้แล้ว" กำกับเสมอ **ห้ามใช้สีแดงหรือถ้อยคำเชิงลบ** กับสถานะนี้ เพราะการใช้ไอเทมไม่ใช่ความผิดพลาดของนักเรียน (สอดคล้องกับข้อ 4.1)

---

## 4. UX Guidelines & Rules

**กฎด้านล่างมีเลขกำกับเพื่อให้เอกสารอื่นอ้างอิงได้** เช่น "ขัดกับ DESIGN.md ข้อ 4.2"

### 4.1 ห้ามมีกลไกลงโทษหรือทำให้เด็กรู้สึกผิด

[[docs/01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|spec 02]] ระบุว่าการแพ้ไม่มีผลเสียใดๆ เลย
**ห้ามใช้** สีแดง เสียงผิด ไอคอนกากบาท หรือข้อความตำหนิ กับการแพ้การแข่งขันหรือการไม่เข้าใช้งาน · ตอบผิดในแบบทดสอบให้บอกเฉลยแบบเป็นกลาง ไม่ใช้ `--state-error-*`

### 4.2 Contrast ต้องผ่านทุก element ที่มีข้อความ

ข้อความปกติ **≥ 4.5:1** · ข้อความใหญ่ตั้งแต่ `--text-xl` ขึ้นไปและองค์ประกอบที่ไม่ใช่ข้อความ **≥ 3:1**
**ต้องวัดทุกหน้าจอ ทุกบทบาท รวม modal และ sheet ที่ต้องเปิดก่อนวัด** และวัดตอน UI มีข้อมูลจริงอยู่ ไม่ใช่ตอนหน้าจอว่าง

### 4.3 พื้นที่กดขั้นต่ำ 48px

ทุกอย่างที่กดได้ต้องมีพื้นที่กดอย่างน้อย `--tap-min` ทั้งกว้างและสูง รวมปุ่มปิด modal และปุ่มไอคอนเล็ก เพราะผู้ใช้เป็นเด็กประถม

### 4.4 ห้ามสื่อความหมายด้วยสีเพียงอย่างเดียว

ทุกที่ที่ใช้สีบอกความหมาย (สายพันธุ์ สถานะงาน สถานะพิเศษ ผลแพ้ชนะ) ต้องมี **ข้อความหรือรูปทรงกำกับด้วย**

### 4.5 สิทธิ์ตามบทบาท — ซ่อน ไม่ใช่ปิดการใช้งาน

สิ่งที่บทบาทนั้นไม่มีสิทธิ์เห็น **ต้องไม่อยู่ใน DOM เลย** ไม่ใช่แสดงแบบ disabled
กรณีที่ต้องระวังที่สุด: [[docs/01-requirements/01-spec/20260821-04-teacher-class-overview|spec 04]] ระบุว่า **หน้าของครูห้ามแสดงแต้มหรือขั้นการเติบโตของสัตว์เลี้ยงนักเรียนเลย** และระบบไม่เก็บชื่อจริงของนักเรียน จึงห้ามมีชื่อ-นามสกุลจริงปรากฏที่ใดในทุกหน้าจอ

### 4.6 รองรับทั้งมือถือและจอใหญ่

[[docs/01-requirements/01-spec/20260821-01-lesson-and-quiz-system|spec 01]] ระบุว่าเด็กใช้ทั้งมือถือที่บ้านและแท็บเล็ต/คอมของโรงเรียน
ออกแบบจากจอเล็กขึ้นไป (mobile-first) · ใช้หน่วยสัมพัทธ์และ flex/grid · `max-width: 100%` กับรูป · ตารางที่กว้างเกินต้องเลื่อนในกล่องของตัวเอง **ห้ามให้หน้าเลื่อนซ้าย-ขวา**

### 4.7 ค่าที่ยังไม่ล็อกต้องกำกับว่าเป็นค่าสมมติ

ตัวเลขที่ spec ยังไม่กำหนด (เกณฑ์แต้มต่อขั้น ค่าพลัง ราคาของแต่งตัว ชื่อสายพันธุ์ ชื่อขั้น) เมื่อปรากฏใน prototype **ต้องมีป้ายกำกับว่าเป็นค่าสมมติ** และต้องบันทึกในไฟล์ `README.md` ของ prototype ว่าค่าใดสมมติขึ้น
ค่าที่ล็อกแล้วมีเพียง **4 ขั้น** และ **5 สายพันธุ์** ซึ่งเป็นค่าตั้งต้นที่ปรับได้

### 4.8 ค่า mock ต้องมีเจ้าของเดียว

ค่าจำลองที่ปรากฏหลายที่ (ชื่อเด็ก ชื่อสัตว์เลี้ยง คะแนน แต้ม จำนวนข้อ) **ต้องประกาศเป็นค่าคงที่หรือ object เดียวที่ต้นไฟล์** แล้วอ้างซ้ำจากที่นั้น ห้ามพิมพ์ตัวเลขซ้ำในหลายจุด เพราะโปรเจกต์ก่อนเคยตั้งตัวเลขชุดเดียวกันไม่ตรงกันข้ามเวอร์ชันจนทำให้ test case รันไม่ผ่านทั้งที่ตรรกะถูก

### 4.9 Prototype ต้อง self-contained

ไฟล์เดียว เปิดออฟไลน์ได้ **ห้ามอ้างอิง CDN ฟอนต์ภายนอก หรือไฟล์รูปภายนอกทั้งสิ้น** ใช้ inline SVG หรือ CSS shape แทน
ต้องพิสูจน์ด้วยการดูรายการ network request จริง ต้องมีแค่ไฟล์ HTML เอง

### 4.10 ห้ามใส่ฟีเจอร์นอกขอบเขต

ทุกหน้าจอต้อง trace กลับไปหา FE ใน [[docs/01-requirements/feature-list|feature-list]] ได้
สิ่งที่อยู่ระดับ **Won't** ห้ามปรากฏใน prototype ได้แก่ บัญชีและหน้าจอสำหรับผู้ปกครอง และการรองรับหลายห้องเรียนหรือหลายโรงเรียน

---

## ประเด็นที่ยังไม่ชัดเจน (Open Questions)

- ~~**ยังไม่มีอาร์ตเวิร์กจริงของสัตว์เลี้ยง** ค่าตั้งต้น 4 ขั้น × 5 สาย ต้องผลิต 20 ภาพ~~ **[แก้ 2026-08-23]** ค่าตั้งต้นเปลี่ยนเป็น **3 สาย** รวมอาร์ตเวิร์ก **15 ภาพ (ไข่ 3 + ร่าง 12)** ([[docs/01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|spec 02]] หัวข้อ ซ.4) — ยังไม่ตัดสินว่าจะลดต่อด้วยการให้บางขั้นใช้ร่างร่วมกันหรือไม่ (spec 02 ยกตัวอย่างไว้ว่าอาจเหลือ 13 หรือ 11 ภาพ) — ยังไม่ตัดสิน prototype จึงใช้รูปทรงเรขาคณิตแทน
- **ยังไม่กำหนดสไตล์ของอาร์ตเวิร์ก** (การ์ตูน 2 มิติ / พิกเซล / อื่นๆ) ซึ่งจะกระทบว่า `--radius-*` และเงาควรปรับไหม
- **ยังไม่มีโลโก้และชื่อแบรนด์ในรูปแบบกราฟิก** ปัจจุบันใช้ชื่อ War Point เป็นข้อความล้วน
- **ยังไม่ได้ทดสอบโทนสองโซนกับเด็กจริง** ว่าโซนอารีน่าที่มืดลงทำให้ตื่นเต้นตามที่ตั้งใจ หรือทำให้เด็กบางคนรู้สึกอึดอัด
- **ยังไม่กำหนดว่าจะมีโหมดสำหรับเด็กที่มองเห็นไม่ชัดเจนแยกหรือไม่** (เช่น โหมดตัวอักษรใหญ่พิเศษ) — [[docs/01-requirements/01-spec/20260821-01-lesson-and-quiz-system|spec 01]] ระบุว่ามีเด็กกลุ่มนี้อยู่จริง และแก้ไว้บางส่วนแล้วด้วยฟีเจอร์ AI อ่านออกเสียง
- **[เพิ่ม 2026-08-23] สถานะล่าสุดของค่าที่เคยบอกว่า "ยังไม่กำหนด":** เกณฑ์แต้มต่อขั้น**ล็อกแล้ว** (ซื้อไข่ 1 / ร่างที่ 1 จ่ายเพิ่ม 5 / ร่างที่ 2 จ่ายเพิ่ม 10 / ร่างที่ 3 จ่ายเพิ่ม 15 / ร่างที่ 4 จ่ายเพิ่ม 20 แต้ม สะสมรวม 51 แต้ม — [[docs/01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|spec 02]] หัวข้อ ซ.1) ส่วน**ราคาไอเทมช่วยยังไม่กำหนด** ([[docs/01-requirements/01-spec/20260823-06-battle-support-items|spec 06]]) และ**หมวดของแต่งตัวถูกยกเลิกแล้ว** ไม่ใช่แค่ยังไม่กำหนดราคาอีกต่อไป
- **[เพิ่ม 2026-08-23] HP อาจกลายเป็นค่าสเตตัสตัวที่ 4** ซึ่งจะทำให้การหารแต้มสเตตัส 50 ต่อขั้นตามสัดส่วนประจำสายพันธุ์ (ปัจจุบันหารระหว่าง 3 ค่า) **ต้องคิดใหม่ทั้งหมด** — ยังเป็น Open Question ที่ยังไม่ปิดใน [[docs/01-requirements/01-spec/20260823-06-battle-support-items|spec 06]] (ข้อ 2)
