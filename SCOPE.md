# SCOPE.md — war-point (ขอบเขตที่เลือกทำสำหรับการบ้าน Module 1–4)

## ประโยคเดียวสรุปขอบเขต

ระบบเก็บ **การส่งงาน/ทำแบบทดสอบของนักเรียนแต่ละครั้ง (quiz attempt)** ซึ่ง **นักเรียน (student)** เป็นผู้สร้างตอนส่งงาน มีสถานะ **ส่งแล้ว (submitted) → รอครูตรวจ (pending_review) → ตรวจแล้ว (graded)** และ **ครู (teacher)** เป็นผู้เปลี่ยนสถานะตอนให้คะแนน/feedback

## ตารางขอบเขต (9 บรรทัด)

| # | หัวข้อ | รายละเอียดของ war-point |
|---|---|---|
| 1 | โฟลเดอร์หลัก | `quizAttempts` — การส่งงาน/ทำแบบทดสอบแต่ละครั้งของนักเรียน (เทียบเท่า `leaveRequests`) |
| 2 | โฟลเดอร์ users | `students` — นักเรียนที่ใช้ระบบ (รหัสประจำตัว + ชื่อเล่น ไม่มีชื่อจริง ตาม FE-07) |
| 3 | โฟลเดอร์ types | `quizSets` — ชุดแบบทดสอบ/ใบงานที่นักเรียนเลือกทำ (เทียบเท่า `leaveTypes`) |
| 4 | โฟลเดอร์ย่อยเฉพาะรายการ | `quizAttempts/{id}/reviews` — บันทึกการตรวจ/ให้คะแนนของครูต่อการส่งงานครั้งนั้นโดยเฉพาะ (เทียบเท่า `approvals`) |
| 5 | สถานะเริ่มต้น | `submitted` — นักเรียนเพิ่งส่งงาน |
| 6 | สถานะระหว่างทาง | `pending_review` — รอครูตรวจ (เทียบเท่า "รอพิจารณา") |
| 7 | สถานะปลายทาง | `graded` — ครูตรวจให้คะแนนแล้ว (เทียบเท่า "อนุมัติ") |
| 8 | ใครสร้างรายการหลัก | นักเรียน (student) กดส่งงาน/ทำแบบทดสอบ |
| 9 | ใครเปลี่ยนสถานะ + ฟิลด์ที่จดชื่อซ้ำ | ครู (teacher) เปลี่ยนสถานะตอนตรวจ · ฟิลด์ `studentNickname` จดซ้ำไว้ใน `quizAttempts` เพราะ Firestore ไม่มี JOIN — หน้ารายการของครูต้องเห็นชื่อเล่นนักเรียนได้ทันทีโดยไม่ query เพิ่มทีละคน |

## เทียบกับ LeaveEasy

| ในคาบทำกับ LeaveEasy | การบ้านทำกับ war-point | ของฉันคืออะไร |
|---|---|---|
| `leaveRequests` = โฟลเดอร์หลัก | โฟลเดอร์หลักของระบบฉัน | `quizAttempts` |
| `users` = คนที่ใช้ระบบ | คนที่ใช้ระบบฉัน | `students` |
| `leaveTypes` = ประเภทที่เลือกได้ | ประเภท/หมวดที่เลือกได้ในระบบฉัน | `quizSets` |
| `approvals` = โฟลเดอร์ย่อยของใบลาแต่ละใบ | สิ่งที่เป็นของรายการนั้นโดยเฉพาะ | `quizAttempts/{id}/reviews` |
| สถานะ รอพิจารณา → อนุมัติ/ไม่อนุมัติ | สถานะของระบบฉัน | `submitted → pending_review → graded` |
| `requesterName` จดชื่อซ้ำไว้ | ชื่อที่ฉันต้องจดซ้ำ | `studentNickname` (ใน `quizAttempts`) |

## อ้างอิงเอกสารต้นทางในโปรเจกต์นี้

- Entity ต้นทาง: `ENT-08 QUIZ_ATTEMPT`, `ENT-01 STUDENT`, `ENT-05 QUIZ_SET` — [database-schema.md](docs/02-design/02-technical/database-schema.md)
- Feature ต้นทาง: FE-16, FE-17, FE-18, FE-20, FE-41 — [feature-list.md](docs/01-requirements/feature-list.md)

> หมายเหตุ: ขอบเขตนี้คือ **ส่วนย่อยที่ตัดมาทำสำหรับการบ้าน Module 1–4** จากสเปกฉบับเต็มของ war-point ซึ่งออกแบบไว้เป็นระบบเชิงสัมพันธ์ (relational, มี transaction ซับซ้อน 27 entity) — การบ้านนี้จำลองเฉพาะส่วน "นักเรียนส่งงาน → ครูตรวจ" ด้วย Firestore (NoSQL, denormalized) เท่านั้น ไม่ใช่การย้ายทั้งระบบ และยังไม่ผูกกับ schema เชิงสัมพันธ์ในเอกสารหลัก
