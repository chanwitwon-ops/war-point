# BACKLOG.md — สิ่งที่ยังไม่เสร็จ ส่งต่อ Module 3

รวมจาก `spec.md` หัวข้อ 5 (out of scope), ผลตรวจของ `data-auth-engineer`/`ai-quiz-assistant`/`tester` ระหว่างการบ้านที่ 4, และสิ่งที่พบระหว่างทดสอบจริงบนเว็บ

## 🔴 ย้ายคีย์ AI ไปไว้ฝั่งที่ผู้ใช้แตะไม่ได้ (สำคัญที่สุด)

ตอนนี้ `js/ai.js` เรียก OpenRouter **ตรงจากเบราว์เซอร์ (client-side fetch)** — แปลว่าคีย์ OpenRouter ที่อยู่ใน `js/ai-config.js` (แม้จะไม่หลุดขึ้น GitHub) **ยังคงถูกส่งออกไปในทุก request จากเบราว์เซอร์ของทุกคนที่ใช้เว็บจริง** ใครก็ตามที่เปิด DevTools → Network ตอนกดปุ่ม AI จะเห็นคีย์นี้ในหัว `Authorization` ได้ทันที นี่คือข้อจำกัดของสถาปัตยกรรม static-site-only ที่ทำมาตั้งแต่การบ้าน 3 ไม่ใช่บั๊กใหม่ แต่ต้องแก้ก่อนใช้งานจริงกับนักเรียน/ครูจริง — ทางแก้คือเพิ่ม backend เล็กๆ (เช่น Firebase Cloud Function) เป็นตัวกลางเรียก OpenRouter แทน โดยเก็บคีย์ไว้ฝั่งเซิร์ฟเวอร์เท่านั้น หน้าเว็บเรียกมาที่ฟังก์ชันของเราแทนที่จะเรียก OpenRouter ตรง

## ฟีเจอร์จาก Feature List ของ Module 1 ที่ยังไม่ได้ทำ

ขอบเขตการบ้าน 2–4 ทำแค่ FE-16, FE-17, FE-20 (ตรวจอัตโนมัติ/ครูตรวจมือ/นักเรียนทำแบบทดสอบ) ตาม `SCOPE.md` — ที่เหลือใน `docs/01-requirements/feature-list.md` (81 ฟีเจอร์ทั้งหมด) ยังไม่ได้ทำ ที่สำคัญที่สุด:

- **FE-01–FE-08 ระบบตัวตนจริง** — ล็อกอินนักเรียนด้วยรหัสประจำตัว+PIN, ครูล็อกอินด้วย Google, นำเข้ารายชื่อจากไฟล์ Excel/CSV, ครูรีเซ็ต PIN — ตอนนี้ใช้ Firebase Auth อีเมล/รหัสผ่านแทนเป็นตัวจำลองเท่านั้น
- **FE-18 / FE-41** — ครูตั้งจำนวนรอบที่ทำซ้ำได้ต่อชุด + ยึดคะแนนรอบดีที่สุด — ตอนนี้ทำได้รอบเดียวต่อชุดเท่านั้น
- **ระบบ pet/economy, weekly arena, แดชบอร์ดสรุป** — ทั้งหมดอยู่นอกขอบเขตรอบนี้ ดูรายละเอียดเต็มที่ `docs/01-requirements/feature-list.md`

## สิ่งที่เทสต์จับได้แต่ยังไม่ได้แก้

ชุดทดสอบ Playwright ทั้ง 5 (`tests/*.spec.js`) **ผ่านครบ 6/6 ไม่มีจุดไหนตก** (ดู `test-results.md`) — แต่ระหว่างทำการบ้านนี้ (ตรวจด้วยมือ ไม่ใช่จาก Playwright) เจอช่องว่างที่ยังไม่ได้ปิดสนิท:

- **`students`/`quizSets` เขียนได้ทุกคนที่ล็อกอินผ่าน devtools** — `firestore.rules` อนุญาต `write` ให้ทุกคนที่ล็อกอิน (ไม่จำกัด teacher-only) แม้หน้าจอจะไม่มีปุ่มให้ทำ นักเรียนที่รู้วิธียิง request ตรงยังแก้ไข/ลบชุดข้อสอบหรือข้อมูลนักเรียนคนอื่นได้ทาง Firestore SDK โดยตรง
- **หน้าจัดการ `quizSets`** (แก้ไข/ลบชุดข้อสอบของครู) — มีแค่หน้าสร้างใหม่ (`quiz-set-new.html`) ยังไม่มีหน้าแก้/ลบ
- **`quiz-attempts.html` ของ teacher ดึงข้อมูลทั้ง collection มาแสดงโดยไม่แบ่งหน้า (pagination)** — ยังไม่เป็นปัญหาตอนข้อมูลน้อย แต่จะช้าลงเมื่อมีนักเรียน/การส่งงานเยอะขึ้นมาก
- **`worksheet`/`attachment` ยังไม่รองรับการแนบไฟล์จริง** — ตอนนี้ใช้ช่องข้อความพิมพ์แทนคำตอบ/ลิงก์ไฟล์เท่านั้น (Firebase Storage อยู่นอกขอบเขต)
