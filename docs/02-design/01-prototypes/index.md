# 01 - Prototypes

เก็บ **ต้นแบบหน้าตาของระบบ (UI/UX Prototype)** เช่น

- Wireframe / mockup ของแต่ละหน้าจอ
- User flow และ navigation flow
- Interactive prototype แบบกดใช้ได้จริง

ใช้สำหรับสื่อสารและตกลงหน้าตาของระบบก่อนลงมือพัฒนาจริง โดยอ้างอิงความต้องการจาก [[../../01-requirements/01-spec/index|01-spec]] และส่งต่อรายละเอียดเชิงระบบให้ [[../02-technical/index|02-technical]]

## Interactive Prototype (single-file HTML)

สร้างด้วย skill `build-prototype` โฟลเดอร์จะขึ้นต้นด้วย `v{n}-{YYYYMMDD}-{slug}` ภายในมี `index.html` (เปิดด้วย browser ได้ทันที self-contained ไม่ต้องต่ออินเทอร์เน็ต) และ `README.md` (ขอบเขต + click-through path + ข้อจำกัด)

**ต้องมี `DESIGN.md` ที่ root ก่อน** — ถ้ายังไม่มี skill `build-prototype` จะถามโทนสี/สไตล์แล้วสร้างให้

| เวอร์ชัน | วันที่ | ขอบเขต | สถานะ |
|---|---|---|---|
| [[v1-20260821-student-learn-and-pet/README\|v1-20260821-student-learn-and-pet]] | 2026-08-21 | UJ-01 นักเรียนทบทวนบทเรียนและเลี้ยงสัตว์เลี้ยง — 8 หน้าจอ (ล็อกอิน / บทเรียนของฉัน / ทำแบบทดสอบ / ผลคะแนน / สัตว์เลี้ยงของฉัน / ร้านของแต่งตัว / เปลี่ยนสายและอัลบั้มเส้นทางการเติบโต / อารีน่า) ครอบ FE-01, FE-08 ถึง FE-10, FE-16, FE-20 ถึง FE-26, FE-31, FE-32, FE-42, FE-72 ถึง FE-74 — **ฝั่งนักเรียนเท่านั้น ไม่มีหน้าจอหรือข้อมูลของครู** | Draft — รอรีวิว (ยังไม่ผ่าน `prototype-auditor`) |

> ตรวจด้วยการวัดค่าจริงในเบราว์เซอร์แล้ว ได้ผลดังนี้: ไม่มี network request ภายนอกเลย · contrast ไม่ล้มเหลวจาก 262 element ที่มีข้อความ รวม modal ที่เปิดวัดแล้ว · พื้นที่กดจริงผ่านเกณฑ์ 48px ทั้ง 115 จุด · ไม่มีค่า hex อยู่นอกบล็อกประกาศ token · และกฎแต้มของ `20260821-02-pet-points-and-weekly-arena` ทำงานถูกต้อง คือยอดสะสมรวมไม่ลดลงในทุกกรณี ส่วนการปลดล็อกขั้นและการซื้อของหักเฉพาะยอดที่ใช้ได้ และการเปลี่ยนชนิดคืนแต้มเต็มพร้อมกลับไปขั้น 1
