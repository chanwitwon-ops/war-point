# Detailed Design — war-point

- **อัปเดตล่าสุด:** 2026-08-25 — เพิ่ม DD-09 (ดูแลสัตว์เลี้ยงประจำวันและค่าความผูกพัน, FE-82/FE-83, API-63) ไม่มี DD/BR/ERR เดิมถูกแก้ไข
- **ระดับความละเอียด:** Conceptual (pseudo code ระบุลำดับขั้น/เงื่อนไข/ขอบเขต transaction ให้ครบ แต่ไม่ผูกกับภาษาโปรแกรมมิ่งหรือ syntax ของ framework ใดๆ — สอดคล้องกับ [[architecture|architecture]]/[[api-spec|api-spec]]/[[database-schema|database-schema]] ที่ยังเป็น conceptual เช่นกัน)
- **ที่มา:** [[architecture|architecture]], [[api-spec|api-spec]], [[database-schema|database-schema]], [[../../03-testing/01-test-plan/acceptance-criteria|acceptance-criteria]]
- **ส่งต่อไป:** การพัฒนาจริง + [[../../03-testing/01-test-plan/index|01-test-plan]]

รอบแรกครอบคลุมเฉพาะ **8 flow ที่มีการคำนวณ/หักแต้ม/เปลี่ยนสถานะ** ตามที่ผู้ใช้ยืนยันขอบเขต (DD-01 ถึง DD-08) — รอบ 2026-08-25 เพิ่ม DD-09 สำหรับ FE-82/FE-83 (ระบบดูแลสัตว์เลี้ยงประจำวัน) ที่เพิ่มเข้ามาใหม่ — flow อื่นที่เหลือของระบบ (auth, roster, lesson, teacher overview ฯลฯ) ยังไม่ถูกเขียนเป็น Detailed Design ในรอบนี้ และยังไม่มีรหัส DD-XX จอง

## 1. สารบัญ Flow

| ID | Flow | Endpoint หลัก | Feature | ไฟล์ |
|---|---|---|---|---|
| DD-01 | ซื้อไข่และเลือกสายพันธุ์ | API-36 | FE-44, FE-75, FE-76 | [[detailed-design/buy-egg-and-choose-species\|ซื้อไข่และเลือกสายพันธุ์]] |
| DD-02 | ปลดล็อกขั้นการเติบโตถัดไป | API-37 | FE-24, FE-77 | [[detailed-design/unlock-next-growth-stage\|ปลดล็อกขั้นการเติบโตถัดไป]] |
| DD-03 | เปลี่ยนสายพันธุ์/รีเซ็ตตัวละคร | API-38 | FE-26 | [[detailed-design/reset-pet\|เปลี่ยนสายพันธุ์/รีเซ็ตตัวละคร]] |
| DD-04 | ซื้อไอเทมช่วยในการต่อสู้ | API-41 | FE-78 | [[detailed-design/purchase-battle-item\|ซื้อไอเทมช่วยในการต่อสู้]] |
| DD-05 | ส่งคำตอบแบบทดสอบ / ยึดคะแนนรอบที่ดีที่สุด | API-31 | FE-16, FE-20, FE-41 | [[detailed-design/submit-quiz-attempt\|ส่งคำตอบแบบทดสอบ]] |
| DD-06 | ใช้ไอเทมในแมตช์ | API-45 | FE-80, FE-81 | [[detailed-design/use-item-in-match\|ใช้ไอเทมในแมตช์]] |
| DD-07 | ประกาศผลการแข่งขันรายสัปดาห์ (รวม HP) | API-51 | FE-27, FE-31, FE-32, FE-79 | [[detailed-design/announce-weekly-arena-result\|ประกาศผลการแข่งขันรายสัปดาห์]] |
| DD-08 | ยืนยันส่งคะแนนเข้า Google Sheets | API-60 | FE-66, FE-69, FE-70, FE-71 | [[detailed-design/confirm-score-export\|ยืนยันส่งคะแนนเข้า Google Sheets]] |
| DD-09 | ดูแลสัตว์เลี้ยงประจำวัน (ให้อาหาร/อาบน้ำ/ลูบหัว/เล่น) และค่าความผูกพัน | API-63 | FE-82, FE-83 | [[detailed-design/care-for-pet\|ดูแลสัตว์เลี้ยงประจำวันและค่าความผูกพัน]] |

---

## 2. กฎธุรกิจร่วม (Global Business Rules)

| ID | กฎ | ค่า/สูตร | ที่มา (spec หรือ AC) | บังคับใช้ที่ไหน |
|---|---|---|---|---|
| BR-01 | ต้นทุนแต้มต่อขั้นการเติบโต จ่ายแบบ**ขั้นต่อขั้น (incremental)** ไม่ใช่ยอดสะสมที่ต้องมี | ไข่=1, ร่างที่1=+5 (สะสม 6), ร่างที่2=+10 (สะสม 16), ร่างที่3=+15 (สะสม 31), ร่างที่4=+20 (สะสม 51) | AC-FE-24-4~10, AC-FE-75-1~5, [[database-schema#4-ตารางค่าคงที่และ-enum\|database-schema §4]] | DD-01, DD-02, DD-03 |
| BR-02 | แต้มสเตตัสตั้งต้นของไข่ = 50 แต้ม และเพิ่ม +50 ทุกขั้นถัดไป | ไข่=50, ร่าง1=100, ร่าง2=150, ร่าง3=200, ร่าง4=250 (สูงสุด) | AC-FE-75-5, AC-FE-77-1/2/5 | DD-01, DD-02 |
| BR-03 | เงื่อนไขแต้มพอสำหรับซื้อไข่/ปลดล็อกขั้น/ซื้อไอเทม ใช้ `available_points >= ราคา` **(`>=` ไม่ใช่ `>`)** | ตัวอย่างขอบ: มี 14 แต้มปลดร่างที่ 3 (ต้องการ 15) ไม่ได้, มี 15 พอดีได้ | AC-FE-75-2/3, AC-FE-24-8/9 | DD-01, DD-02, DD-04 |
| BR-04 | ยอดแต้มสะสมรวม (`total_earned_points`) ไม่มีวันลดจากการกระทำใดๆ ของนักเรียน (ซื้อไข่/ปลดล็อกขั้น/ซื้อไอเทม/รีเซ็ต) | เพิ่มขึ้นได้จากการแปลงคะแนนแบบทดสอบเท่านั้น | AC-FE-22-2 | DD-01, DD-02, DD-03, DD-04, DD-05 |
| BR-05 | ยอดแต้มที่ใช้ได้ (`available_points`) เท่านั้นที่ถูกหักเมื่อซื้อไข่/ปลดล็อกขั้น/ซื้อไอเทม | หักเฉพาะ `available_points` ไม่แตะ `total_earned_points` | AC-FE-23-1 | DD-01, DD-02, DD-04 |
| BR-06 | สายพันธุ์มี 3 สายคงที่ (ไข่มีปีก/ไข่มีขา/ไข่มีหาง) เลือกได้เฉพาะตอนซื้อไข่เท่านั้น เปลี่ยนสายพันธุ์ของ `PET` ตัวเดิมไม่ได้ (เปลี่ยนสาย = รีเซ็ตสร้างตัวใหม่) | 3 สาย, เลือกครั้งเดียวตอนสร้าง | AC-FE-44-1, [[database-schema#3-รายละเอียดแต่ละ-entity\|ENT-15]] | DD-01, DD-03 |
| BR-07 | นักเรียนมีสัตว์เลี้ยงที่ `is_active=true` ได้เพียง 1 ตัวในเวลาเดียว บังคับด้วย unique partial index ระดับฐานข้อมูล | - | AC-FE-74-1, ENT-15 | DD-01, DD-03 |
| BR-08 | บันทึกเส้นทางการเติบโต (แถว `PET` ที่ `is_active=false`) ต้องไม่ถูกลบเมื่อรีเซ็ต จำนวนรายการต้องเพิ่มขึ้นอย่างเดียวไม่ว่ารีเซ็ตกี่ครั้ง | - | AC-FE-73-1/2 | DD-03 |
| BR-09 | เมื่อนักเรียนรีเซ็ต ระบบคืนแต้มทั้งหมดที่เคยใช้ไปกับสัตว์เลี้ยงตัวนั้นเต็มจำนวน **รวมแต้มที่ใช้ซื้อไอเทมด้วย** (`entry_type IN ('egg_purchase','stage_unlock','item_purchase')`) ทำให้ `available_points` กลับมาเท่ากับ `total_earned_points` พอดี (ตรงกับกฎเดิมข้อ ข.3 ทุกประการ ไม่มีข้อยกเว้น) — แลกกับการที่ `STUDENT_ITEM_INVENTORY` ทั้งหมดของนักเรียนคนนั้นถูกเคลียร์ทิ้งพร้อมกัน (`quantity=0`, soft-delete) ในธุรกรรมเดียวกัน เพื่อปิดช่องโหว่ "รีเซ็ตแล้วได้ทั้งไอเทมและแต้มคืนพร้อมกัน" — PET เดิมถูกตั้ง `is_active=false` เท่านั้น **ไม่ insert PET ใหม่ทันที** นักเรียนต้องเรียก API-36 (DD-01) แยกต่างหากเพื่อซื้อไข่ใหม่และจ่าย 1 แต้ม | ดูสูตรอ้างอิงตาราง BR-01 (คืนเต็มจำนวนตามขั้นปัจจุบัน + ยอดที่เคยใช้ซื้อไอเทมทั้งหมด) | spec 06 Open Question ข้อ 6 (ปิดแล้ว 2026-08-23), spec 02 ข้อ ข.3/ซ.6, [[database-schema#3-รายละเอียดแต่ละ-entity\|database-schema.md ENT-15/ENT-18]] | DD-03 |
| BR-10 | ไอเทมช่วยในการต่อสู้ไม่กระทบค่าสเตตัส/`PET.stat_total`/`PET_STAT` เลยไม่ว่าจุดใด | - | spec 06 ข้อ ค., ENT-17 | DD-04, DD-06, DD-07 |
| BR-11 | ไอเทมหนึ่งชนิดใช้ได้อย่างมาก 1 ครั้งต่อฝ่ายต่อแมตช์ บังคับด้วย unique constraint `(arena_match_id, student_id, shop_item_id)` ระดับฐานข้อมูล ไม่ใช่แค่ disable ปุ่มที่ client | - | spec 06 ข้อ ก., ENT-22 | DD-06 |
| BR-12 | การแพ้ไม่มีผลเสียใดๆ ต่อสัตว์เลี้ยงเลย — ไม่เสียแต้ม ไม่ลดขั้น ไม่เปลี่ยนสภาพ ไม่ว่าจะแพ้กี่ครั้งก็ตาม | - | AC-FE-27-2, spec 06 ข้อ ข. | DD-06, DD-07 |
| BR-13 | HP ถึง 0 ของฝ่ายใด = ตายทันที = จบแมตช์ทันที นับเป็นแพ้ "ตาย" เป็นสภาพภายในแมตช์เท่านั้น ไม่ถูกนำไปกระทบ `PET`/`POINTS_ACCOUNT` เลย | - | spec 06 ข้อ ข. | DD-07 |
| BR-14 | สถานะแมตช์/สัปดาห์ต้องแยก "คำนวณแล้วรอประกาศ" (`computed`) กับ "ประกาศแล้ว" (`announced`) อย่างชัดเจน ระบบห้ามประกาศผลเองก่อนครูกดสั่งแม้คำนวณเสร็จแล้ว | - | AC-FE-31-1/2 | DD-07 |
| BR-15 | **("ซ่อน ไม่ใช่ disable")** response ทุก endpoint ใต้ `/teacher/...` ต้องไม่มี field แต้มสะสมรวม/แต้มที่ใช้ได้/ขั้นการเติบโต/ค่าสเตตัส/สายพันธุ์/HP ในแมตช์/ไอเทมที่ใช้ของนักเรียนคนใดปรากฏเลยแม้แต่ field เดียว ต้องกรองที่ query/service layer ก่อนส่ง ไม่ใช่กรองที่ client | - | architecture.md ข้อ 7, api-spec.md ข้อ 1.7, AC-FE-27-3, AC-FE-58-2/3, AC-FE-63-2 | DD-07, DD-08 (และทุก endpoint ฝั่งครูโดยทั่วไป) |
| BR-16 | ยึดคะแนนรอบที่ดีที่สุด: แต้มที่ได้จากชุดแบบทดสอบหนึ่งคำนวณจาก `best_score` เท่านั้น รอบใหม่ที่แย่กว่าเดิม**ห้ามแก้** `best_score`/`points_awarded_total`/แต้มใดๆ รอบที่คะแนนเท่ากับ best เดิมพอดีไม่ถือว่า "ดีกว่า" (ยึดรอบแรกที่ทำได้คะแนนนั้น) | - | AC-FE-41-1/2, ENT-09 | DD-05 |
| BR-17 | การส่งคำตอบแบบทดสอบต้องกันการส่งซ้ำด้วย `Idempotency-Key` → `client_request_id` — คำขอที่ key ซ้ำกับที่สำเร็จแล้วต้องคืนผลลัพธ์เดิม ไม่สร้าง attempt ใหม่ ไม่บวกแต้มซ้ำ | - | DF-01 ของ architecture.md, ENT-08 | DD-05 |
| BR-18 | การส่งออกคะแนนต้องจับคู่นักเรียนด้วย**รหัสประจำตัวเท่านั้น** (ห้ามใช้ชื่อ/ลำดับแถว) แถวที่จับคู่ไม่ได้ต้องถูกข้ามและรายงานไว้ ห้ามเขียนทับแถวเหล่านั้นเด็ดขาดไม่ว่าขั้นตอนใด | - | AC-FE-69-1/2, AC-FE-70-1/2 | DD-08 |
| BR-19 | ห้ามเขียน/ส่งข้อมูลคะแนนจริงไปยังปลายทางใดๆ จนกว่า `SCORE_EXPORT_JOB.status='validated'` และครูกดยืนยันส่งแล้วเท่านั้น | - | AC-FE-71-1/2/3 | DD-08 |
| BR-20 | เพดานความหิวจาก action "ให้อาหาร" (`feed`) ทำสำเร็จได้สูงสุด 3 ครั้ง/วัน ใช้เงื่อนไข `hunger_today < 3` (ไม่ใช่ `<=`) | 3 ครั้ง/วัน | AC-FE-82-1, AC-FE-82-2 | DD-09 |
| BR-21 | เพดานรวมค่าความผูกพันต่อวัน (นับรวมทุก action) สูงสุด 10 หน่วย/วัน ใช้เงื่อนไข `daily_bonding_quota_used < 10` (ไม่ใช่ `<=`) | 10 หน่วย/วัน | AC-FE-83-2, AC-FE-83-3 | DD-09 |
| BR-22 | Action ดูแลสัตว์เลี้ยงทั้ง 4 แบบที่นับผลสำเร็จ ได้ค่าความผูกพัน +1 หน่วยเท่ากันทุกแบบ ไม่มีน้ำหนักต่างกันตาม `actionType` | +1 หน่วย/ครั้ง | AC-FE-83-1 | DD-09 |
| BR-23 | Action ดูแลสัตว์เลี้ยงทั้ง 4 แบบต้องไม่มีผลใดๆ ต่อ `available_points`/`total_earned_points` (`POINTS_ACCOUNT`) และไม่มีผลใดๆ ต่อ `PET_STAT` ไม่ว่ากรณีใด | ค่าคงที่เสมอ | AC-FE-82-4, AC-FE-82-5 | DD-09 |
| BR-24 | Lazy reset รายวันของ `PET_CARE_STATE`: เมื่อ `last_recorded_date` ไม่ตรงกับวันปัจจุบัน ตั้ง `hunger_today=0`, `daily_bonding_quota_used=0` ก่อนประมวลผล **ห้ามแตะ `total_bonding_score`** | รีเซ็ตทุกวันใหม่ ยอดสะสมรวมไม่ลด | AC-FE-83-4 | DD-09 |

---

## 3. แคตตาล็อกข้อผิดพลาดร่วม (Error Catalog)

| ID | สถานการณ์ | Status | รหัสข้อผิดพลาด | ข้อความถึงผู้ใช้ | ระบบต้องทำอะไรต่อ |
|---|---|---|---|---|---|
| ERR-01 | ไม่มี session หรือ session หมดอายุ | 401 | UNAUTHENTICATED | "กรุณาเข้าสู่ระบบใหม่อีกครั้ง" | ปฏิเสธคำขอทันที ไม่ประมวลผลใดๆ ต่อ ใช้กับทุก endpoint ที่ต้องมี session |
| ERR-02 | Role ของ session ไม่ตรงกับที่ endpoint กำหนด (เช่น นักเรียนเรียก endpoint ครู) | 403 | FORBIDDEN_ROLE | "ไม่มีสิทธิ์ทำรายการนี้" | ปฏิเสธก่อนแตะ business logic ใดๆ — ใช้กับ DD-07/DD-08 (teacher-only) และป้องกัน DD-01/02/03/04/05/06 จากผู้ใช้ที่ไม่ใช่นักเรียน |
| ERR-03 | ไม่พบทรัพยากรที่ระบุ หรือทรัพยากรนั้นไม่ใช่ของผู้เรียก (pet/quiz/match/shop item/export job) | 404 | NOT_FOUND / NO_ACTIVE_PET | ข้อความเฉพาะ endpoint เช่น "ไม่พบสัตว์เลี้ยง — ไปซื้อไข่ตัวแรกกันเถอะ", "ไม่พบแมตช์นี้" | ไม่เปิดเผยว่าทรัพยากรนั้นมีอยู่จริงของคนอื่นหรือไม่ (กันการเดา id ของเพื่อน) |
| ERR-04 | `available_points` ไม่พอสำหรับราคาที่ต้องจ่าย | 422 | INSUFFICIENT_POINTS | "ยังสะสมแต้มที่ใช้ได้ไม่พอ ต้องมีอย่างน้อย {required} แต้ม (ตอนนี้มี {available} แต้ม)" — เป็นกลาง ไม่ตำหนิ (DESIGN.md ข้อ 4.1) | ไม่หักแต้มใดๆ, ไม่สร้าง entity ใหม่ใดๆ, transaction ทั้งหมด rollback |
| ERR-05 | นักเรียนมีสัตว์เลี้ยงที่ `is_active=true` อยู่แล้ว พยายามซื้อไข่ซ้ำ | 409 | ACTIVE_PET_EXISTS | "มีสัตว์เลี้ยงที่ใช้งานอยู่แล้ว — ใช้เมนูรีเซ็ตถ้าต้องการเปลี่ยนสายพันธุ์" | ไม่สร้าง PET ใหม่ ไม่หักแต้ม |
| ERR-06 | สัตว์เลี้ยงอยู่ที่ร่างที่ 4 (สูงสุด) แล้ว พยายามปลดล็อกอีก | 409 | STAGE_ALREADY_MAX | "สัตว์เลี้ยงของคุณโตเต็มที่แล้วในรอบทดลองนี้" | ไม่หักแต้ม ไม่เปลี่ยนสถานะ |
| ERR-07 | ข้อมูลนำเข้าไม่ถูกต้อง/ไม่ครบ (เช่น `confirm` ไม่ใช่ `true`, ไม่ส่ง `speciesId`, ตอบไม่ครบทุกข้อ) | 400 | VALIDATION_ERROR | ข้อความเฉพาะ endpoint ตามที่ระบุใน api-spec.md | ไม่ประมวลผลต่อ ไม่มีข้อมูลใดถูกเขียน |
| ERR-08 | ใช้ไอเทมเดียวกันในแมตช์เดียวกันซ้ำ | 409 | ITEM_ALREADY_USED | "ใช้ไอเทมนี้ในแมตช์นี้ไปแล้ว" | ไม่หักคลังไอเทมซ้ำ ไม่บันทึกการใช้ซ้ำ |
| ERR-09 | แมตช์ประกาศผลแล้ว พยายามใช้ไอเทมอีก | 409 | MATCH_ALREADY_ANNOUNCED | "แมตช์นี้จบไปแล้ว ใช้ไอเทมไม่ได้อีก" | ปฏิเสธก่อนแตะคลังไอเทม |
| ERR-10 | ไม่มีไอเทมนั้นถือครองอยู่ (`quantity <= 0`) | 422 | ITEM_NOT_OWNED | "ไม่มีไอเทมนี้ในคลังของคุณ" | ไม่บันทึกการใช้ไอเทม |
| ERR-11 | ทำครบจำนวนรอบแล้ว หรือชุดยังไม่เปิด/ปิดรับคำตอบแล้ว | 403 | ATTEMPT_LIMIT_REACHED / QUIZ_NOT_OPEN | "ทำครบจำนวนรอบที่กำหนดแล้ว" / "แบบทดสอบชุดนี้ยังไม่เปิดหรือปิดรับคำตอบแล้ว" | ไม่สร้าง attempt ใหม่ |
| ERR-12 | `Idempotency-Key` ซ้ำกับคำขอก่อนหน้าที่สำเร็จแล้ว (ไม่ใช่ error จริง) | 200 | DUPLICATE_REQUEST (คืนผลลัพธ์เดิม) | ไม่มี — คืนผลลัพธ์เดิมเหมือนครั้งแรก | ไม่สร้าง attempt/ledger entry ใหม่ ไม่บวกแต้มซ้ำ |
| ERR-13 | ไม่มีคู่แข่งใดของสัปดาห์นี้ให้ประกาศผล | 409 | NO_MATCHES | "ยังไม่มีคู่แข่งของสัปดาห์นี้ให้ประกาศผล" | ไม่เปลี่ยนสถานะใดๆ |
| ERR-14 | งานส่งออกยังไม่ผ่านสถานะ `validated` ก่อนกดยืนยัน | 409 | NOT_VALIDATED_YET | "กรุณาตรวจสอบข้อมูลก่อนส่งก่อนกดยืนยัน" | ไม่เขียนข้อมูลใดๆ ไปปลายทาง |
| ERR-15 | ระบบภายนอก (Google Sheets ผ่าน CMP-14/CMP-19) เขียนไม่สำเร็จระหว่างทาง | 502 | UPSTREAM_WRITE_FAILED | "ส่งคะแนนไม่สำเร็จบางส่วน กรุณาตรวจสอบและลองใหม่" | ตั้ง `status='failed'` ไม่ตั้งเป็น `completed`; ต้องไม่ปล่อยให้ครูเข้าใจผิดว่าสำเร็จทั้งหมด |
| ERR-16 | ไอเทมที่ต้องการซื้อยังไม่มีราคาจริงถูกกำหนด (`price_points` เป็นค่าที่ยังไม่ล็อก — ดู Gap) | 409 | ITEM_NOT_PRICED | "ไอเทมนี้ยังไม่เปิดให้ซื้อในตอนนี้" | มาตรการป้องกันที่เอกสารนี้เพิ่มเติมเอง เพื่อไม่ให้ระบบยอมให้ซื้อด้วยราคา null/0 โดยไม่ตั้งใจ ก่อนที่ราคาไอเทมจะถูกล็อกจริง |
| ERR-17 | มี `PET` ที่ `is_active=true` แต่หาแถว `PET_CARE_STATE` คู่กันไม่เจอ (ผิดจาก precondition ที่ควรมีเสมอตาม DD-01 ขั้นตอน f2 — ข้อผิดพลาดของข้อมูลภายใน ไม่ใช่ error จากผู้ใช้) | 500 | INTERNAL_DATA_ERROR | "เกิดข้อผิดพลาดภายใน กรุณาลองใหม่ภายหลัง" | rollback ทันที ไม่ตอบสำเร็จบางส่วน บันทึก log ภายในเพื่อตรวจสอบ data integrity |

---

## 4. ข้อกำหนดความปลอดภัยระดับจุด (Security)

| หัวข้อ | ข้อกำหนด | ใช้กับ endpoint ใด | ที่มา |
|---|---|---|---|
| Input validation | ทุก endpoint ต้อง validate ชนิด/ค่าที่อนุญาตที่ชั้น server เสมอ ไม่พึ่งการ validate ฝั่ง client (เช่น `speciesId` ต้องอยู่ใน `SPECIES.is_active=true` เท่านั้น, `confirm` ต้องเป็นค่า boolean `true` ตรงตัว ไม่ใช่ truthy string ใดๆ) | API-36, API-37, API-38, API-41, API-31, API-45, API-51, API-60 | api-spec.md ข้อ 1.5 และรายละเอียดต่อ endpoint |
| กันกดซ้ำ/ยิงซ้ำ (idempotency & double-submit guard) | endpoint ที่หักแต้ม/เปลี่ยนสถานะทุกตัวต้องกันกดซ้ำ/retry ด้วย DB-level unique constraint หรือ `Idempotency-Key` ไม่ใช่แค่ disable ปุ่มที่ client | API-36, API-37, API-38, API-41, API-45, API-31, API-60 | DF-01 ของ architecture.md, unique index ต่างๆ ใน database-schema.md |
| Transaction & locking (race condition) | ทุกจุดที่หักแต้ม (`POINTS_ACCOUNT`) ต้องอยู่ใน database transaction เดียวกับการเขียน ledger/entity ที่เกี่ยวข้อง พร้อม row lock กันแย่งแก้พร้อมกันจากหลายอุปกรณ์/หลายแท็บ | API-36, API-37, API-38, API-41, API-31 | architecture.md ข้อ 1, database-schema.md ENT-10 |
| Data masking ("ซ่อน ไม่ใช่ disable") | response ทุก endpoint ใต้ `/teacher/...` ต้องกรอง field แต้ม/ค่าสเตตัส/ขั้น/สายพันธุ์/HP/ไอเทมของนักเรียนที่ชั้น service/serializer ก่อนตอบกลับ | API-47, API-51, API-53, API-55, API-56–61 | architecture.md ข้อ 7, api-spec.md ข้อ 1.7 |
| Authorization ownership check | endpoint ที่อ้างอิง resource id ต้องตรวจว่าทรัพยากรนั้นเป็นของผู้เรียกเองจริง (`matchId` เป็นคู่ของตนเอง, `jobId` เป็นของครูคนนี้) ไม่ใช่แค่ตรวจ role ถูกต้องอย่างเดียว | API-45, API-46, API-56–61 | AC-FE-42-2, architecture.md ข้อ 7 |
| ความปลอดภัยของ external call | การเขียนไป Google Sheets ต้องไม่ commit สถานะ "สำเร็จ" ใน DB ก่อนเขียนสำเร็จจริง และต้องรายงานความล้มเหลวบางส่วนอย่างชัดเจน ไม่ปล่อยให้ครูเข้าใจผิดว่าสำเร็จทั้งหมด | API-60 | DF-04 ของ architecture.md |

---

## 5. ช่องว่างที่พบ (Gap)

- ~~**G-DD-01 (Open Decision สำคัญ, ต้องยืนยันก่อนพัฒนาโค้ดจริง):** ช่องโหว่ "รีเซ็ตแล้วได้แต้มค่าไอเทมคืนวนซ้ำ" — [[detailed-design/reset-pet|reset-pet]] (DD-03) เขียนตาม BR-09 ซึ่งเป็น**สมมติฐานที่ผู้ช่วยเสนอ** (คืนเฉพาะแต้มค่าไข่+ค่าขั้นของ PET เดิม ไม่คืนแต้มค่าไอเทม) เพราะตรงกับเจตนาเดิมของกฎคืนแต้มมากที่สุด (กฎเดิมมีไว้กันการทดลองเปลี่ยนสายพันธุ์เสียเปรียบ ไม่ได้มีไว้ให้ปั๊มไอเทมฟรี) **แต่ยังไม่ผ่านการยืนยันจาก user อย่างเป็นทางการ** ต้องเปิด requirement ใหม่ยืนยันก่อนพัฒนาโค้ดจริง — ผลข้างเคียงสำคัญที่ต้องแจ้ง user พร้อมกัน: การใช้กฎนี้จะทำให้ค่าคงที่เดิมที่ [[database-schema#3-รายละเอียดแต่ละ-entity|database-schema.md ENT-10/ENT-15]] และ test case `TC-FE-26-002` ยึดไว้ ("`available_points` กลับมาเท่ากับ `total_earned_points` พอดีหลังรีเซ็ต") **จะไม่เป็นจริงอีกต่อไปในกรณีที่นักเรียนเคยซื้อไอเทมมาก่อนรีเซ็ต** — ต้องแก้ทั้ง database-schema.md, test case และ AC ที่เกี่ยวข้องพร้อมกันเมื่อ user ตัดสินใจปิด Gap นี้~~ **ปิดแล้ว (2026-08-23):** user ปฏิเสธข้อเสนอข้างต้นและยืนยันกฎที่ถูกต้องแทน — เมื่อรีเซ็ต ระบบคืนแต้มทั้งหมด **รวมแต้มค่าไอเทมด้วย** ทำให้ `available_points` กลับมาเท่ากับ `total_earned_points` พอดีตามกฎเดิมข้อ ข.3 ทุกประการ (ไม่มีข้อยกเว้น ตรงกับที่ `TC-FE-26-002` ยึดไว้อยู่แล้ว ไม่ต้องแก้ test case) แต่เพิ่มกลไกใหม่ปิดช่องโหว่แทน: เคลียร์ `STUDENT_ITEM_INVENTORY` ทั้งหมดของนักเรียนคนนั้นพร้อมกันในธุรกรรมเดียวกัน (`quantity=0`) ทำให้ไม่สามารถได้ทั้งไอเทมและแต้มคืนพร้อมกัน — อ้างอิง spec 06 Open Question ข้อ 6 (ปิดแล้ว 2026-08-23) และ database-schema.md ที่แก้ตามแล้ว (ENT-15, ENT-18) ดู BR-09 (แก้ไขแล้ว) และ [[detailed-design/reset-pet|reset-pet]] (DD-03, แก้ไขแล้ว) สำหรับรายละเอียด
- ~~**G-DD-02 (ความขัดแย้งที่พบระหว่างเอกสารต้นทาง):** database-schema.md ข้อ ENT-15 ระบุว่า transaction การรีเซ็ตต้อง "(2) INSERT แถวใหม่ `stage='egg'`, `stat_total=50`" ทันที แต่ `api-spec.md` API-38 (request/response จริง) และ spec 02 ข้อ ซ.6 ("ต้องจ่าย 1 แต้มซื้อไข่ใหม่") ยืนยันตรงกันว่าการรีเซ็ต**ไม่สร้าง `PET` ใหม่ทันที** นักเรียนต้องไปซื้อไข่ผ่าน API-36 (DD-01) อีกครั้งเพื่อเลือกสายพันธุ์ใหม่และจ่าย 1 แต้ม — เอกสารนี้ยึดตาม api-spec.md/spec 02 (ระบุ request/response และกฎธุรกิจชัดเจนกว่า) ในการเขียน [[detailed-design/reset-pet|reset-pet]] และ**รายงานความขัดแย้งนี้กลับให้ user/ทีมแก้ database-schema.md ข้อ ENT-15 ในรอบ `sync-api-db` ถัดไป** ไม่ใช่หน้าที่ของเอกสารนี้ที่จะแก้ไฟล์นั้นเอง~~ **ปิดแล้ว (2026-08-23):** database-schema.md ข้อ ENT-15 ถูกแก้ให้ตรงกับ api-spec.md/spec 02 แล้ว — ยืนยันว่าการรีเซ็ตทำแค่ (1) ปิดใช้งาน `PET` เดิม (2) คืนแต้มเต็มจำนวนรวมค่าไอเทม (3) เคลียร์ `STUDENT_ITEM_INVENTORY` ทั้งหมด และ**ไม่ insert `PET` ใหม่ทันที** นักเรียนต้องเรียก API-36 (DD-01) แยกต่างหาก — ไม่มีความขัดแย้งระหว่างเอกสารต้นทางอีกต่อไป
- **G-DD-03:** สูตรตัดสินแพ้-ชนะและสูตรคำนวณ HP ใน [[detailed-design/announce-weekly-arena-result|announce-weekly-arena-result]] (DD-07) ยังไม่มีจาก spec (ต้นทาง: architecture.md ข้อ 8/9, database-schema.md G-DB-04, api-spec.md G-API-04) — เอกสารนี้เขียนเฉพาะโครง pseudo code เชิงแนวคิด (เปรียบเทียบค่าสเตตัสรวม ปรับด้วยผลไอเทม แล้วลด HP ทีละรอบจนถึง 0) **ไม่มีตัวเลขหรือสูตรคณิตศาสตร์ที่แต่งขึ้นเอง** ต้องรอ user ตัดสินใจก่อนเขียนโค้ดจริง
- **G-DD-04:** กฎการถือครองไอเทม (จำนวนถือครองสูงสุด/ซื้อซ้ำได้ไหม/เก็บข้ามสัปดาห์ได้ไหม/ใช้แล้วหักจำนวนหรือไม่) ยังไม่ล็อกใน spec 06 ข้อ 5 (ต้นทาง: database-schema.md G-DB-06, api-spec.md G-API-03) — [[detailed-design/purchase-battle-item|purchase-battle-item]] (DD-04) และ [[detailed-design/use-item-in-match|use-item-in-match]] (DD-06) ออกแบบ schema/logic ให้รองรับได้ทั้งแบบ unlimited และ capped แต่**เลือกพฤติกรรม "ใช้แล้วหักจำนวน (หมดไป)" เป็นค่าเริ่มต้นในการเขียน pseudo code** เพราะสอดคล้องกับความหมายคำว่า "ล้างสถานะได้ 1 ครั้ง"/"ป้องกันการโจมตีถึงตายได้ 1 ครั้ง" ของ spec 06 มากที่สุด — **ยังต้องรอ user ยืนยันอย่างเป็นทางการก่อนพัฒนาโค้ดจริง**
- **G-DD-05:** ราคาไอเทมช่วยในร้าน (`SHOP_ITEM.price_points`) ยังไม่ล็อก (ต้นทาง: database-schema.md G-DB-05, api-spec.md G-API-02) — [[detailed-design/purchase-battle-item|purchase-battle-item]] (DD-04) เพิ่มมาตรการป้องกัน ERR-16 (ปฏิเสธการซื้อถ้าราคายังไม่ถูกกำหนดค่าจริง) เพื่อไม่ให้ระบบยอมให้ซื้อด้วยราคา null/0 โดยไม่ตั้งใจ — ไม่ใช่การเดาราคาเอง
- **G-DD-06:** อัตราแปลงคะแนนแบบทดสอบเป็นแต้ม (points conversion ratio) ที่ใช้ใน [[detailed-design/submit-quiz-attempt|submit-quiz-attempt]] (DD-05) มีเพียง AC-FE-21-1 ที่ยืนยันว่า "คำนวณจากสัดส่วนข้อถูกคูณคะแนนเต็ม" และมีหมายเหตุ "ค่าสมมติ" ว่าอัตราแปลงคะแนนเป็นแต้ม 1:1 มาจาก mock ของ prototype ไม่ใช่ค่าที่ spec ล็อกอย่างเป็นทางการ — pseudo code ของ DD-05 อ้างอิงสูตรเชิงสัดส่วนตาม AC-FE-21-1 เท่านั้น ไม่ผูกอัตราส่วนตายตัว
- **G-DD-07:** นโยบายส่งคะแนนซ้ำรอบสอง (เขียนทับ/เพิ่มคอลัมน์ใหม่/ถามครูก่อน) และรายละเอียดการรายงานว่าแถวใดเขียนสำเร็จไปแล้วบ้างเมื่อ Google Sheets ล่มกลางทาง ยังไม่ล็อกใน spec 05 (ต้นทาง: database-schema.md G-DB-09, api-spec.md G-API-09) — [[detailed-design/confirm-score-export|confirm-score-export]] (DD-08) ออกแบบ `SCORE_EXPORT_JOB` เป็น "งานใหม่ทุกครั้ง" ตามที่ schema ปัจจุบันรองรับ และระบุ ERR-15 (`UPSTREAM_WRITE_FAILED`) เป็นทางออกชั่วคราวเมื่อเขียนไม่สำเร็จ แต่ยังไม่มีกลไกรายงานระดับรายแถวว่าแถวใดสำเร็จแล้วบ้าง — รอ user ตัดสินใจ
- **G-DD-08:** FE-79 ถึง FE-81 (HP และไอเทมในแมตช์) และ FE-26/FE-78 (รีเซ็ต/ซื้อไอเทม) **ยังไม่มีรหัส AC อย่างเป็นทางการ** ใน acceptance-criteria.md (AC ปิดเมื่อ 2026-08-22 ก่อน spec 06 เพิ่มเข้ามาวันถัดไป, และ FE-26/FE-78 เป็นระดับ Could ตามกฎ MoSCoW ของเอกสาร AC — ดู Coverage Gap G-06/G-08 ของ acceptance-criteria.md) — [[detailed-design/reset-pet|reset-pet]] (DD-03), [[detailed-design/purchase-battle-item|purchase-battle-item]] (DD-04), [[detailed-design/use-item-in-match|use-item-in-match]] (DD-06), [[detailed-design/announce-weekly-arena-result|announce-weekly-arena-result]] (DD-07) จึงอ้างอิง spec ต้นทาง (spec 02, spec 06) และ test case (`TC-FE-26-*`) โดยตรงแทนรหัส AC ที่ยังไม่มี — เมื่อ acceptance-criteria.md ถูกขยายให้ครอบคลุม FE เหล่านี้ในรอบถัดไป ต้องย้อนกลับมาเติมรหัส AC ในไฟล์ DD ที่เกี่ยวข้อง
- **ไม่มีช่องว่างอื่นที่กระทบโครงสร้าง flow ระดับนี้นอกเหนือจากที่ระบุข้างต้น** — ตัวเลขที่ล็อกแล้วทั้งหมด (ตารางต้นทุนแต้ม, แต้มสเตตัส, จำนวนสายพันธุ์) ตรงกับ acceptance-criteria.md และ database-schema.md ทุกจุดที่ตรวจสอบ
