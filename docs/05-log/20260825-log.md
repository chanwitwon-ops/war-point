# Log 2026-08-25

- แก้ `docs/02-design/02-technical/non-functional-requirement.md` (mode: update, placement: standalone) — แก้เฉพาะเนื้อหา `NFR-10` (Usability — contrast/tap target) ไม่แตะรหัส/ข้ออื่นใดในเอกสาร: audit พบว่า NFR-10 เขียนเกณฑ์ contrast ง่ายเกินจริงเป็นค่าเดียว "ทุก element ต้องมี contrast ≥ 4.5:1" ทั้งที่อ้างว่าอ้างอิงจาก `DESIGN.md` ข้อ 4.2 โดยตรง แต่ `DESIGN.md` ข้อ 4.2 (บรรทัด 279) ระบุ 2 เกณฑ์แยกตามประเภท element (ข้อความปกติ ≥ 4.5:1 · ข้อความใหญ่ตั้งแต่ `--text-xl` ขึ้นไปและองค์ประกอบที่ไม่ใช่ข้อความ ≥ 3:1) ซึ่งตรงกับที่ `test-plan.md` หัวข้อ 2.4 จับคู่ไว้ถูกต้องอยู่แล้ว — แก้ทั้งแถวในตารางสรุป (หัวข้อ 2) และย่อหน้ารายละเอียด (หัวข้อ 3 NFR-10) ให้ระบุ 2 เกณฑ์ตรงกับ `DESIGN.md` แทนค่าเดียวเดิม พร้อมอัปเดต "อัปเดตล่าสุด" ที่ header เป็น 2026-08-25
- ไม่ได้แตะ NFR-XX ข้ออื่น, ไม่เพิ่ม/ลบข้อ, ไม่แตะไฟล์อื่นนอกเหนือจากไฟล์นี้
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- สร้างเอกสาร requirement ใหม่: [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] — บันทึกกลไกดูแลสัตว์เลี้ยงแบบทามาก็อตจิ (ให้อาหาร/อาบน้ำ/ลูบหัว/เล่น-ออกกำลังกาย) พร้อมหลอดสถานะ 3 ตัวที่รีเซ็ตทุกเที่ยงคืน และค่าความผูกพัน (เพดาน 10 หน่วย/วัน) ที่แยกขาดจากแต้ม (FE-23) และค่าสเตตัสต่อสู้ (FE-77) โดยสิ้นเชิง และรีเซ็ตเป็น 0 เมื่อเปลี่ยนสายพันธุ์/รีเซ็ตตัวละคร (FE-26) — อัปเดต `docs/01-requirements/backlog.md` เพิ่มแถวใหม่ (สถานะ Draft) ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ 6 ไฟล์ใน `docs/02-design/02-technical/detailed-design/` (mode: update, ไม่แตะรหัส DD-XX/BR-XX/ERR-XX เดิม, ไม่แตะ hub `detailed-design.md`) เพราะ audit ยืนยันด้วย `mermaid.parse()` จริงว่าทุกบล็อก `stateDiagram-v2` ใน 6 ไฟล์นี้ parse ไม่ผ่าน จากการเขียนชื่อ state เป็น string ครอบ double quote ตรงๆ ในเส้น transition (syntax ที่ `stateDiagram-v2` ไม่รองรับ ต่างจาก `flowchart`/`sequenceDiagram`) — แก้โดยประกาศ `state "ข้อความไทยเดิม" as <shortId>` ก่อน แล้วเขียนเส้น transition ด้วย shortId ที่ไม่มี quote ตามด้วย label ภาษาไทยบนเส้นแบบเดิม (label บนเส้นครอบ quote ได้ปกติ) ไม่เปลี่ยนความหมาย/เนื้อหา diagram เดิม ไม่มี `;` ปิดบรรทัด:
  - `buy-egg-and-choose-species.md` (DD-01) — 2 state (s_none, s_egg)
  - `unlock-next-growth-stage.md` (DD-02) — 5 state (s_egg, s_stage1, s_stage2, s_stage3, s_stage4)
  - `reset-pet.md` (DD-03) — 5 state (s_active, s_inactive, s_album, s_inv_before, s_inv_after) รวมแก้ `note right of` ให้ชี้ไป shortId แทนสตริงเดิม
  - `submit-quiz-attempt.md` (DD-05) — 3 state (s_submitted, s_graded, s_pending_review)
  - `announce-weekly-arena-result.md` (DD-07) — 3 state (s_pending, s_computed, s_announced)
  - `confirm-score-export.md` (DD-08) — 4 state (s_validated, s_inprogress, s_completed, s_failed)
  - ตารางอธิบาย state transition ใต้ diagram ในทุกไฟล์ไม่ถูกแตะ (แก้เฉพาะบล็อก mermaid ตามที่สั่ง)
- แก้ traceability ที่ตกหล่น: เพิ่มการอ้างอิง `AC-FE-23-2` เข้าไปในแถว `ERR-04` (คอลัมน์ Status + ข้อความ) ของตาราง "กรณีผิดพลาดและการรับมือ" ใน 3 ไฟล์ที่ audit พบว่า implement ข้อความปฏิเสธแบบเป็นกลาง ไม่ตำหนิผู้ใช้ตรงตามกฎนี้อยู่แล้ว แต่ไม่เคยอ้างรหัสไว้ — ไม่เปลี่ยนเนื้อหาอื่นในแถว:
  - `buy-egg-and-choose-species.md`
  - `unlock-next-growth-stage.md`
  - `purchase-battle-item.md`
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] (mode: update, เฉพาะ 2 จุด) — เติมตัวอย่าง animation ของ action "อาบน้ำ" ในหัวข้อ จ. ตามที่ผู้ใช้ยืนยันเพิ่มเติม: ตัวสัตว์เลี้ยงแช่อยู่ในบ่อน้ำร้อน/ออนเซ็น (เห็นแค่ส่วนบนของตัวโผล่จากน้ำ ล้อมด้วยไอน้ำ/ก้อนหิน) เป็นเวลาสั้นๆ ประมาณ 3-5 วินาที และปิด gap ข้อ 3 เดิม (animation อาบน้ำที่ยังไม่มีตัวอย่าง) ออกจากรายการช่องว่างที่พบ เพราะไม่ใช่ gap อีกต่อไป พร้อมปรับเลขลำดับ gap ข้อถัดไป (เดิมข้อ 4 เรื่องปลายทางรางวัลท้ายเทอม) เป็นข้อ 3 — ไม่แตะ Acceptance Criteria, หัวข้อ ก./ข./ค./ง., scope, บริบท หรือ backlog.md (สถานะยังคง Draft) ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] (mode: update, กลไกใหม่มาแทนที่หลอดสถานะ 3 ตัวเดิมทั้งหมด) — user ตัดสินใจให้กลไกง่ายลงกว่าฉบับก่อนหน้ามาก: (1) ตัดหลอด "ความสกปรก" (อาบน้ำ) และหลอด "ความเบื่อ/ความสนุก" (เล่น/ออกกำลังกาย) ออกทั้งหมด เหลือแค่ "ความหิว" เป็นค่าซ่อนภายในเพียงตัวเดียวที่ผูกกับ action "ให้อาหาร" โดยเฉพาะ (ไม่แสดงหลอด/เกจให้เห็น) ส่วน "อาบน้ำ", "ลูบหัว", "เล่น/ออกกำลังกาย" ไม่มีค่าสถานะ/หลอดซ่อนใดๆ อีกต่อไป กดได้ไม่จำกัดจำนวนครั้งอย่างแท้จริง (2) เมื่อกด "ให้อาหาร" ตอนอิ่มแล้ว ต้องมี feedback message ชัดเจนว่า "ตัวละครอิ่มแล้ว" (3) ล็อกค่าความผูกพัน: 1 หน่วยต่อการกดสำเร็จ 1 ครั้ง เท่ากันทุก action ไม่มีน้ำหนักต่างกัน สะสมได้สูงสุด 10 หน่วย/วันรวมทุก action (เพดานเดิมไม่เปลี่ยน) (4) เมื่อถึงเพดานรวม 10/วัน ต้องมี feedback message เชิงบวก/ไม่กดดันบอกผู้เล่น — เขียนใหม่หัวข้อ ข. (เปลี่ยนชื่อเป็น "ความหิว (ค่าซ่อนเฉพาะ Action ให้อาหาร)"), แก้หัวข้อ ค. (ล็อกตัวเลข 1 หน่วย/ครั้ง + อธิบาย feedback message ทั้ง 2 กรณี), ปรับ Acceptance Criteria ทั้งชุดให้ตรงกลไกใหม่ และปิด gap เดิม 2 ข้อ (ลูบหัวมีหลอดไหม, แต้มต่อ action เท่าไหร่) เหลือ gap เดียวคือปลายทางรางวัลท้ายเทอม — ไม่แตะหัวข้อ ก., ง., จ., scope, บริบท/ที่มา, wikilink อ้างอิงเดิม ไม่แตะ backlog.md (สถานะยังคง Draft) ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] (mode: update, จุดเดียวในหัวข้อ Scope + ปรับคำ 1 บรรทัดใน AC) — user ตรวจทานพบว่าบรรทัด "อยู่ในขอบเขต" ยังเขียนตกค้างจากกลไกเดิม ("หลอดสถานะ 3 ตัว: ความหิว, ความสกปรก, ความเบื่อ/ความสนุก และตรรกะ 'เต็มแล้วกดซ้ำไม่มีผล'") ซึ่งขัดแย้งกับหัวข้อ ข./ค. และ AC ที่แก้เป็นกลไกใหม่ไปแล้ว (เหลือ "ความหิว" ค่าซ่อนเดียวผูกกับ "ให้อาหาร" เท่านั้น ส่วนอาบน้ำ/ลูบหัว/เล่น-ออกกำลังกายไม่มีหลอด กดได้ไม่จำกัด) — แก้บรรทัดนั้นให้ตรงกับกลไกล่าสุด และเติมวลี "(ปุ่มยังกดได้ตามปกติ ไม่ถูกปิดใช้งาน/ไม่หายไป)" ต่อท้าย AC ข้อเพดานรวม 10/วัน เพื่อยืนยันชัดว่าเป็นแค่การไม่สะสมแต้มเพิ่ม ไม่ใช่การ disable ปุ่ม — ไม่แตะส่วนอื่นของไฟล์ ไม่แตะ backlog.md ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] (mode: update, 2 เรื่อง) — (1) ล็อกตัวเลขที่ค้างไว้ในหัวข้อ ข.: กด "ให้อาหาร" สำเร็จได้สูงสุด **3 ครั้ง/วัน** (ครั้งที่ 1-3 ได้ผลปกติ +1 หน่วยผูกพัน/ครั้ง รวม 3 หน่วยจากการให้อาหารอย่างเดียว, ครั้งที่ 4 เป็นต้นไปถือว่าอิ่มแล้ว ไม่มีผลใดๆ พร้อม feedback message เดิม) แก้ถ้อยคำในหัวข้อ ค. ที่อ้างถึงเพดานของ "ให้อาหาร" ให้ระบุ 3 ครั้ง/วันด้วย (ไม่เปลี่ยนเพดานรวม 10 หน่วย/วัน) และแก้ Acceptance Criteria เดิม 2 ข้อเรื่องให้อาหารเป็น "ครั้งที่ 1-3" กับ "ครั้งที่ 4 เป็นต้นไป" ให้สะท้อนตัวเลขนี้ (2) แก้คำตกค้างพหูพจน์ในหัวข้อ Scope > อยู่ในขอบเขต: เปลี่ยน "การรีเซ็ตหลอดสถานะทุกตัวและตัวนับค่าความผูกพันของวันทุกเที่ยงคืน" เป็น "การรีเซ็ตความหิวและค่าความผูกพันของวันทุกเที่ยงคืน (รีเซ็ตทั้งหมดพร้อมกันตามวันปฏิทิน ไม่ใช่นับถอยหลังเป็นชั่วโมง)" เพราะเหลือค่าซ่อนแค่ "ความหิว" ตัวเดียวแล้ว ไม่ใช่หลอดหลายตัว — ไม่แตะหัวข้อ ก./ง./จ., บริบท, gap, wikilink, backlog.md ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] (mode: update, แก้ความเข้าใจผิดเรื่องการรีเซ็ตค่าความผูกพัน) — เอกสารเดิมเขียนผิดว่า "ค่าความผูกพันของวันนั้น รีเซ็ตนับใหม่ทุกวันพร้อมกับความหิว (เที่ยงคืน)" ซึ่งไม่ตรงกับที่ user ต้องการ แก้เป็นแยกชัดเจน 2 ค่าคนละตัว: **"โควตาหาแต้มต่อวัน" (daily earn quota)** — ตัวนับเทียบเพดาน 10 หน่วย/วัน (เพดานเดิมไม่เปลี่ยน) ที่**รีเซ็ตกลับเป็น 0 ทุกเที่ยงคืน** พร้อมความหิว เพื่อเปิดโควตาใหม่ให้หาแต้มได้อีก vs **"ยอดสะสมรวมของค่าความผูกพัน" (total accumulated bonding score)** — ค่าที่เก็บระยะยาว **ไม่รีเซ็ตทุกเที่ยงคืน** สะสมต่อเนื่องทุกวันที่ทำ action สำเร็จ (เช่น วันนี้ 8 พรุ่งนี้ 5 รวมเป็น 13 ไม่ใช่รีเซ็ตกลับ 0) รีเซ็ตกลับ 0 เฉพาะตอนเปลี่ยนสายพันธุ์/รีเซ็ตตัวละคร (FE-26) ตามที่ระบุไว้แล้วในข้อ ง. (ไม่แตะข้อ ง.) — เหตุผล: ยอดสะสมนี้มีไว้ให้ครูเรียกดูได้ในอนาคตเพื่อใช้ตัดสินรางวัลท้ายเทอม (อ้างอิงหัวข้อ "ไม่อยู่ในขอบเขต" เดิม ไม่ได้ย้ายออกจาก out-of-scope เพราะยังไม่มีกลไก/หน้าจอรางวัลจริง) ถ้ารีเซ็ตทุกวันยอดจะไม่มีความหมายให้เปรียบเทียบ — ระบบยังไม่มีผลอัตโนมัติอื่นใดจากค่ายอดสะสมนี้ ("ยังไม่มีผลเพิ่มเติม") แก้ 3 จุด: หัวข้อ ค. (แยกเป็น 2 ค่าพร้อมเหตุผล), หัวข้อ Scope > อยู่ในขอบเขต (ระบุเฉพาะความหิว+โควตาที่รีเซ็ต ไม่ใช่ยอดสะสมรวม), Acceptance Criteria ข้อเที่ยงคืน (ระบุชัดว่ายอดสะสมรวมไม่ถูกรีเซ็ต) — ไม่แตะหัวข้อ ก./ข./ง./จ., บริบท, gap, wikilink, AC ข้ออื่น ไม่แตะ backlog.md ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- จัดระเบียบเอกสาร [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] (mode: update, ไม่เปลี่ยนเนื้อหาเชิงธุรกิจ/ตัวเลข) — รวม blockquote ประวัติการแก้ไขที่เคยกระจายอยู่ในหัวข้อ ข., ค. และ Acceptance Criteria (รวมทั้งใน Gap) มารวมไว้เป็นบรรทัด "อัปเดตล่าสุด" พร้อมรายการ bullet ประวัติเรียงตามเวลาที่ header ด้านบนสุดของไฟล์แทน แล้วลบ blockquote เดิมทั้งหมดออกจากเนื้อหาแต่ละหัวข้อ ให้ตรงกับ convention ของ `20260821-02-pet-points-and-weekly-arena.md` พร้อมแก้คำตกค้าง 2 จุดที่ยังอ้างถึง "หลอดสถานะ" (หัวข้อ Scope บรรทัดเพดานความผูกพัน และหัวข้อ "ไม่อยู่ในขอบเขต" บรรทัดท่าทางแยกตามอารมณ์) เป็น "ความหิว" ให้ตรงกับกลไกปัจจุบันที่เหลือค่าซ่อนเดียว — ตรวจทานทั้งไฟล์แล้วว่าตัวเลข (3 ครั้ง/วันของให้อาหาร, เพดานรวม 10/วัน, ยอดสะสมไม่รีเซ็ต) ยังตรงกันทุกหัวข้อ ไม่แตะ backlog.md ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- อัปเดต `docs/01-requirements/feature-list.md` (mode: update, ต่อท้ายกลุ่ม E เท่านั้น, คงรหัส FE-01 ถึง FE-81 เดิมไว้ทั้งหมด) — เพิ่ม `FE-82` ถึง `FE-84` เข้ากลุ่ม E ระดับ **Could** ทั้งหมด แตกจาก [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]]: `FE-82` action ดูแล 4 แบบพร้อมความหิว (เพดาน 3 ครั้ง/วันเฉพาะให้อาหาร), `FE-83` ค่าความผูกพันสะสม (+1/action, โควตา 10 หน่วย/วันที่รีเซ็ตเที่ยงคืน แยกจากยอดสะสมรวมที่ไม่รีเซ็ต ผูกกับ FE-26), `FE-84` animation overlay ของแต่ละ action — อัปเดตตารางสรุป MoSCoW ท้ายไฟล์ (Could 7→10, รวม 81→84, ยังใช้งานอยู่ 83 รายการ + FE-25 เลิกใช้ 1) และอัปเดต "อัปเดตล่าสุด" ที่ header เป็น 2026-08-25 — ไม่แตะกลุ่ม A-D, F-J และไม่แตะ Gap section
- อัปเดต `docs/01-requirements/user-journey.md` (mode: update, เฉพาะ UJ-01, คงรหัส UJ-01 และ node เดิมทั้งหมดไว้ ไม่แตะ UJ-02/UJ-03/UJ-04) — เพิ่ม node ใหม่ `PC1`/`PC2` เป็น loop ขนานที่แตกจาก node เดิม `L3` (จุดที่นักเรียนมีไข่/สัตว์เลี้ยงอยู่แล้ว) สะท้อนว่านักเรียนดูแลสัตว์เลี้ยงประจำวันได้ (ให้อาหาร/อาบน้ำ/ลูบหัว/เล่น-ออกกำลังกาย — FE-82) แล้วได้ค่าความผูกพันสะสม (FE-83) โดยไม่ต้องรอแต้มหรือกระทบวงจรแรงจูงใจหลัก เพิ่มแถว mapping ใหม่ 2 แถวใต้ diagram และอัปเดต "อัปเดตล่าสุด" ที่ header เป็น 2026-08-25 พร้อมบันทึกหมายเหตุการอัปเดตในหัวเอกสาร — ตรวจ syntax ของ mermaid block ด้วยการเทียบรูปแบบ quote/bracket/arrow กับ node เดิมในไฟล์เดียวกันที่ยืนยันแล้วว่าถูกต้อง (agent นี้ไม่มีเครื่องมือรันโค้ด/execute `mermaid.parse()` จริงในสภาพแวดล้อมนี้ — ควรให้ audit/skill ที่มีเครื่องมือรันจริงตรวจซ้ำก่อน commit)
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ `docs/02-design/01-prototypes/v3-20260823-student-egg-economy/index.html` (mode: update, แก้เฉพาะหน้า `screen-pet` ที่มีอยู่แล้ว ไม่แตะหน้าจออื่นทั้ง 8 หน้าที่เหลือ ไม่เพิ่มหน้าจอใหม่ ยังคง 9 หน้าจอเท่าเดิม) — เพิ่มโซน "ดูแลสัตว์เลี้ยงประจำวัน" ตาม FE-82/FE-83/FE-84 ([[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|20260825-07-pet-care-and-bonding]], node PC1/PC2 ใน user-journey.md UJ-01):
  - เพิ่มปุ่ม action ดูแล 4 ปุ่ม (ให้อาหาร/อาบน้ำ/ลูบหัว/เล่น-ออกกำลังกาย) กดแล้วมีผลทันทีไม่มี confirm modal
  - เพิ่มค่าซ่อน "ความหิว" (`state.care.feedCountToday`) ไม่แสดงบนจอ ผูกกับ "ให้อาหาร" เท่านั้น เพดาน 3 ครั้ง/วัน ครั้งที่ 4 ขึ้นไปแสดง toast "ตัวละครอิ่มแล้ว" ไม่มีผลต่อความหิว/ค่าความผูกพัน — อีก 3 action ไม่มีเพดานของตัวเอง กดได้ไม่จำกัด
  - เพิ่มค่าความผูกพัน 2 ระดับแยกกันชัดเจนในโซนนี้: "โควตาวันนี้" (`state.care.dailyQuota`, X/10, +1 ทุก action สำเร็จเท่ากันหมด ถึง 10 แล้วแสดง toast เชิงบวก "วันนี้ดูแลเต็มที่แล้ว พรุ่งนี้ค่อยมาใหม่นะ" ปุ่มไม่ disable) กับ "ยอดสะสมรวม" (`state.care.totalBonding`, ไม่รีเซ็ตรายวัน สะสมต่อเนื่องในเซสชัน)
  - ผูกยอดสะสมรวมเข้ากับปุ่ม "รีเซ็ต / เปลี่ยนสายพันธุ์" (FE-26) เดิม — กดยืนยันรีเซ็ตแล้ว `state.care.feedCountToday`/`dailyQuota`/`totalBonding` กลับเป็น 0 ทั้งหมด (ทดสอบ state จริงได้เหมือน pattern คืนแต้มเต็มตอนรีเซ็ตที่ v3 มีอยู่แล้ว)
  - เพิ่มปุ่ม **"จำลองวันใหม่ (รีเซ็ตความหิว+โควตาวันนี้)"** ใน "เครื่องมือสำหรับทดสอบ prototype" ที่หัวไฟล์เดิม (ไม่ใช่ส่วนของ UI จริง เช่นเดียวกับปุ่มตั้งยอดแต้มที่มีอยู่แล้ว) รีเซ็ตเฉพาะความหิว+โควตาวันนี้ ไม่แตะยอดสะสมรวม — ใช้ทดสอบ AC ข้ามเที่ยงคืนโดยไม่ต้องรอเวลาจริงเพราะ prototype ไม่มี backend
  - เพิ่ม animation overlay ด้วย CSS keyframes ล้วน (ไม่มี library ภายนอก) ซ้อนบนท่ายืนปกติของสัตว์เลี้ยง: ให้อาหาร = ไอคอนอาหาร (inline SVG) เล็กลง+จางหาย, ลูบหัว = สัตว์เลี้ยงเด้ง (scale bounce) + หัวใจ (inline SVG) ลอยขึ้นจาง, เล่น/ออกกำลังกาย = สัตว์เลี้ยงเลื่อน (translateX) ออกนอกกรอบแล้วกลับมาใน ~2.5 วิ, อาบน้ำ = พื้นหลังวงกลมสีฟ้าอ่อนโปร่งแสงล้อมตัวสัตว์เลี้ยง 4 วิ — สีทั้งหมดอ้างจาก token เดิมใน `:root` (`--bright-yellow`, `--deep-green`, `--bright-pink`, `--bright-teal`) ไม่มี hex ใหม่
  - เพิ่มค่า mock ใหม่ 3 ตัวใน object `MOCK` เดิม (`careFeedCapPerDay: 3`, `careDailyQuotaCap: 10`, `careBondingPerAction: 1`) กำกับคอมเมนต์ "ล็อกแล้วจาก spec" ตาม DESIGN.md ข้อ 4.7/4.8 — ไม่แก้ตัวเลขที่ล็อกแล้วของฟีเจอร์อื่น (ตารางต้นทุนไข่/ขั้น, ราคาไอเทม, HP) ไม่ผูก action ดูแลกับแต้มที่ใช้ได้ (FE-23) หรือค่าสเตตัสต่อสู้ (FE-77) เลย ไม่เพิ่ม action เกิน 4 แบบ ไม่มีท่าทาง/หน้าตาแยกตามอารมณ์ (นอกขอบเขตตาม spec)
- อัปเดต `docs/02-design/01-prototypes/v3-20260823-student-egg-economy/README.md` — เพิ่ม FE-82/83/84 เข้า "Feature ที่ครอบ" และหัวข้อ "ครอบคลุม Journey / Feature" (อ้าง node PC1/PC2), เพิ่มแถว mapping `screen-pet` ให้รวม FE-82/83/84 และ requirement ต้นทางใหม่, เพิ่มตัวเลขล็อกแล้ว 4 แถวในตาราง `MOCK`, เพิ่มหมายเหตุปุ่ม "จำลองวันใหม่" ในย่อหน้าเครื่องมือทดสอบและหัวข้อสมมติฐานเพิ่มเติม, เพิ่มขั้นตอนกดทดลองใหม่ (ให้อาหารจนอิ่ม, กดจนโควตาเต็ม 10, จำลองวันใหม่, กดรีเซ็ตแล้วดูยอดสะสมรวมกลับ 0), อัปเดตตาราง "สิ่งที่ยังไม่ได้ทำ" และ "ข้อจำกัดของ prototype (สรุป)" ให้ครอบคลุมของใหม่
- อัปเดต `docs/02-design/01-prototypes/index.md` — แก้แถว v3 ให้รวม FE-82/83/84, node PC1/PC2 และหมายเหตุการอัปเดต 2026-08-25 (ยังคง 9 หน้าจอเท่าเดิม)
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **test-writer (`sync-test-plan`, mode: update สำหรับ acceptance-criteria.md/test-plan.md, mode: new สำหรับ test-cases ใหม่)** — เพิ่ม AC/test case สำหรับ FE-82/83/84 (ระบบดูแลสัตว์เลี้ยง Pet Care & Bonding, [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|20260825-07-pet-care-and-bonding]]) ครอบ node PC1/PC2 ของ UJ-01:
  - แก้ `docs/03-testing/01-test-plan/acceptance-criteria.md` (ต่อท้ายหมวด E เท่านั้น ไม่แตะ/ไม่เปลี่ยนรหัส AC เดิมข้อใด) — เพิ่ม **11 AC ใหม่**: `AC-FE-82-1~5` (ให้อาหารครั้งที่ 1-3 ปกติ, ครั้งที่ 4 อิ่มแล้วไม่มีผล+ข้อความ "ตัวละครอิ่มแล้ว", อาบน้ำ/ลูบหัว/เล่นไม่มีเพดานตัวเอง, ไม่กระทบแต้มที่ใช้ได้ FE-23, ไม่กระทบค่าสเตตัสต่อสู้ FE-77), `AC-FE-83-1~5` (+1 หน่วย/action เท่ากันทุกแบบ, ขอบเขตแตะเพดานรวม =10 พอดียังได้แต้ม, เกินเพดาน =11 ไม่ได้แต้มเพิ่มแต่ปุ่มไม่ disable, ข้ามเที่ยงคืนความหิว+โควตาวันนี้เป็น 0 แต่ยอดสะสมรวมไม่ลด, รีเซ็ตตัวละครแล้วยอดสะสมรวมกลับเป็น 0), `AC-FE-84-1` (animation เป็น overlay ซ้อนท่ายืนปกติ ไม่ใช่ท่าทางแยกใหม่ตามอารมณ์) — เพิ่ม **Coverage Gap G-12** บันทึกว่า FE-82/83/84 เป็นระดับ Could ตาม feature-list.md แต่เขียน AC เต็มรูปแบบเป็นข้อยกเว้นเฉพาะกรณีนี้ (ไม่ใช่บรรทัดฐานสำหรับ FE ระดับ Could ตัวอื่น) — อัปเดต Traceability Matrix ครบทั้ง 3 ตาราง (ตาราง 1 เพิ่ม 3 แถว FE-82/83/84 → รวม 71 แถว, ตาราง 2 เพิ่ม FE-82/83/84 เข้าแถว UJ-01 และปิด node PC1/PC2 ที่เคยขาด AC, ตาราง 3 เพิ่มแถว spec 07 นับ AC checklist 8 ข้อของ spec มีตัวรับตรงครบทุกข้อ) ยืนยันด้วยการนับจริง `grep "^#### AC-"` ได้ **178 รายการ** (167 เดิม + 11 ใหม่) ตรงกับตัวเลขที่บันทึกไว้
  - สร้างไฟล์ใหม่ `docs/03-testing/01-test-plan/test-cases/pet-care-and-bonding.md` — **9 test case** (`TC-FE-82-001~004`, `TC-FE-83-001~004`, `TC-FE-84-001`) ครอบ AC ทั้ง 11 ข้อครบ ใช้ชื่อปุ่ม/ข้อความจริงจาก prototype `v3-20260823-student-egg-economy/index.html` หน้า `screen-pet` (ปุ่ม "ให้อาหาร"/"อาบน้ำ"/"ลูบหัว"/"เล่น/ออกกำลังกาย", ข้อความ "ตัวละครอิ่มแล้ว", "วันนี้ดูแลเต็มที่แล้ว พรุ่งนี้ค่อยมาใหม่นะ", "โควตาวันนี้: X / 10 วันนี้", "ยอดสะสมรวม: X หน่วย", ปุ่ม QA "จำลองวันใหม่ (รีเซ็ตความหิว+โควตาวันนี้)", ปุ่ม "รีเซ็ต / เปลี่ยนสายพันธุ์ (เริ่มใหม่ที่ขั้นไข่)") ครอบ boundary สำคัญครบ: ให้อาหารครั้งที่ 3 vs 4, แตะโควตา 9→10→11, ข้ามเที่ยงคืนด้วยปุ่ม QA แล้วยืนยันความหิว/โควตา=0 แต่ยอดสะสมรวมไม่เปลี่ยน, กดรีเซ็ต/เปลี่ยนสายพันธุ์แล้วยอดสะสมรวมกลับเป็น 0, ทำครบ 4 action แล้วยืนยันแต้มที่ใช้ได้/สะสมรวม (FE-22/23) และค่าสเตตัสต่อสู้ (FE-77) ไม่เปลี่ยนแปลง — ทุกเคส **รันได้ 100% บน prototype v3 วันนี้** (สถานะ prototype ดีกว่าไฟล์ test-case อื่นในโฟลเดอร์เดียวกันที่ส่วนใหญ่ยังรันไม่ได้เพราะ prototype v1 เป็นฐานเก่า) — แยกตามประเภท: Happy path (ล้วน) 4, Boundary/Negative 2, Negative (ล้วน) 1, Edge case (ล้วน) 1, Happy/Edge 1, ไม่มี Permission — Actual Result เว้นว่างทุกเคสพร้อมหมายเหตุให้บันทึกที่ `docs/03-testing/02-test-result/`
  - แก้ `docs/03-testing/01-test-plan/test-plan.md` (เพิ่มการอ้างอิงเท่านั้น ไม่แตะ Test Strategy ระดับโปรเจกต์) — เพิ่ม FE-82/83/84 เข้าหัวข้อ 1.1 (ส่วนต่อขยายหมวด E), เพิ่มแถวไฟล์ `test-cases/pet-care-and-bonding` เข้าหัวข้อ 3.5 (จาก 4 ไฟล์เป็น 5 ไฟล์), เพิ่มลิงก์ spec 07 และ prototype v3 README เข้าหัวข้อที่มา/ส่งต่อไป, อัปเดตตัวเลขสรุป AC จาก 167 เป็น 178 รหัส และ Coverage Gap จาก 11 เป็น 12 ข้อในบล็อกอัปเดตใหม่ (ไม่แก้ตัวเลขเดิมของรอบก่อน เพิ่มเป็นบล็อกต่อท้าย)
  - แก้ `docs/03-testing/01-test-plan/index.md` — เปลี่ยนหัวข้อ "เอกสารในโฟลเดอร์นี้ (ยังไม่ถูกสร้าง)" เป็นลิงก์จริงไปยัง `acceptance-criteria.md`/`test-plan.md` และเพิ่มรายการไฟล์ `test-cases/` ที่มีอยู่จริงทั้ง 5 ไฟล์ (รวม `pet-care-and-bonding` ใหม่)
  - **Coverage Gap ที่พบเพิ่มเติม:** ไม่มี Coverage Gap ใหม่นอกเหนือจาก G-12 ที่บันทึกไว้แล้ว — FE-82/83/84 ทุกข้อมี AC ครบ, ทุก AC มี test case รองรับครบ, ไม่มี requirement ของ spec 07 ที่ขาดตัวรับ
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **architecture-writer (`sync-architecture`, mode: update)** — เพิ่ม `CMP-21 — Pet Care and Bonding Module` เข้า `docs/02-design/02-technical/architecture.md` สำหรับ FE-82/FE-83/FE-84 (ระบบดูแลสัตว์เลี้ยง Pet Care & Bonding, [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|20260825-07-pet-care-and-bonding]], node PC1/PC2 ของ UJ-01) ตามที่ user ยืนยันแล้วว่าให้ **สร้าง component แยกใหม่ ไม่ขยาย CMP-08** เพราะ spec ระบุเองว่าระบบนี้ "แยกขาดโดยสิ้นเชิงจากวงจรแต้ม-เติบโต-ต่อสู้เดิม":
  - ไม่แตะเนื้อหา/รหัส CMP-01 ถึง CMP-20 เดิม, ไม่แตะ DF-02/DF-03/DF-04, คงระดับความละเอียด Conceptual ทั้งไฟล์ (ไม่ใส่ชื่อเทคโนโลยี/framework ใหม่)
  - หัวข้อ 2 (Mermaid flowchart): เพิ่ม node `M11["Pet Care and Bonding Module (CMP-21)"]` ในกลุ่ม subgraph "ชั้น Logic", เพิ่มเส้น `C1 --> M11` (Student Web Client เรียก) และ `M11 --> D1` (เขียน Relational Database CMP-16) — ตรวจ syntax ด้วยตาเทียบ node/edge เดิมที่ผ่านแล้วในไฟล์เดียวกัน (ทุก node ครอบ double quote มีรหัส CMP-21 กำกับ ไม่มี `;`) — **หมายเหตุ: ไม่มีเครื่องมือรัน `mermaid.parse()` จริงในสภาพแวดล้อมนี้ ควรให้ audit ที่มีเครื่องมือรันจริงตรวจซ้ำก่อน commit**
  - หัวข้อ 3 (ตาราง Component): เพิ่มแถว `CMP-21` ต่อท้าย `CMP-20` ระบุหน้าที่ (action ดูแล 4 แบบ, ควบคุมความหิวเพดาน 3 ครั้ง/วัน, ค่าความผูกพัน +1/action เพดานรวม 10/วัน + ยอดสะสมรวมไม่รีเซ็ตรายวัน, lazy reset เทียบวันที่แทน background job) และหมายเหตุย้ำว่าแยกจาก CMP-08 ตามการตัดสินใจของ user, ไม่มีเส้นเชื่อมข้อมูลไปยัง CMP-07/CMP-10 เรื่องแต้ม/ค่าต่อสู้เลย, รับรู้ (ไม่ตัดสินใจ) event รีเซ็ตยอดสะสมรวมจาก CMP-08 เมื่อเปลี่ยนสายพันธุ์/รีเซ็ตตัวละคร (FE-26 — CMP-08 เป็นเจ้าของ event), state เก็บฝั่ง server ผ่าน CMP-16
  - หัวข้อ 4 (Actor): เพิ่ม `CMP-21 (จัดการสัตว์เลี้ยงของตนเอง)` เข้าสิทธิ์ของ "นักเรียน" (แถวเดิม), เพิ่ม "CMP-21 เข้าถึงไม่ได้เลย" เข้าคอลัมน์ของ "ครู" (แถวเดิม) พร้อมเหตุผลว่ายังไม่มีกลไก/หน้าจอให้ครูดูยอดสะสมรวมในรอบนี้
  - หัวข้อ 6: เพิ่มรายละเอียดเข้า **DF-01 เดิม** (ไม่สร้าง DF ใหม่) — เพิ่ม participant `CARE as "Pet Care and Bonding Module (CMP-21)"` และขั้นตอนย่อยหลังจบ flow ซื้อไข่/ปลดล็อกขั้นเดิม: นักเรียนกด action ดูแล → C เรียก CARE → CARE เช็ค lazy reset (เทียบวันที่บันทึกล่าสุดกับวันนี้) → เช็คเพดาน (ความหิว 3/วัน หรือโควตารวมค่าความผูกพัน 10/วัน) → อัปเดตค่าใน DB (CMP-16) → ตอบกลับ C พร้อม feedback message — เพิ่มแถวในตารางอธิบายขั้นตอนย้ำว่า **ไม่ผ่าน CMP-07/CMP-08 เรื่องแต้ม/ค่าสเตตัสเลย** เป็นเส้นทางข้อมูลที่แยกขาดสมบูรณ์
  - หัวข้อ 8 (Open Decision): เพิ่มประเด็น "ช่องทางให้ครูดูยอดสะสมรวมของค่าความผูกพัน (CMP-21)" — ยังไม่มีคำตอบว่าจะเปิดผ่านหน้าจอไหน (ขยาย CMP-11 หรือเปิดโมดูล/หน้าจอใหม่) รอ user เปิด requirement ใหม่ก่อนตัดสินใจ ไม่เดาแทน
  - อัปเดต `docs/02-design/02-technical/index.md` แก้คำอธิบาย architecture.md จาก "CMP-01 ถึง CMP-20" เป็น "CMP-01 ถึง CMP-21"
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ `docs/02-design/02-technical/architecture.md` (mode: update, จุดเดียว) — user ตรวจทานพบรหัส FE ผิดในบล็อก sequence diagram ของ **DF-01** บรรทัด `CARE->>DB: "อัปเดตความหิว/โควตารายวัน/ยอดสะสมรวมของค่าความผูกพัน (FE-84)"` — `FE-84` คือ "Animation แบบ overlay ของแต่ละ action" ไม่เกี่ยวกับการอัปเดตข้อมูลความหิว/โควตา/ยอดสะสม ที่ถูกต้องคือ `FE-82` (ความหิว) และ `FE-83` (ค่าความผูกพัน/โควตา/ยอดสะสม) ตามที่ใช้ถูกต้องอยู่แล้วในบรรทัดก่อนหน้า — แก้เฉพาะคำในวงเล็บจาก `(FE-84)` เป็น `(FE-82/FE-83)` บรรทัดเดียว ไม่แตะเนื้อหาอื่นในไฟล์
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **api-db-writer (`sync-api-db`, mode: update ทั้งคู่)** — เพิ่ม `ENT-27`/`API-62`/`API-63` สำหรับ FE-82/FE-83 (CMP-21 Pet Care and Bonding Module, [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|20260825-07-pet-care-and-bonding]]) ไม่แตะ ENT-01~26/API-01~61 เดิมทั้งเนื้อหาและรหัส:
  - แก้ `docs/02-design/02-technical/database-schema.md` — เพิ่ม `ENT-27 — PET_CARE_STATE` ต่อท้าย ENT-26 (หัวข้อ 2 และ 3) ความสัมพันธ์ 1:1 กับ `PET` (ENT-15) ผ่าน `pet_id` (unique) มี field `hunger_today` (0-3, CHECK), `daily_bonding_quota_used` (0-10, CHECK), `total_bonding_score` (ไม่มีเพดานบน ไม่รีเซ็ตรายวัน), `last_recorded_date` (วันที่, ใช้ lazy reset เทียบวันปัจจุบัน ไม่มี background job/cron) — บันทึกชัดว่าเมื่อรีเซ็ตตัวละคร (FE-26/API-38) ไม่ต้องมี event ข้ามโมดูลระดับ DB เพราะแถว `PET_CARE_STATE` ใหม่ผูกกับ `pet_id` ใหม่ที่ insert คู่กับ `PET` ใหม่ (ผ่าน API-36) เริ่มที่ 0 โดยอัตโนมัติจาก default อยู่แล้ว, ระบุชัดว่า**ไม่มี FK/trigger เชื่อมไปยัง ENT-10/ENT-11/ENT-16**, ระบุเป็นข้อยกเว้นจาก pattern soft-delete มาตรฐานของเอกสาร (ไม่มี `is_active`/`disabled_at` เพราะสถานะของ `PET` ที่อ้างอิงทำหน้าที่แทนได้ครบแล้ว) — เพิ่มเส้น ER diagram `PET ||--|| PET_CARE_STATE : "มีสถานะการดูแลประจำวัน"` (ยังไม่ได้รัน mermaid parser จริง ระบุไว้ตรงๆ) — อัปเดต "อัปเดตล่าสุด" เป็น 2026-08-25
  - แก้ `docs/02-design/02-technical/api-spec.md` — เพิ่ม `API-62 GET /student/me/pet/care-status` (คืน `dailyQuotaUsed`/`dailyQuotaCap`/`totalBonding` เท่านั้น ห้ามคืนค่า `hunger_today` ดิบ, รัน lazy reset ก่อนอ่านค่าเสมอ) และ `API-63 POST /student/me/pet/care-actions` (รับ `actionType` enum `feed`/`bathe`/`pat`/`play`, pet_id derive จาก session ตาม pattern เดียวกับ API-34/37/38) พร้อม 3 response variant ของ API-63: `resultStatus="counted"` (สำเร็จปกติ), `"already_full"` (feed ครั้งที่ 4+ ของวัน — 200 ไม่ error พร้อมข้อความ "ตัวละครอิ่มแล้ว"), `"daily_cap_reached"` (ถึงเพดานรวม 10/วัน — 200 ไม่ error พร้อมข้อความ "วันนี้ดูแลเต็มที่แล้ว พรุ่งนี้ค่อยมาใหม่นะ") และ error case จริง 404 `NO_ACTIVE_PET` + 400 `VALIDATION_ERROR` — ตัวเลข 3 ครั้ง/วัน (ความหิว) และ 10 หน่วย/วัน (เพดานรวม) ตรงกับ AC-FE-82-1/2 และ AC-FE-83-2/3 ทุกจุด ไม่ได้ตั้งเลขใหม่ — เพิ่มตารางสรุปหัวข้อ 2 และปรับสรุปจำนวน method (GET 22→23, POST 32→33, รวม 61→63 endpoint) — เพิ่ม **G-API-12** บันทึกว่าลำดับการตรวจ "ความหิวเต็ม" กับ "เพดานรวม 10/วัน" เมื่อเกิดพร้อมกันตอนกด `feed` ยังไม่มี AC ทดสอบตรงจุดนี้ เอกสารเลือกตรวจความหิวก่อนเป็นการตีความที่สอดคล้องกับสเปกที่มีอยู่ (ไม่ใช่ตัวเลขใหม่) แต่ยังไม่ผ่านการยืนยันจาก requirement โดยตรง — อัปเดต "อัปเดตล่าสุด" เป็น 2026-08-25
  - อัปเดต `docs/02-design/02-technical/index.md` — แก้คำอธิบาย database-schema.md เป็น "ENT-01 ถึง ENT-27" และ api-spec.md เป็น "API-01 ถึง API-63"
  - **Self-check ที่ทำจริง:** นับ endpoint ใหม่จากไฟล์ = 2 ตรงกับตารางสรุป, ทุก endpoint ใหม่มี error case พร้อม status code ตัวเลขจริง (400/401/404), ตัวเลข 3/10 เทียบกับ acceptance-criteria.md แล้วตรงกันทุกจุด — **ยังไม่ได้รัน `mermaid.parse()` จริงกับ ER diagram ที่แก้ (ไม่มีเครื่องมือรันโค้ดในสภาพแวดล้อมนี้) ควรให้ audit ที่มีเครื่องมือรันจริงตรวจซ้ำก่อน commit**
  - ไม่แตะ ENT-01~26, API-01~61 เนื้อหาเดิม, ไม่แตะความสัมพันธ์ ENT-15/ENT-16/ENT-10/ENT-11 เดิม
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- แก้ `docs/02-design/02-technical/database-schema.md` (mode: update, จุดเดียว) — user รัน `mermaid.parse()` จริงในเบราว์เซอร์ยืนยันแล้วว่า ER diagram ทั้งไฟล์ (รวมเส้น `PET ||--|| PET_CARE_STATE` ที่เพิ่งเพิ่มใน ENT-27) parse ผ่านสำเร็จ (ผล PARSE_OK) — ปิดหมายเหตุเดิมที่ระบุว่า "ยังไม่ได้รันผ่าน mermaid parser จริง" เปลี่ยนเป็นยืนยันด้วยการรันจริงแทน ไม่แตะเนื้อหาอื่นในไฟล์
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **detailed-design-writer (`sync-detailed-design`, mode: new สำหรับ DD-09, mode: update สำหรับ hub และจุดเดียวใน DD-01)** — เพิ่ม `DD-09` สำหรับ FE-82/FE-83 (ดูแลสัตว์เลี้ยงประจำวันและค่าความผูกพัน, API-63 ผูก API-62 ประกอบ, CMP-21) และแก้ DD-01 เพิ่มขั้นตอน insert `PET_CARE_STATE`:
  - สร้างไฟล์ใหม่ `docs/02-design/02-technical/detailed-design/care-for-pet.md` (DD-09) ครบ 6 หัวข้อตาม convention เดิม — Sequence diagram (participant S/C/CARE/D), Precondition, Pseudo code (validate actionType → หา PET active → transaction เดียว: lock `PET_CARE_STATE` → lazy reset → เช็คความหิว `hunger_today < 3` เฉพาะ feed → เช็คเพดานรวม `daily_bonding_quota_used < 10` → commit), ตาราง BR/ERR, stateDiagram-v2 (3 state: ปกติ/อิ่มแล้ว/ถึงเพดานรวมวันนี้ ใช้ `state "..." as shortId` ก่อนเสมอ ไม่มี quote ตรงในเส้น transition) — ออกรหัสใหม่ **BR-20 ถึง BR-24** (เพดานความหิว `<3`, เพดานรวม `<10`, +1 หน่วยเท่ากันทุก action, ห้ามแตะ `POINTS_ACCOUNT`/`PET_STAT`, lazy reset ไม่แตะ `total_bonding_score`) และ **ERR-17** (ใหม่ 1 รายการ — กรณี data integrity: มี PET active แต่หา `PET_CARE_STATE` คู่กันไม่เจอ, 500 INTERNAL_DATA_ERROR) ส่วน session หมดอายุ/role ผิด/actionType ไม่ถูกต้อง/ไม่มี PET active ใช้รหัส **ERR-01/ERR-02/ERR-07/ERR-03 เดิมซ้ำ** (ตรงกับ response จริงใน api-spec.md API-63 ทุกจุด ไม่ได้สร้างรหัสซ้ำซ้อนโดยไม่จำเป็น) — กำกับอ้างอิง **Gap G-API-12** ของ api-spec.md ไว้ในหัวข้อ 5 (ลำดับตรวจความหิวก่อนเพดานรวมเมื่อชนกันพอดี ยังไม่มี AC ยืนยันเป็นทางการ) โดยไม่พยายามปิด gap นี้เอง
  - แก้ `docs/02-design/02-technical/detailed-design.md` (hub) — เพิ่มแถว DD-09 ในตาราง "1. สารบัญ Flow", เพิ่ม BR-20~24 ในตาราง "2. กฎธุรกิจร่วม", เพิ่ม ERR-17 ในตาราง "3. แคตตาล็อกข้อผิดพลาดร่วม", อัปเดตบรรทัด "อัปเดตล่าสุด" และคำอธิบายขอบเขตด้านบน — ไม่แตะ DD-01~08/BR-01~19/ERR-01~16 เดิม
  - แก้ `docs/02-design/02-technical/detailed-design/buy-egg-and-choose-species.md` (DD-01) **จุดเดียว** — แทรกขั้นตอนย่อยใหม่ `f2. insert PET_CARE_STATE(pet_id=<petId ที่เพิ่ง insert>, hunger_today=0, daily_bonding_quota_used=0, total_bonding_score=0, last_recorded_date=วันนี้)` ระหว่างขั้นตอน f (insert PET_STAT) กับ g (commit) ในหัวข้อ "3. ขั้นตอนการทำงาน" เพื่อรองรับ 1:1 กับ `PET_CARE_STATE` (ENT-27) ที่ DD-09 ต้องพึ่งพา — ไม่แตะเนื้อหาอื่นใดในไฟล์นี้ทั้งหมด รวมถึงรหัส BR/ERR เดิม
  - แก้ `docs/02-design/02-technical/index.md` — อัปเดตคำอธิบาย detailed-design.md เป็น "DD-01 ถึง DD-09, BR-01 ถึง BR-24, ERR-01 ถึง ERR-17"
  - **Self-check ที่ทำจริง:** ตัวเลข 3 (เพดานความหิว), 10 (เพดานรวม), +1 (ทุก action) เทียบกับ acceptance-criteria.md (AC-FE-82-1/2, AC-FE-83-1/2/3) และ api-spec.md (API-63) แล้วตรงกันทุกจุด — กฎ boundary ทุกข้อระบุ `<`/`<=`/`=` ชัดเจน (ใช้ `<` ที่เพดานทั้งคู่ตามที่ api-spec.md ระบุ) — stateDiagram-v2 ใหม่ประกาศ `state "..." as shortId` ก่อนเสมอ ไม่มี quote ตรงในเส้น transition — รหัส DD-09/BR-20~24/ERR-17 ไม่ชนกับ DD-01~08/BR-01~19/ERR-01~16 เดิม (ตรวจแล้วว่า BR-19/ERR-16 คือเลขสูงสุดเดิมก่อนเพิ่ม)
  - **AC ที่ยังไม่ถูก flow ใดครอบคลุมโดยตรง:** AC-FE-84-1 (animation overlay ของ FE-84) — เป็นเรื่อง UI/animation ล้วน ไม่มีการคำนวณ/หักแต้ม/เปลี่ยนสถานะฝั่ง server จึงไม่อยู่ในขอบเขตของ Detailed Design (เอกสารนี้ครอบเฉพาะ flow ที่มี business logic ฝั่ง server ตามขอบเขตเดิมของทั้งเอกสาร)
  - ไม่มีการ commit/push ใดๆ — รอ user ยืนยันก่อน

- **nfr-writer (`sync-nfr`, mode: update, จุดเดียวใน NFR-07)** — ประเมินแล้วว่าฟีเจอร์ดูแลสัตว์เลี้ยง (FE-82/FE-83/FE-84, `CMP-21` Pet Care and Bonding Module, `ENT-27` PET_CARE_STATE) **ไม่ต้องการ NFR ใหม่** ตามที่ user ยืนยัน เพราะ NFR ที่มีอยู่ 13 ข้อครอบคลุมเพียงพอ (ฟีเจอร์เล็ก ไม่มี burst การใช้งาน ไม่มีข้อมูลอ่อนไหว ไม่มี external dependency ใหม่) — ไม่เพิ่มรหัส NFR-XX ใหม่ ไม่แตะ NFR-01 ถึง NFR-13 ข้ออื่นทั้งหมด รวมถึงตารางสรุปหัวข้อ 2 (ไม่มีแถวใหม่เพราะไม่มีรหัสใหม่เกิดขึ้น)
  - แก้ `docs/02-design/02-technical/non-functional-requirement.md` เฉพาะหัวข้อ **NFR-07** 2 จุด: (1) บรรทัด "ข้อกำหนด" — เติมคำอธิบายให้ชัดว่า "สถานะสัตว์เลี้ยง" รวมถึงข้อมูลใน `PET_CARE_STATE` (ความหิว, โควตาหาแต้มต่อวัน, ยอดสะสมรวมของค่าความผูกพัน) ตาม `database-schema.md` ENT-27 โดยคงตัวเลข RPO = 0 เดิมไว้ไม่เปลี่ยน (2) บรรทัด "กระทบสถาปัตยกรรมอย่างไร" — เติมอ้างอิงถึง `CMP-21` (Pet Care and Bonding Module) และ `ENT-27` ต่อจาก `CMP-07`/`CMP-08`/`CMP-16` เดิม เพื่อยืนยันว่า NFR-07 บังคับใช้กับข้อมูลชุดนี้ด้วยโดยไม่ต้องตีความไขว้เขวในอนาคต — ไม่แตะบรรทัด "ทำไมต้องมี", "วิธีพิสูจน์", "ผลถ้าไม่ทำตั้งแต่แรก" ของ NFR-07
  - ไม่แตะ "อัปเดตล่าสุด" ที่ header (ยังคง 2026-08-25 ค่าเดิมอยู่แล้ว) ไม่แตะไฟล์อื่นใด
- ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **requirement-doc-writer (mode: update, จุดเดียวต่อไฟล์)** — เปลี่ยนสถานะ [[../01-requirements/01-spec/20260825-07-pet-care-and-bonding|ระบบดูแลสัตว์เลี้ยง (Pet Care & Bonding)]] จาก `Draft` เป็น `Confirmed` ตามผล audit-backlog รอบวันนี้ที่ยืนยันว่าทั้งสายเอกสาร (feature-list FE-82/83/84, user-journey PC1/PC2, prototype v3, acceptance-criteria/test-case, architecture CMP-21, database-schema ENT-27, api-spec API-62/63, detailed-design DD-09, non-functional-requirement) ถูกสร้างครบสมบูรณ์แล้ว ตัวเลขนิ่งแล้วจริง (3 ครั้ง/วัน, 10 หน่วย/วัน, +1/action) และผ่านการตรวจสอบซ้ำหลายรอบโดย user เอง — แก้บรรทัด `สถานะ` เป็น `Confirmed` และเพิ่มบรรทัด "อัปเดตล่าสุด" บันทึกเหตุผลการเปลี่ยนสถานะ ไม่แตะเนื้อหาอื่นของไฟล์
  - อัปเดต `docs/01-requirements/backlog.md` แถวของเอกสารนี้ เปลี่ยนคอลัมน์สถานะจาก `Draft` เป็น `Confirmed` ไม่แตะแถวอื่น/คอลัมน์อื่น
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **test-writer (`sync-test-plan`, mode: update, เฉพาะหัวข้อ "ช่องว่างความครอบคลุม (Coverage Gap)" ท้าย `docs/03-testing/01-test-plan/acceptance-criteria.md` เท่านั้น)** — เพิ่ม Coverage Gap ใหม่ 3 รายการต่อท้าย G-12 เดิม (ตรวจแล้วว่า G-13/14/15 ยังว่างจริงก่อนเพิ่ม) ตามผล audit-backlog รอบ 2026-08-25 ที่ user ยืนยันให้บันทึกไว้ก่อน ยังไม่แก้ตอนนี้ — ไม่แตะ AC ใดๆ, ไม่แตะ Traceability Matrix, ไม่แตะเนื้อหาส่วนอื่นทั้งหมดของไฟล์:
  - **G-13** — มี **52 wikilink** ในเอกสารชี้ไปยังไฟล์ test-case ที่ไม่เคยถูกสร้างจริง 5 ไฟล์ (`test-cases/student-identity-and-roster`, `test-cases/teacher-lesson-and-quiz`, `test-cases/student-learn-and-pet`, `test-cases/teacher-class-overview`, `test-cases/score-export-gradebook`) ทำให้ AC ประมาณ 115 จาก 178 รหัส (~65%) ไม่มี test case รองรับเลย รวมฟีเจอร์ระดับ Must เช่น FE-01/FE-16/FE-20 — Traceability Matrix ตาราง 1 ที่สรุปว่า "ไม่มี FE ตัวใดตกหล่น" จึงคลาดเคลื่อนในส่วนความครอบคลุมฝั่ง test case
  - **G-14** — FE-78/79/80/81 (ไอเทมช่วยในการต่อสู้/HP, spec 06, ระดับ Should) ไม่มี AC รองรับเลยแม้แต่ข้อเดียว และไม่เคยถูกประกาศไว้ในหัวข้อ "ขอบเขตของรอบนี้" หรือ Coverage Gap ใดมาก่อน ทั้งที่ feature-list.md/user-journey.md (UJ-03 node P1/P2/P3) มีครบแล้ว
  - **G-15** — FE-25 ("ซื้อของแต่งตัวด้วยแต้ม") ถูกประกาศเลิกใช้แล้วใน feature-list.md/user-journey.md (แทนที่ด้วย FE-78 ตั้งแต่ 2026-08-23) แต่หัวข้อ `### FE-25` ในเอกสารนี้ และ `test-cases/points-ledger.md` (TC-FE-25-001~004) ยังไม่มีคำว่า "เลิกใช้" กำกับ ยังนำเสนอเป็นฟีเจอร์ปกติ
  - ทั้ง 3 รายการเป็นการบันทึกช่องว่างเท่านั้น ยังไม่ได้แก้ปัญหาจริง (ยังไม่สร้างไฟล์ test-case ที่ขาด, ยังไม่เขียน AC-FE-78~81, ยังไม่เติมหมายเหตุเลิกใช้ที่ FE-25) — รอคำสั่งแยกต่างหากในรอบถัดไป
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **prototype-writer (mode: update, จุดเดียว)** — แก้ `docs/02-design/01-prototypes/v3-20260823-student-egg-economy/index.html` เท่านั้น ตามผล `audit-technical-spec` รอบ 2026-08-25 ที่พบว่า `api-spec.md` (API-63) และ `docs/02-design/02-technical/detailed-design/care-for-pet.md` (DD-09) ล็อกค่า `actionType` ของการอาบน้ำเป็น `"bathe"` (คำกริยา ตรงกับ `feed`/`pat`/`play` ที่เป็นคำกริยาทั้งหมด) แต่ prototype ใช้ `"bath"` (ไม่มี e) ไม่ตรงกัน — user ยืนยันให้ยึด `"bathe"` เป็นค่าจริง แก้ 2 จุดในไฟล์เดียวกันให้ตรงกัน: บรรทัด `} else if (kind === "bath") {` เป็น `"bathe"` (ในฟังก์ชัน `triggerCareAnimation`) และ `data-care-action=\"bath\"` เป็น `\"bathe\"` (ปุ่ม "อาบน้ำ" ในหน้า `screen-pet`) — ตรวจสอบ flow `data-care-action` → `handleCareAction` → `handleGenericCareAction` → `triggerCareAnimation` แล้วยืนยันว่าไม่มีจุดเทียบค่าอื่นตกหล่น (grep `bath` หลังแก้เหลือแค่ 2 จุด คือ class name `.care-bath-overlay`/`bubble.className` ที่ตั้งใจไม่แตะตามคำสั่ง เพราะเป็นชื่อ CSS class ไม่ใช่ค่า enum)
  - **ไม่ได้แตะ** ชื่อ CSS class `.care-bath-overlay` (ทั้ง `<style>` และ `bubble.className`), action อื่น (`feed`/`pat`/`play`), หน้าจออื่นในไฟล์, README.md ของ v3
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **prototype-writer (mode: update, จุดเดียว)** — แก้ `docs/02-design/01-prototypes/v1-20260821-student-learn-and-pet/README.md` เท่านั้น ตามผล `audit-prototype` รอบ 2026-08-25 ที่พบว่า README v1 อ้างถึงสิ่งที่ล้าสมัยไปแล้ว 2 เรื่องเทียบกับสถานะปัจจุบันของเอกสารต้นทาง: (1) อ้าง "ร้านของแต่งตัว"/FE-25 ทั้งที่ `feature-list.md` ประกาศ FE-25 เลิกใช้ตั้งแต่ 2026-08-23 (แทนที่ด้วย FE-78) และ (2) ใช้ชื่อ design token สายพันธุ์แบบเก่า `--species-1` ถึง `--species-5` (5 สายพันธุ์) ทั้งที่ `DESIGN.md` ล็อกชื่อ token เป็น `--species-wing/leg/tail` (3 สายพันธุ์) ตั้งแต่ 2026-08-22/23 — เพิ่มย่อหน้าหมายเหตุใหม่ต่อจาก metadata หัวไฟล์ อธิบายว่า README/`index.html` ฉบับนี้เขียนเมื่อ 2026-08-21 ก่อนการล็อกทั้ง 2 เรื่องดังกล่าว จึงไม่ตรงกับ `DESIGN.md`/`feature-list.md` ฉบับปัจจุบัน**โดยตั้งใจ** เพราะโฟลเดอร์นี้เป็น frozen baseline เก็บไว้เทียบเวอร์ชันเท่านั้น (ไม่ใช่บั๊ก) พร้อมชี้ผู้อ่านไปดูกลไก/token ฉบับล่าสุดที่ `v3-20260823-student-egg-economy/` แทน
  - **ไม่ได้แตะ** `index.html` ของ v1 (frozen baseline ตามที่ระบุใน `docs/02-design/01-prototypes/index.md`), ไม่แตะ README/`index.html` ของ v2 หรือ v3, ไม่แตะเนื้อหาอื่นในไฟล์ README v1 (ตาราง feature ที่ครอบ, รายชื่อหน้าจอ, click-through path, ตาราง MOCK เดิมทั้งหมดคงไว้เหมือนเดิม)
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **test-writer (`sync-test-plan`, mode: update, จุดเดียว)** — แก้ `docs/03-testing/01-test-plan/test-cases/pet-care-and-bonding.md` บรรทัดเดียว (ตารางองค์ประกอบ UI แถว "ปุ่มอาบน้ำ") ตามผล `audit-technical-spec` รอบ 2026-08-25 ที่พบว่า `api-spec.md`/`DD-09` ล็อกค่า `actionType` ของการอาบน้ำเป็น `"bathe"` (ตรงกับ feed/pat/play ที่เป็นคำกริยาทั้งหมด) แต่ไฟล์นี้เขียนอ้างอิงไว้เป็น `"bath"` (ไม่มี e) — user ยืนยันให้ยึด `"bathe"` เป็นค่าจริง (prototype v3 กำลังถูกแก้ตามพร้อมกันในงานคู่ขนาน) — แก้ `data-care-action="bath"` เป็น `data-care-action="bathe"` ในบรรทัดนั้นเพียงจุดเดียว

- **architecture-writer (`sync-architecture`, mode: update, เฉพาะหัวข้อ 7)** — แก้ `docs/02-design/02-technical/architecture.md` ตามผล `audit-technical-spec` ที่พบว่าตาราง RBAC ในหัวข้อ 7 (Authentication & Authorization) ไม่ได้อัปเดตให้รวม `CMP-21` (Pet Care and Bonding Module) ที่เพิ่มเข้ามาวันนี้ ทั้งที่หัวข้อ 4 (ผู้ใช้และบทบาท) ระบุไว้แล้วว่านักเรียนเข้าถึง CMP-21 ได้ (จัดการสัตว์เลี้ยงของตนเอง) และครูเข้าถึงไม่ได้เลย — แก้ 2 จุดในหัวข้อ 7 เท่านั้น: (1) ขยายช่วง "ทุก endpoint ของ CMP-03 ถึง CMP-15" เป็น "CMP-03 ถึง CMP-15 และ CMP-21" (2) ตาราง RBAC แถว "นักเรียน" เพิ่ม `CMP-21 (จัดการสัตว์เลี้ยงของตนเอง)` ต่อจาก CMP-10 เดิม แถว "ครู" เพิ่มหมายเหตุในคอลัมน์ "สิทธิ์" ว่าห้ามเข้าถึง CMP-21 เช่นกัน (ไม่เพิ่ม CMP-21 เข้าคอลัมน์ CMP ที่เข้าถึงได้ของครู เพราะเข้าถึงไม่ได้จริง) — ไม่แตะหัวข้อ 1-6, 8, 9, ตาราง Component หัวข้อ 3, diagram หัวข้อ 2, Data Flow หัวข้อ 6
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน
  - **ไม่ได้แตะ** ชื่อ CSS class `care-bath-overlay` (แค่ชื่อ class ของ prototype ไม่ใช่ค่า enum ยืนยันแล้วว่าเป็นจุดเดียวที่เหลือของคำว่า "bath" ในไฟล์นี้), ไม่แตะ test case ID/AC references/ตัวเลข mock อื่นใดในไฟล์
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **api-db-writer (`sync-api-db`, mode: update, จุดเดียวต่อไฟล์)** — แก้ 2 ไฟล์ตามผล `audit-technical-spec` ที่พบว่ายังไม่ได้เพิ่ม `CMP-21`/`ENT-27` (PET_CARE_STATE) เข้ารายการข้อมูลที่ครูห้ามเข้าถึง:
  - แก้ `docs/02-design/02-technical/database-schema.md` — บรรทัด "ข้อมูลที่ครูห้ามเข้าถึงไม่ว่ากรณีใด" (ท้ายหัวข้อ 5) เพิ่ม `PET_CARE_STATE` เข้ารายการ (ต่อท้าย `PET_STAT`) ให้ครบเป็น `POINTS_ACCOUNT, POINTS_LEDGER_ENTRY, PET, PET_STAT, PET_CARE_STATE, STUDENT_ITEM_INVENTORY, ARENA_MATCH_PARTICIPANT, MATCH_ITEM_USAGE`
  - แก้ `docs/02-design/02-technical/api-spec.md` หัวข้อ 1.7 — เพิ่มย่อหน้าใหม่ต่อท้ายรายการ field เดิม ระบุว่าข้อมูลของ `CMP-21` (ความหิว, โควตาหาแต้มต่อวัน, ยอดสะสมรวมของค่าความผูกพัน) ปัจจุบันไม่มี endpoint ฝั่งครูเข้าถึงเลย (ไม่มี `GET /teacher/...` ที่แตะ `ENT-27`) แต่ถ้าเปิด endpoint ฝั่งครูในอนาคตต้องเพิ่มเข้ารายการห้ามส่งทันที
  - ไม่แตะ ENT-XX/API-XX เดิมทุกตัว, ไม่แตะเนื้อหาอื่นในทั้งสองไฟล์
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **detailed-design-writer (`sync-detailed-design`, mode: update, เฉพาะหัวข้อ "5. กรณีผิดพลาดและการรับมือ" ของ 2 ไฟล์ ไม่แตะหัวข้ออื่น ไม่แตะ DD-XX/BR-XX/ERR-XX เดิม ไม่แตะไฟล์ detailed-design อื่นใด)** — ปิด gap ที่ user ยืนยันแล้วว่า API-41/API-45 มี field บังคับ (`shopItemId`) แต่หัวข้อ 5 ของ DD-04/DD-06 ไม่มีแถวกรณี "ข้อมูลนำเข้าไม่ถูกต้อง" ต่างจาก DD-01/02/03/05/07/09 ที่มี ERR-07 กำกับชัด (สไตล์อ้างอิงจากแถว ERR-07 ของ `care-for-pet.md` DD-09):
  - แก้ `docs/02-design/02-technical/detailed-design/purchase-battle-item.md` (DD-04) — เพิ่มแถว `| ERR-07 | ไม่ส่ง shopItemId หรือรูปแบบไม่ถูกต้อง (ตาม API-41) | ปฏิเสธก่อนแตะ transaction ใดๆ | 400 VALIDATION_ERROR — "กรุณาเลือกไอเทมที่ต้องการซื้อ" |` ในตารางหัวข้อ 5 ต่อจากแถว ERR-01 ก่อนแถว ERR-03
  - แก้ `docs/02-design/02-technical/detailed-design/use-item-in-match.md` (DD-06) — เพิ่มแถว `| ERR-07 | ไม่ส่ง shopItemId หรือรูปแบบไม่ถูกต้อง (ตาม API-45) | ปฏิเสธก่อนแตะ transaction ใดๆ | 400 VALIDATION_ERROR — "กรุณาเลือกไอเทมที่ต้องการใช้" |` ในตารางหัวข้อ 5 ต่อจากแถว ERR-01 ก่อนแถว ERR-03
  - ใช้รหัส `ERR-07` เดิมที่มีอยู่แล้วในตาราง "3. แคตตาล็อกข้อผิดพลาดร่วม" ของ `detailed-design.md` (hub) — ไม่ได้ออกรหัสใหม่ ไม่แตะ hub, ไม่แตะหัวข้อ 1-4/6 ของทั้งสองไฟล์, ไม่แตะไฟล์ detailed-design อื่นใด (ไม่รอการแก้ `api-spec.md` API-41/API-45 คู่ขนาน ตามที่สั่ง)
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **api-db-writer (`sync-api-db`, mode: update, เฉพาะ `docs/02-design/02-technical/api-spec.md`, 2 จุด, ไม่แตะ `database-schema.md` และไม่แตะรหัส `API-XX`/`ENT-XX` ใดๆ ที่มีอยู่แล้ว)** — แก้ตามขอบเขตที่ user ยืนยันแล้วและผ่าน `audit-technical-spec`:
  - **จุดที่ 1** — เพิ่มแถว `400 VALIDATION_ERROR` ในตาราง "Response ผิดพลาด" ของ `API-41` (ซื้อไอเทมช่วย, บรรทัด 1229-1233) และ `API-45` (ใช้ไอเทมช่วยในแมตช์, บรรทัด 1334-1340) สำหรับกรณีไม่ส่ง field บังคับ `shopItemId` หรือรูปแบบไม่ถูกต้อง — ใช้ error code `VALIDATION_ERROR` ตาม pattern เดียวกับ endpoint อื่นที่มี field บังคับในไฟล์นี้ (เช่น API-01, API-36, API-62/63) ซึ่งตรงกับ `ERR-07` (400 VALIDATION_ERROR) ใน `detailed-design.md`
  - **จุดที่ 2** — เพิ่ม field "ระดับความละเอียด" ที่ header ต้นไฟล์ (บรรทัดใหม่ ระหว่างบรรทัด "รูปแบบ" กับ "Base path") ระบุ "Conceptual + Contract-level (ระบุ HTTP method/path/JSON shape ตามที่ architecture.md ข้อ 7 ตัดสินใจไปแล้ว ไม่ผูกกับ framework ที่ implement จริง)" ให้สม่ำเสมอกับ field เดียวกันใน `architecture.md`/`database-schema.md`/`detailed-design.md` — ไม่แก้เนื้อหาบรรทัด "รูปแบบ" (REST/JSON/Session-based) เดิม
  - **ยืนยัน:** ไม่ได้แตะรหัส `API-XX`/`ENT-XX` ตัวใดเลย, ไม่ได้แก้ `database-schema.md`, ไม่แตะ endpoint อื่นนอกจาก API-41/API-45
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **audit-technical-spec (full scope C1–C10, ตรวจโดย `technical-spec-auditor` แล้วผู้ใช้สุ่มยืนยันเองอีก 4 ข้อ)** — ตรวจ `architecture.md`, `database-schema.md`, `api-spec.md`, `detailed-design.md` + 9 flow ย่อย, `non-functional-requirement.md` เทียบกับ requirement/feature-list/user-journey/acceptance-criteria/test-case ทั้งหมด (การบ้าน RAISE2 W4D1 Technical Spec):
  - **เครื่องมือที่ใช้:** สคริปต์ Python นับรหัส/ตัวเลขข้ามไฟล์ + `mermaid.parse()` จริงผ่าน mermaid.js v11.16.0 + Node/jsdom (ไม่ใช่ตรวจด้วยตา)
  - **ผลผ่าน (ยืนยันเองซ้ำแล้ว):** C1 traceability (CMP=21, ENT=27, API=63, DD=9, dangling=0), C2 Mermaid parse 22/22 ผ่านจริง, C3 ตัวเลข/enum ตรงข้ามไฟล์ทุกจุด, C7 NFR วัดได้ 13/13, C8 wikilink 86 เส้น broken=0, C9 endpoint นับได้ 63 (GET23+POST33+PUT5+PATCH2) ตรงกับที่เอกสารอ้างถึงตัวเอง — บั๊กเดิม bathe/bath enum และ CMP-21 RBAC gap (จาก commit ก่อนหน้า) ยืนยันว่าแก้แล้วสะอาดจริง ไม่มีจุดตกหล่น
  - **พบปัญหาใหม่ 4 จุด และแก้ครบแล้วตามที่ user เลือก (ทุกจุดแนะนำว่า "แก้ตาม" และ user ยืนยัน):**
    1. [ปานกลาง] `architecture.md:156` ระบุครูเข้าถึง CMP-09/CMP-10 แต่ `api-spec.md` ไม่มี endpoint ฝั่งครูแตะ CMP-09 เลย → แก้โดยตัด CMP-09 ออก เหลือ CMP-10 อย่างเดียว
    2. [ปานกลาง] `architecture.md:147` เขียนว่า CMP-08 "ส่งสัญญาณให้ CMP-21 รีเซ็ต" ขัดกับ `database-schema.md`/`care-for-pet.md` (DD-09) ที่ยืนยันว่าเป็นการ insert แถวใหม่ ไม่มี event ข้ามโมดูล → แก้ข้อความให้ตรงกลไกจริง
    3. [เล็กน้อย] `API-41`/`API-45` มี field บังคับ `shopItemId` แต่ไม่มี 400 VALIDATION_ERROR กำกับ, `DD-04`/`DD-06` ก็ไม่มีแถวอ้างอิงคู่กัน → เพิ่ม 400 VALIDATION_ERROR ใน `api-spec.md` และเพิ่มแถว ERR-07 ใน `purchase-battle-item.md`/`use-item-in-match.md`
    4. [เล็กน้อย] `api-spec.md` header ไม่มี field "ระดับความละเอียด" เหมือน 3 ไฟล์พี่น้อง → เพิ่ม field ให้สม่ำเสมอ
  - **ผลตรวจซ้ำของผู้ใช้เอง (ไม่ใช่แค่เชื่อ agent):** grep นับ endpoint 63/63 ตรง, grep CMP-XX 21/21 ตรง, grep ENT-XX 27/27 ตรง, grep `bath` ทั้ง repo สะอาดจริง, เปิดไฟล์อ่านบรรทัด 147/156 ของ architecture.md ตรงกับที่ agent อ้างคำต่อคำ, เปิด api-spec.md/DD-04/DD-06 ยืนยันแถวที่เพิ่มถูกต้องหลังแก้
  - **สรุปสถานะการบ้าน:** ไฟล์ครบทั้ง 4 หัวข้อ (Architecture, API Spec, Database Schema, Detailed Design + NFR แบบ optional) ไม่มีจุดรุนแรง (severe) ปานกลาง 2 + เล็กน้อย 2 จุดถูกแก้ครบแล้ว
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **test-writer (`sync-test-plan`, mode: update)** — ปิด Coverage Gap **G-14** และ **G-15** ตามขอบเขตที่ user ยืนยันแล้ว (agent อื่นกำลังสร้าง `test-cases/weekly-arena.md`/`student-identity-and-roster.md` ฯลฯ คู่ขนานอยู่ ไม่ได้แตะไฟล์เหล่านั้น):
  - **ปิด G-14** — แก้ `docs/03-testing/01-test-plan/acceptance-criteria.md` ต่อท้ายหมวด F เท่านั้น (ไม่แตะ AC เดิมข้อใด) — เพิ่ม **12 AC ใหม่**: `AC-FE-78-1~4` (ร้านค้าไม่มีหมวดของแต่งตัวเหลือ, ไอเทมไม่กระทบค่าสเตตัส, หักเฉพาะแต้มที่ใช้ได้ไม่กระทบสะสมรวม, แต้มส่วนเกินหลังร่างที่ 4 ซื้อไอเทมต่อได้), `AC-FE-79-1~4` (HP=0 จบแมตช์ทันทีนับเป็นแพ้, จบแมตช์แล้วไม่เสียแต้ม/ไม่ลดขั้น/กลับสภาพเดิม, "ตาย" เป็นสภาพในแมตช์เท่านั้นไม่ถาวร, ไอเทมไม่เพิ่มบทลงโทษการแพ้), `AC-FE-80-1,2` (ล้างสถานะใช้ได้ 1 ครั้ง/แมตช์แล้วหมดไป, ใช้ซ้ำในแมตช์เดียวกันไม่ได้), `AC-FE-81-1,2` (ป้องกันโจมตีถึงตายกัน HP=0 ได้ 1 ครั้ง/แมตช์, ครั้งที่สอง HP ถึง 0 ได้ตามปกติ) — ดึงกฎธุรกิจ/ตัวเลขที่ล็อกแล้วจาก [[../01-requirements/01-spec/20260823-06-battle-support-items|spec 06]] ทั้งหมด (1 ครั้ง/แมตช์, HP=0=ตาย=แพ้, 51 แต้ม) **ไม่ตั้งตัวเลขใหม่เอง** — ราคาไอเทม/สูตรคำนวณ HP ที่ spec 06 ยังไม่ล็อก (Open Question ข้อ 1, 4) ไม่ถูกผูกไว้ใน AC ข้อใดเลย (เขียนแบบไม่พึ่งตัวเลขเหล่านั้นได้ครบทุกข้อ) **จึงไม่ต้องเปิด Coverage Gap G-16 ใหม่** — ทั้ง 9 ข้อใน AC checklist ของ spec 06 เองมีตัวรับ AC-FE-78~81 ตรงครบ 9/9 ข้อ ไม่มีข้อใดตกหล่น — อัปเดต Traceability Matrix ครบ 3 ตาราง: ตาราง 1 เพิ่ม 4 แถว FE-78/79/80/81 ชี้ไป `test-cases/weekly-arena` (ไฟล์นี้กำลังถูกสร้างคู่ขนานโดย agent อื่น) รวมเป็น **75 แถว**, ตาราง 2 เพิ่ม FE-78/79/80/81 เข้าแถว UJ-03 และปิด node P1/P2/P3 ที่เคยขาด AC, ตาราง 3 เพิ่มแถวใหม่ spec 06 (9 ข้อมีตัวรับตรงครบ) พร้อมปรับยอดรวมทั้งตาราง (104→113 บรรทัด, 94→103 ข้อมีตัวรับตรง) — แก้สถานะ **G-14 เดิม**: ต่อท้ายข้อความเดิมด้วยบรรทัด "สถานะ: ปิดแล้ว 2026-08-25 — เพิ่ม AC-FE-78~81 แล้ว" (ไม่ลบข้อความเดิม)
  - **ปิด G-15** — เพิ่มหมายเหตุเลิกใช้ที่หัวข้อ `### FE-25` ของ `acceptance-criteria.md` (หมวด D) และที่ `docs/03-testing/01-test-plan/test-cases/points-ledger.md` (ทั้ง header บรรทัด "ครอบ FE" และหัวข้อ `## FE-25`) โดยยึดถ้อยคำ/รูปแบบเดียวกับที่ `feature-list.md` ทำไว้แล้ว ("~~ซื้อของแต่งตัวด้วยแต้ม~~ **[เลิกใช้ 2026-08-23 — แทนที่ด้วย FE-78]**") — **ไม่ลบ** AC-FE-25-1/AC-FE-25-2 หรือ TC-FE-25-001~004 ข้อใดเลย ทุกข้อยังอยู่ครบ มีเพียงหมายเหตุกำกับว่าไม่ใช่พฤติกรรม/เคสที่ต้องทดสอบซ้ำอีกต่อไป — แก้สถานะ **G-15 เดิม**: ต่อท้ายข้อความเดิมด้วยบรรทัด "สถานะ: ปิดแล้ว 2026-08-25" (ไม่ลบข้อความเดิม)
  - **ไม่ได้แตะ:** AC-XX เดิมข้อใดนอกจาก FE-25/78/79/80/81 (ยืนยันด้วยการอ่านทวนทุกจุดที่แก้), ไฟล์ `test-cases/` อื่นนอกจาก `points-ledger.md` (ไม่แตะ `weekly-arena.md`/`student-identity-and-roster.md` ที่ agent อื่นกำลังสร้างคู่ขนาน), Coverage Gap G-01 ถึง G-13 (แก้ได้เฉพาะ G-14/G-15 ตามสั่ง)
  - **เพิ่มบล็อกคอมเมนต์ปิดท้ายไฟล์** `acceptance-criteria.md` บันทึกสรุปการแก้รอบนี้ต่อจากบล็อกเดิมของรอบ FE-82/83/84 (ไม่ลบของเดิม)
  - **หมายเหตุ:** จำนวนรวม "104→113 บรรทัด" และ "94→103 ข้อมีตัวรับตรง" ในตาราง 3 ยังคงความคลาดเคลื่อนทางเลขคณิตเดิมที่มีอยู่ก่อนแก้ (ยอดรวม 3 หมวดย่อยไม่เท่ากับยอดบรรทัดที่ยังใช้งานเพราะมี 1 ข้อของ spec 02 เรื่องอาร์ตเวิร์กที่ไม่ถูกนับในหมวดใดเลย) — ไม่ใช่สิ่งที่เกิดจากการแก้รอบนี้ บันทึกหมายเหตุไว้ในตารางให้ทีมพิจารณาแก้รอบถัดไป ไม่ได้แก้เองเพราะอยู่นอกขอบเขตงานที่สั่ง (G-14/G-15 เท่านั้น)
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **test-writer (`sync-test-plan`, mode: new — สร้างไฟล์เดียว)** — สร้าง `docs/03-testing/01-test-plan/test-cases/student-identity-and-roster.md` เพื่อปิดส่วนหนึ่งของ Coverage Gap **G-13** (52 wikilink ใน `acceptance-criteria.md` ชี้ไปไฟล์ test-case ที่ยังไม่เคยสร้างจริง 5 ไฟล์ — ไฟล์นี้คือ 1 ใน 5 ไฟล์นั้น) ครอบหมวด A ทั้ง 17 FE (FE-01, FE-02, FE-03, FE-04, FE-05, FE-06, FE-07, FE-47, FE-48, FE-49, FE-50, FE-51, FE-52, FE-53, FE-54, FE-55, FE-56) ตามขอบเขตที่ user ยืนยันแล้ว (agent อื่นกำลังสร้าง `weekly-arena.md` ฯลฯ คู่ขนานอยู่ ไม่ได้แตะไฟล์เหล่านั้นหรือ `acceptance-criteria.md`/`test-plan.md`/wikilink ใดๆ ตามที่สั่งห้าม):
  - เขียนรวม **39 test case** (`TC-FE-01-001~004`, `TC-FE-02-001~002`, `TC-FE-03-001~002`, `TC-FE-04-001~002`, `TC-FE-05-001~003`, `TC-FE-06-001~002`, `TC-FE-07-001~002`, `TC-FE-47-001~002`, `TC-FE-48-001~003`, `TC-FE-49-001~002`, `TC-FE-50-001~002`, `TC-FE-51-001~002`, `TC-FE-52-001~002`, `TC-FE-53-001~002`, `TC-FE-54-001~002`, `TC-FE-55-001~002`, `TC-FE-56-001~003`) ครอบ AC ทั้ง 39 ข้อของหมวด A ครบทุกข้ออย่างน้อย 1 test case ตามระดับความละเอียดที่ user เลือก (happy path + negative — ทุก AC มี happy path อย่างน้อย 1 ข้อ, AC ที่มีเงื่อนไขขอบเขต/ข้อผิดพลาดมี negative/edge case เพิ่ม ไม่ทำ permission/concurrent แบบเต็มรูปแบบ) — แยกตามประเภท: **Happy path 17, Negative 11, Edge case 9, Permission 2** (รวม 39)
  - ใช้ชื่อปุ่ม/ข้อความหน้าจอจริงจาก prototype `v1-20260821-student-learn-and-pet/index.html` (หน้า `screen-login` — FE-01) และ `v2-20260821-teacher-classroom-manage/index.html` (หน้า `screen-login`/`screen-overview`/`screen-roster`/modal `pin-modal` — FE-02/03/04/05/06/07/49/50/51/52/55/56) ยืนยันข้อความ/รหัสจริงจากซอร์สของ prototype โดยตรง (เช่น toast "นำเข้ารายชื่อจากไฟล์สำเร็จ 3 คน (ใช้คอลัมน์ที่เลือกเป็นรหัสประจำตัว ไม่นำเข้าชื่อจริง)", ข้อความ modal รีเซ็ต PIN, รหัสนักเรียน mock 4101–4113/4114-4116/4119-4120) — **28 test case (FE-01/02/03/04/05/06/07/49/50/51/55/56) รันบน prototype ได้จริง 100% วันนี้**
  - **FE-47, FE-48, FE-53 (9 test case รวม) เขียนจากข้อความ AC โดยตรงตามที่ Coverage Gap G-02 ของ `acceptance-criteria.md` บันทึกไว้แล้วว่าไม่มีหน้าจอจริงเทียบ** — ไม่ได้เดากลไกเพิ่มเติมเอง มีหมายเหตุกำกับไว้ในแต่ละเคสว่าอ้างอิง G-02
  - **FE-52 (2 test case)** — บางส่วนรันได้จริง (คอลัมน์ "เข้าใช้งานล่าสุด" ในหน้าภาพรวมชั้นเรียนของ v2 แสดงผลค่า mock ได้จริง) แต่ **AC-FE-52-1 ขั้นตอน "ล็อกอินแล้วค่าอัปเดตทันที" พิสูจน์ด้วยการคลิกจริงใน prototype ไม่ได้** เพราะ prototype v1 (นักเรียน) กับ v2 (ครู) เป็นคนละไฟล์ mock state แยกกันคนละชุด ไม่เชื่อมต่อกัน — บันทึกหมายเหตุไว้ในไฟล์ ไม่ได้เดาว่าทำได้
  - **พบเพิ่มเติมนอกขอบเขตที่สั่ง (รายงานไว้ ไม่ได้แก้เอง เพราะห้ามแตะ `acceptance-criteria.md`):** FE-54 (กรองคำหยาบชื่อเล่นอัตโนมัติ) ก็ไม่มีหน้าจอ/กลไกใน prototype v1 หรือ v2 เลยเช่นเดียวกับ FE-47/48/53 (ตรวจสอบด้วย grep คำว่า "คำหยาบ"/"ไม่เหมาะสม" ในทั้งสองไฟล์ index.html แล้วไม่พบ) แต่ Coverage Gap **G-02 ยังไม่ได้ระบุ FE-54 ไว้** (ระบุเฉพาะ FE-47/FE-48/FE-53/FE-41) — เขียน 2 test case ของ FE-54 จากข้อความ AC โดยตรงเช่นเดียวกับ 3 FE ที่เหลือ พร้อมบันทึกข้อสังเกตนี้ไว้ท้ายไฟล์ test-case ให้ผู้ดูแลเอกสารพิจารณาเสริม FE-54 เข้า G-02 ในรอบตรวจสอบถัดไป (ไม่ใช่ Coverage Gap ใหม่ที่เปิดเอง เป็นเพียงข้อสังเกตที่ยังไม่ได้บันทึกเป็นทางการ)
  - **ไม่มี AC ข้อใดในหมวด A ที่กำกวมเกินกว่าจะเขียน test step ได้** — ทุกข้อเขียนได้ครบจากเนื้อหา AC + prototype (หรือจากข้อความ AC ตรงๆ เมื่อไม่มีหน้าจอ)
  - ไม่ได้แก้ไฟล์อื่นใดนอกจากสร้างไฟล์นี้ไฟล์เดียว (ไม่แตะ `acceptance-criteria.md`, `test-plan.md`, `test-cases/index.md` หรือ wikilink ใดๆ ตามที่สั่งห้ามไว้ชัดเจน)
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **test-writer (`sync-test-plan`, mode: update, ต่อท้ายไฟล์เดียวเท่านั้น)** — เพิ่ม test case สำหรับ FE-78/79/80/81 (ไอเทมช่วยในการต่อสู้/HP, [[../01-requirements/01-spec/20260823-06-battle-support-items|spec 06]]) ต่อท้าย `docs/03-testing/01-test-plan/test-cases/weekly-arena.md` เท่านั้น ตามขอบเขตที่ user ยืนยันแล้วเพื่อปิด Coverage Gap **G-14** ฝั่ง test-case (ไม่แตะ test case เดิม TC-FE-27~46 ข้อใดเลย ไม่แตะไฟล์อื่นใดนอกจากไฟล์นี้):
  - **พบระหว่างทำงานว่าอีก agent หนึ่งได้เพิ่ม `AC-FE-78-1~4`/`AC-FE-79-1~4`/`AC-FE-80-1,2`/`AC-FE-81-1,2` (รวม 12 ข้อ) เข้า `acceptance-criteria.md` หมวด F เสร็จสมบูรณ์แล้วก่อนไฟล์นี้เขียนเสร็จ** (ไม่ใช่สถานะ pending ตามที่คำสั่งงานตั้งต้นคาดไว้) — ยืนยันด้วยการอ่านไฟล์จริงแทนการเดาต่อ แล้วแก้ TC ทั้งชุดให้อ้างอิงรหัส AC จริงที่ยืนยันแล้วครบทุกข้อ (ไม่มีรหัส AC ค้าง "(pending)" เหลืออยู่ในไฟล์)
  - เขียนรวม **16 test case ใหม่**: `TC-FE-78-001~006` (ครอบ AC-FE-78-1~4 ครบ 4/4 ข้อ), `TC-FE-79-001~004` (ครอบ AC-FE-79-1~4 ครบ 4/4 ข้อ), `TC-FE-80-001~003` (ครอบ AC-FE-80-1,2 ครบ 2/2 ข้อ), `TC-FE-81-001~003` (ครอบ AC-FE-81-1,2 ครบ 2/2 ข้อ) — **12/12 AC ที่ยืนยันแล้วมีตัวรับอย่างน้อย 1 test case** — แยกตามประเภท: **Happy path 6, Boundary 1, Negative 8, Edge case 1** (รวม 16) — 3 เคสเสริม (`TC-FE-78-006`, `TC-FE-80-003`, `TC-FE-81-003`) ทดสอบ negative case ตามระดับความละเอียดที่ user สั่ง (ซื้อไอเทมตอนแต้มไม่พอ / ใช้ไอเทมที่ไม่ได้ถือครองอยู่) แต่ไม่มี AC ข้อใดครอบโดยตรง จึงอ้างอิงเป็น "เงื่อนไขสะท้อนกลับ" ของ AC ที่ใกล้ที่สุดร่วมกับ `api-spec.md`/`detailed-design/*.md` แทน ไม่ได้ผูกรหัส AC ปลอมขึ้นมาเอง
  - ใช้ตัวเลข/ข้อความจริงจาก prototype `v3-20260823-student-egg-economy/index.html` หน้า `screen-shop`/`screen-arena` (ราคาไอเทมสมมติ ล้างสถานะ 6 แต้ม/ป้องกันการโจมตีถึงตาย 10 แต้ม, HP สูงสุดสมมติ 50, ข้อความ toast/badge จริงจาก source) — ยืนยันความสอดคล้องกับ `api-spec.md` (API-40/41/45) และ `detailed-design/purchase-battle-item.md` (DD-04)/`use-item-in-match.md` (DD-06) ก่อนเขียนทุกจุด ไม่ตั้งตัวเลขใหม่เอง
  - **10 เคสรันได้วันนี้เต็มรูปแบบ, 5 เคสรันได้บางส่วน, 1 เคสยังรันไม่ได้ (`TC-FE-81-002`)** — ตัวเลข/กฎที่ spec 06 ยังไม่ล็อกซึ่งกระทบ: (1) ราคาไอเทมจริง (2) สูตรคำนวณ HP และสูตรตัดสินแพ้-ชนะ (Open Question ข้อ 4 — กระทบ `TC-FE-79-001`/`TC-FE-81-002` ตรงๆ ทำให้ `TC-FE-81-002` รันไม่ได้เลย) (3) กฎการถือครองไอเทม (Open Question ข้อ 5) (4) การจำลองสถานะพลังพิเศษบนตัวสัตว์เลี้ยงระหว่างแมตช์ — prototype ไม่มีเลย กระทบ `TC-FE-80-001` โดยตรง (พิสูจน์ "ล้างสถานะจริง" ไม่ได้ ยืนยันได้เฉพาะ "ใช้แล้วหมดสภาพ")
  - **จุดสังเกตที่พบเพิ่มเติม (บันทึกไว้ในไฟล์ ไม่ได้แก้เอง เพราะห้ามแตะไฟล์อื่น):** `AC-FE-81-1` เขียนว่าไอเทม "ป้องกันการโจมตีถึงตาย" ทำงาน **"อัตโนมัติ"** ตอนกำลังจะโดนโจมตีถึงตาย แต่ prototype v3 ใช้ปุ่ม "ใช้" แบบเดียวกับ "ล้างสถานะ" (นักเรียนกดเปิดใช้งานเองล่วงหน้า ไม่ใช่ระบบสั่งงานอัตโนมัติ) — ระบุไว้ชัดใน `TC-FE-81-001` ว่าพิสูจน์ความหมาย "อัตโนมัติ" ไม่ได้ ไม่ใช่ Coverage Gap ใหม่ที่เปิดเอง เป็นข้อสังเกตให้ผู้ดูแล prototype/AC พิจารณาว่าจะแก้ฝั่งไหน
  - แก้ 2 บรรทัดใน header ของไฟล์ (เดิมที่มีอยู่แล้ว ไม่ได้เพิ่มใหม่): บรรทัด "ครอบ FE" เพิ่ม FE-78/79/80/81 ต่อท้าย, บรรทัด "อ้างอิง AC" เพิ่ม AC-FE-78-1~4/79-1~4/80-1,2/81-1,2 ต่อท้าย (รหัสเดิมในทั้งสองบรรทัดไม่ถูกแตะ)
  - ไม่ได้แก้ `acceptance-criteria.md`, `test-plan.md`, `test-cases/index.md` หรือไฟล์อื่นใดตามที่สั่งห้ามไว้ชัดเจน (มี agent อื่นกำลังแก้ไฟล์เหล่านี้คู่ขนานอยู่)
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **sync-test-plan (orchestrator consolidation, 7 agent คู่ขนาน + แก้เอง 2 จุด)** — ปิด Coverage Gap G-13/G-14/G-15 ครบตามที่ user ยืนยัน ("ตรวจสอบและแก้ไขให้ครบก่อน") ทวนงานของทั้ง 7 agent เองแล้ว (นับ TC จริงด้วย grep เทียบกับรายงาน, เปิดอ่าน AC-FE-78 เต็มเนื้อหายืนยัน Given-When-Then ครบ, เทียบ index.md กับไฟล์จริง):
  - **G-13 ปิดแล้ว:** สร้างไฟล์ test-case ใหม่ครบ 5 ไฟล์ — `student-identity-and-roster.md` (39 TC, ยืนยันด้วย grep ตรงกับรายงาน), `teacher-lesson-and-quiz.md` (26 TC, ยืนยันตรง), `student-learn-and-pet.md` (4 TC), `teacher-class-overview.md` (17 TC, ยืนยันตรง), `score-export-gradebook.md` (22 TC, ยืนยันตรง) — รวม **108 test case ใหม่** ครอบ AC หมวด A/B/C/H/I เกือบทั้งหมด (ยกเว้นจุดที่บันทึกไว้แล้วใน G-02/G-05) — แก้ `acceptance-criteria.md` เพิ่มบรรทัดสถานะ "ปิดแล้ว" ต่อท้าย G-13 เอง และแก้ `docs/03-testing/01-test-plan/index.md` ที่ล้าสมัย (ยังไม่มีลิงก์ 5 ไฟล์ใหม่แม้ประทับวันที่ 2026-08-25 ไว้แล้ว) เพิ่มลิงก์ให้ครบ
  - **G-14 ปิดแล้ว:** ยืนยันจาก agent อื่นแล้ว (ดู entry ด้านบน) — AC-FE-78~81 (12 ข้อ) + TC-FE-78~81 (16 ข้อ) ครบ
  - **G-15 ปิดแล้ว:** ยืนยันจาก agent อื่นแล้ว — หมายเหตุเลิกใช้ FE-25 เพิ่มครบทั้ง acceptance-criteria.md และ points-ledger.md โดยไม่ลบ AC/TC เดิม
  - **G-02 ขยายเพิ่ม:** agent ที่เขียน `student-identity-and-roster.md` พบว่า FE-54 (กรองคำหยาบชื่อเล่น) ก็ไม่มีกลไกจำลองในทั้ง v1/v2 เหมือนกัน (grep "คำหยาบ"/"ไม่เหมาะสม" ไม่พบ) แต่ไม่เคยถูกบันทึกใน G-02 มาก่อน — เพิ่ม FE-54 เข้ารายการ G-02 เอง
  - **G-16 เปิดใหม่ (พบระหว่างเขียน test case รอบนี้ ยังไม่ได้แก้):** prototype 3 จุดไม่ตรงกับ AC ที่ยืนยันแล้ว — (1) `renderExportColumnMap()` ใน v2 ไม่ตรวจคอลัมน์ปลายทางซ้ำตาม AC-FE-68-2 (2) `renderOverview()` ใน v2 ไม่แสดงข้อความสถานะว่างเมื่อกรองไม่พบผลลัพธ์ตาม AC-FE-62-3 (3) prototype v3 ใช้ปุ่มกดเอง (manual) สำหรับไอเทมป้องกันการโจมตีถึงตาย ขัดกับ AC-FE-81-1 ที่ระบุว่าต้องทำงานอัตโนมัติ — บันทึกไว้เป็น gap ใหม่ ยังไม่ได้ตัดสินใจว่าจะแก้ prototype หรือแก้ AC เพราะเกินขอบเขตที่ user ยืนยันไว้รอบนี้ (เฉพาะ G-13/14/15)
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน

- **audit-prototype (v1/v2/v3 ทั้งชุด, ตรวจโดย `prototype-auditor` ในเบราว์เซอร์จริง)** — ตรวจ 23 ข้อ ผ่าน 16, พบปัญหา 4 (ยืนยัน G-16 ทั้ง 3 จุดตรงกับที่บันทึกไว้ด้วยเบราว์เซอร์จริง ไม่ใช่แค่อ่านโค้ด), ยังไม่ได้พิสูจน์ 3 (touch target 44/48px ติด bug เครื่องมือ viewport 0×0 — ไม่ใช่บั๊ก prototype) ผมสุ่มยืนยันเองก่อนเชื่อรายงาน 4 ข้อ (เปิดโค้ดจริง 3 จุด + grep นับ token) พบ **1 จุดคลาดเคลื่อน**: รายงานบอก "3 token ไม่ตรง" (`--species-1/2/3`) แต่นับจริงเจอ **5 ตัว** (`--species-1` ถึง `--species-5`) — ยึดค่านับเองแทน (DESIGN.md เก็บ species-4/5 ไว้เป็นประวัติแบบขีดฆ่า แต่ v2 ก็ยังมีทั้ง 5 ตัวเป็น dead code เหมือนกันหมด)
  - user ยืนยันให้แก้ทั้ง 4 จุด: #1/#2 แก้ prototype v2 ให้ตรง AC, #3 แก้ v3 เฉพาะกรณี fixed-outcome ปัจจุบัน (ไม่รอสูตร HP จริงจาก spec 06), #4 ลบ dead token ทิ้ง
  - ส่งงานให้ `prototype-writer` 2 ตัวคู่ขนาน (v2, v3) — **ทั้งคู่ hit session usage limit กลางทางจน task แสดงสถานะ "failed"** แต่ Edit ที่ทำไปก่อนโดนตัดยังคงอยู่จริง (verify แล้วว่าไม่ใช่ partial-write เสีย) — v2 agent ทำครบ 3 จุดจริง, v3 agent ทำ auto-trigger logic เสร็จแต่ report ขาดหาย (ไม่ได้บอกว่าทดสอบยังไง)
  - **ทั้งสอง agent ไม่มี tool เบราว์เซอร์ (prototype-writer มีแค่ Read/Write/Edit/Grep/Glob)** เป็นความผิดพลาดของผมเองที่สั่งให้ agent "ทดสอบด้วยเบราว์เซอร์จริง" ทั้งที่ agent type นี้ไม่มีเครื่องมือนั้น — ไปตรวจเองด้วยเบราว์เซอร์จริงแทน (Claude_Browser + javascript_tool ขับ event จริงเพราะ viewport 0×0 ทำให้ click ตาม coordinate ใช้ไม่ได้ในเซสชันนี้) พบบั๊กเพิ่มอีก 1 จุดที่ agent ทั้งสองไม่ทันสังเกต: ปุ่ม "ใช้" แบบ manual ที่ยังค้างอยู่คู่กับไอเทม guard ใน v3 ทำให้กดก่อนดูผลได้แล้วไปเซ็ต `usedThisMatch.guard=true` โดยไม่ได้ป้องกันจริง (ก่อนแก้ manual click ไม่มีผลเกมใดๆ ต่อ guard เลย) ทำให้ auto-trigger เงื่อนไข `!usedThisMatch.guard` เป็น false และ HP ตกถึง 0 ได้ทั้งที่ถือไอเทมอยู่ — **แก้เพิ่มเอง** โดยเอาปุ่ม manual ออกเฉพาะไอเทม `guard` ในฟังก์ชัน `itemChipHtml()` (เหลือไว้ให้ `cleanse` ตามเดิม) เพราะ AC-FE-81-1/2 ไม่เคยพูดถึงการกดใช้เองเลย เป็น automatic-only ตามเจตนา
  - ยืนยันทั้ง 4 จุดด้วยเบราว์เซอร์จริง (ขับผ่าน `javascript_tool` เพราะ `computer`/`read_page` ใช้ไม่ได้จาก viewport 0×0 บั๊ก): (1) v2 ตั้งคอลัมน์ปลายทาง 2 ฟิลด์ให้ซ้ำกัน → เห็นข้อความเตือน 2 จุด + ปุ่ม preview/confirm ปิดจริง แก้กลับไม่ซ้ำ → ข้อความหาย ปุ่มเปิดคืน (2) v2 ตรวจงานค้าง 2 คน (4106, 4109) ให้ครบแล้วกรอง "เฉพาะที่มีงานค้างตรวจ" → ได้ `colspan="8"` ตรงกับจำนวนคอลัมน์จริง พร้อมข้อความ "ไม่มีนักเรียนที่ตรงเงื่อนไขนี้ในขณะนี้" ตรงคำ AC เป๊ะ (3) v3 grep `--species` เหลือ 0 จุดทั้งไฟล์ (4) v3 เล่นจริงครบ flow (ตั้งแต้ม 51 → ซื้อไข่ปีก → ปลดล็อกร่างที่ 4 → เติมแต้ม 20 → ซื้อไอเทม guard 10 แต้ม → เข้าอารีน่าเลือกคู่แข่ง "ฟ้า" ที่ outcomeMock=lose → เห็น item-chip เปลี่ยนเป็น "พร้อมป้องกันอัตโนมัติเมื่อ HP จะถึง 0" ไม่มีปุ่มใช้แล้ว → กด "ดูผลการแข่งขัน" โดยไม่แตะปุ่มไอเทมเลย → **HP จบแมตช์ = 1/50** (ไม่ใช่ 0) พร้อมข้อความ "🛡️ ไอเทมป้องกันการโจมตีถึงตายทำงานอัตโนมัติ กัน HP ไม่ให้ลดถึง 0 ได้ทันโดยไม่ต้องกดใช้เอง (FE-81)" ตรงตาม AC-FE-81-1 ทุกประการ
  - ปิด Coverage Gap **G-16** — เพิ่มบรรทัดสถานะ "ปิดแล้ว" ต่อท้ายใน `acceptance-criteria.md` พร้อมค่าที่วัดได้ก่อน/หลัง
  - ไฟล์ที่แก้: `v2-20260821-teacher-classroom-manage/index.html` (renderExportColumnMap, renderOverview, ลบ `--species-1~5`), `v3-20260823-student-egg-economy/index.html` (arena result branch, itemChipHtml)
  - ยังไม่ได้ commit/push — รอ user ยืนยันก่อน
