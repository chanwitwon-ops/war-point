# Log 2026-08-23

- สร้างเอกสาร requirement ใหม่: [[../01-requirements/01-spec/20260823-06-battle-support-items|ไอเทมช่วยในการต่อสู้และการใช้แต้มส่วนเกิน (Battle Support Items & Surplus Points)]] — เปลี่ยนแนวคิดร้านค้าจากของแต่งตัวเพื่อความสวยงามเป็นไอเทมช่วยลดความซวยจากดวงในการต่อสู้ เพิ่มแนวคิด HP ในแมตช์ และปิดความเสี่ยงเดิมเรื่อง "51 แต้มอาจถึงร่างที่ 4 เร็วเกินไป" ด้วยการใช้แต้มส่วนเกินเป็นโบนัสซื้อไอเทม
- อัปเดตเอกสาร requirement เดิม: [[../01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|ระบบสัตว์เลี้ยง แต้ม และการแข่งขันรายสัปดาห์ (Pet, Points & Weekly Arena)]] — ยกเลิกหมวดของแต่งตัว (แทนที่ด้วยไอเทมช่วยตาม spec 06), เพิ่มหมายเหตุว่าผลของแมตช์มีไอเทมช่วยเข้ามาเกี่ยวได้แต่ค่าสเตตัสยังมาจากขั้นการเติบโตล้วนๆ, และปิด Open Question 2 ข้อ (ราคาของแต่งตัว, ความเสี่ยง 51 แต้มถึงร่างที่ 4 เร็วเกินไป)
- อัปเดต `docs/01-requirements/backlog.md` — เพิ่มแถวใหม่วันที่ 2026-08-23 ชี้ไปยัง spec 06 สถานะ Confirmed
- อัปเดต `docs/01-requirements/feature-list.md` (mode: update) — เปลี่ยนสถานะ `FE-25` (ซื้อของแต่งตัวด้วยแต้ม) เป็น **เลิกใช้** พร้อมขีดฆ่าชื่อ/คำอธิบายเดิมและกำกับว่าแทนที่ด้วย `FE-78` (คงรหัสไว้ ไม่ลบ ไม่ย้ายออกจากกลุ่ม E); เพิ่ม `FE-78` (ร้านไอเทมช่วยที่ซื้อด้วยแต้ม, กลุ่ม E), `FE-79` (HP ในแมตช์และการจบแมตช์เมื่อ HP หมด, กลุ่ม F), `FE-80` (ไอเทมล้างสถานะได้ 1 ครั้ง, กลุ่ม F), `FE-81` (ไอเทมป้องกันการโจมตีถึงตายได้ 1 ครั้ง, กลุ่ม F) ทุกตัวระดับ Should; เพิ่มหมายเหตุอัปเดตที่ `FE-27` ว่าค่าสเตตัสยังมาจากขั้นการเติบโตล้วนๆ แต่ผลของแมตช์มีไอเทมและ HP เข้ามาเกี่ยวได้; นับตารางสรุป MoSCoW ใหม่ทั้งหมด (Must 26, Should 45, Could 7, Won't 2, เลิกใช้ 1, รวม 81); ปิดช่องว่างเดิม 2 ข้อ (ราคาของแต่งตัวยังไม่ล็อก, ต้นทุน 51 แต้มอาจถึงร่างที่ 4 เร็วเกินไป — ปิดเพราะแต้มส่วนเกินมีร้านไอเทมรองรับแล้ว) ขยายช่องว่างเดิม 1 ข้อ (สูตรตัดสินแพ้-ชนะ เพิ่มประเด็นสูตรคำนวณ HP) และเพิ่มช่องว่างใหม่ 5 ข้อ (รายการ/ราคาไอเทมยังไม่ล็อก, HP เป็นค่าสเตตัสหรือไม่, ข้ออ้างเรื่องเกรด 1 ยังไม่มีกลไกรองรับ, กฎการถือครองไอเทมยังไม่กำหนด, ช่องโหว่รีเซ็ตแล้วได้แต้มไอเทมคืนวนได้) — คงรหัส `FE-01` ถึง `FE-77` เดิมไว้ครบทุกตัว
- อัปเดต `docs/01-requirements/user-journey.md` (mode: update) — **UJ-01**: แก้ node `O` จาก "ซื้อของแต่งตัวด้วยแต้ม (FE-25)" เป็น "ใช้แต้มส่วนเกินซื้อไอเทมช่วยในการต่อสู้จากร้านค้า (FE-78)" พร้อมแก้ decision node `M` และตาราง Mapping ให้ตรง เพิ่มหมายเหตุอธิบายการเปลี่ยนสถานะ `FE-25`; **UJ-03**: เพิ่ม node ใหม่ 3 จุด (`P1` ใช้ไอเทมล้างสถานะ/ป้องกันการโจมตีถึงตาย FE-80/FE-81, `P2` HP ลดลงจนแมตช์จบเมื่อถึง 0 FE-79, `P3` ยืนยันว่าจบแมตช์แล้วสัตว์เลี้ยงกลับสภาพเดิมไม่เสียแต้มไม่ลดขั้น FE-79) แทรกระหว่างขั้นจับคู่กับขั้นตัดสินผล และปรับข้อความ node `H` ให้สื่อว่าผลแมตช์มี HP/ไอเทมร่วมตัดสินด้วย พร้อมอัปเดตตาราง Mapping — node id ที่เพิ่ม (`P1`, `P2`, `P3`) ไม่ชนกับ id เดิม (`A0`, `A`–`O` ของ UJ-03) — คงรหัส `UJ-01` ถึง `UJ-04` เดิมไว้ครบทุกตัว ไม่แตะ `UJ-02`/`UJ-04`
- สร้าง interactive prototype v3 ตาม `build-prototype`: **[[../02-design/01-prototypes/v3-20260823-student-egg-economy/README|v3-20260823-student-egg-economy]]** (ครอบ UJ-01 ฝั่งนักเรียนทั้งเส้น + UJ-03 เฉพาะส่วนที่นักเรียนเห็น) — สร้างใหม่ 2 ไฟล์: `index.html` (9 หน้าจอ: ล็อกอิน/บทเรียนของฉัน/ทำแบบทดสอบ/ผลคะแนน/ซื้อไข่/ข้อมูลสายพันธุ์+อัลบั้ม/สัตว์เลี้ยงของฉัน/ร้านไอเทมช่วย/อารีน่า, ~1600 บรรทัด, self-contained ไม่มี network request ภายนอก, 0 hex นอก `:root`) และ `README.md`; **ไม่แก้ v1/v2** เก็บไว้เทียบ
- อัปเดต `DESIGN.md` (mode: update) 3 จุดก่อนเริ่มเขียน prototype: **2.5** ลดสายพันธุ์จาก 5 เหลือ 3 สาย เปลี่ยนชื่อ token `--species-1/2/3` เป็น `--species-wing/leg/tail` (ค่าสีเดิมไม่เปลี่ยน) ขีดฆ่า `--species-4`/`--species-5` พร้อมกำกับวันที่ (คงค่าเดิมไว้เป็นประวัติ ไม่ลบ) และเพิ่มหมายเหตุที่ตาราง contrast ข้อ 2.6 ว่าไม่กระทบค่าที่วัดไว้; **3.5** เปลี่ยนชื่อหัวข้อเป็น "ป้ายบอกค่าสเตตัสและพลังพิเศษ" ขีดฆ่าข้อความเดิมที่จัดประเภทไฟไหม้/ติดพิษ/มึนงงเป็นค่าสเตตัสผิด แก้เป็นพลังพิเศษประจำสาย (ตัวอย่าง เพิ่มได้) และระบุค่าสเตตัสจริงคือความเร็ว/พละกำลัง/ความแม่นยำ (placeholder); เพิ่ม **3.8** แถบ HP และ **3.9** ป้ายไอเทม เป็น component pattern ใหม่; อัปเดตหัวข้อ "ประเด็นที่ยังไม่ชัดเจน" ให้ตรงกับ 3 สายพันธุ์/15 ภาพ, ปิดสถานะเกณฑ์แต้มต่อขั้นที่ล็อกแล้ว, และเพิ่มประเด็นใหม่เรื่อง HP อาจเป็นค่าสเตตัสตัวที่ 4 — ทุกจุดที่แก้ใช้ `~~...~~` ขีดฆ่าพร้อมกำกับ `[แก้ 2026-08-23]` ไม่ได้ลบข้อความเดิม
- อัปเดต `docs/02-design/01-prototypes/index.md` — เพิ่มแถวตารางของ v3 พร้อมสรุปขอบเขตและสถานะ Draft
- เตรียมเครื่องมือสำหรับ phase Technical Spec (การบ้าน RAISE2 W4D1) — สร้าง agent 5 ตัว: `architecture-writer`, `api-db-writer`, `detailed-design-writer`, `nfr-writer` (เขียนไฟล์) และ `technical-spec-auditor` (read-only ไม่มี Write/Edit) พร้อม skill คู่กัน 5 ตัว: `sync-architecture`, `sync-api-db`, `sync-detailed-design`, `sync-nfr`, `audit-technical-spec` — ตรวจแล้วว่าไฟล์ครบ 10/10 ไฟล์ frontmatter ถูกต้อง ชื่อตรงกับโฟลเดอร์ และ auditor ไม่มีสิทธิ์เขียนจริง
- อัปเดต `CLAUDE.md` — แก้สถานะ repo (เดิมเขียนว่ายังไม่มีเอกสารเนื้อหาและยังไม่มี `DESIGN.md` ซึ่งไม่ตรงกับความจริงแล้ว), ขยาย document pipeline ให้ครอบคลุมสาย `02-design/02-technical/` (architecture -> db/api -> detailed design -> NFR), เพิ่มรหัสที่ห้ามเปลี่ยน (`CMP-XX`, `ENT-XX`, `API-XX`, `DD-XX`, `BR-XX`, `ERR-XX`, `NFR-XX`), เพิ่ม 5 แถวในตาราง agent/skill และเพิ่มลำดับการเริ่มงานข้อ 5-6
- ยังไม่ได้สร้างเอกสาร technical spec ใดๆ และยังไม่ได้ commit/push — รอ user ตัดสินใจเรื่องลำดับงานและว่าจะเลือก tech stack ก่อนหรือทำแบบ conceptual
- สร้างเอกสาร High-Level Architecture ใหม่ (mode: new) ผ่าน agent `architecture-writer`: **[[../02-design/02-technical/architecture|architecture.md]]** — stack: conceptual (ไม่ระบุภาษา/framework/ยี่ห้อ DB ใดๆ ใช้บทบาทกลางแทน), tier_style: 3-tier monolith (Client / API Server ก้อนเดียวแบ่งโมดูลภายในตามโดเมน / Relational Database ก้อนเดียว) พร้อมเหตุผลอ้างอิงบริบท 1 ห้องเรียน ~30 คน และ Session-based auth ที่ user เลือก, scope: all (ครอบ FE-01 ถึง FE-81 ยกเว้น FE-35/FE-36 ระดับ Won't) ออกรหัส `CMP-01` ถึง `CMP-20` (Client 2, Logic 13, Data 2, External 3) ทุกตัวอ้างอิง FE ต้นทางได้ครบ ไม่มี component ที่ไม่มีต้นทาง; วาด data flow ครบ 4 เส้น `DF-01` ถึง `DF-04` ตรงกับ UJ-01 ถึง UJ-04 พร้อมตาราง failure point ทุกเส้น; ออกแบบ Authentication เป็น Session-based (stateful) — นักเรียนใช้รหัสประจำตัว+PIN (บังคับเปลี่ยนครั้งแรก FE-47, ล็อคชั่วคราว FE-48) ครูใช้ Google OAuth (FE-03/FE-49); Authorization เป็น RBAC 2 บทบาท (นักเรียน/ครู) พร้อมระบุชัดว่าแต้ม/ค่าสเตตัส/ขั้นการเติบโต/HP-ไอเทมในแมตช์ต้อง **ซ่อนที่ชั้น server (ไม่ส่ง field มาเลย) ไม่ใช่แค่ปิดปุ่มที่ client** ตามที่ `20260821-04-teacher-class-overview` ยืนยัน; บันทึก Open Decision 6 ข้อ (จำนวนหลัก PIN/เกณฑ์ล็อค, OAuth scope, จังหวะ sync Google Sheets, นโยบายส่งคะแนนซ้ำ, sync/async ของ AI Gateway, real-time หรือ polling ของโหมดฉายผลในห้อง, สูตรตัดสินแพ้-ชนะ/HP) และ Gap 4 ข้อ (acceptance-criteria.md ยังไม่ครอบ FE-79 ถึง FE-81, ช่องโหว่รีเซ็ตแล้วได้แต้มไอเทมคืนวนซ้ำ, กฎการถือครองไอเทมยังไม่กำหนด, เกณฑ์ "ไม่เข้าใช้งานนาน" ของ FE-59 ยังไม่ล็อก) — อ่านต้นทางครบทั้ง `01-spec/*.md` (7 ฉบับ), `feature-list.md`, `user-journey.md`, `backlog.md`, `acceptance-criteria.md` (บางส่วนที่เกี่ยวข้อง เพราะไฟล์เกิน 256KB) และ prototype v1/v2/v3 (ดูรายชื่อไฟล์จาก Glob ไม่ได้เปิดอ่านทุกไฟล์ prototype)
- อัปเดต `docs/02-design/02-technical/index.md` — เพิ่มลิงก์ไปยัง `architecture.md` พร้อมสรุปขอบเขตสั้นๆ
- ยังไม่ได้ commit/push — รอ user ตรวจสอบ Open Decision และ Gap ที่ระบุไว้ก่อน แล้วจึงตัดสินใจว่าจะทำ `sync-api-db` ต่อหรือเลือก tech stack ก่อน
- สร้างเอกสาร Database Schema และ API Spec ใหม่ (mode: new) ผ่าน agent `api-db-writer`: **[[../02-design/02-technical/database-schema|database-schema.md]]** และ **[[../02-design/02-technical/api-spec|api-spec.md]]** — stack: conceptual (ชนิดข้อมูลกลางล้วน ไม่มี VARCHAR/BIGINT/ชื่อ DB engine), db_kind: relational (สืบทอดจาก CMP-16), scope: all (FE-01 ถึง FE-81 ยกเว้น FE-35/FE-36 Won't และ FE-25 เลิกใช้), นโยบายลบ: soft delete เป็นค่าเริ่มต้นทุกตาราง
  - **database-schema.md:** ออกรหัส `ENT-01` ถึง `ENT-26` ครบทุก FE ที่อยู่ในขอบเขต ไม่มีตารางใดไม่มีต้นทาง; ออกแบบ `PET` ให้ soft-delete เป็นกลไกเดียวกับ "อัลบั้มบันทึกเส้นทางการเติบโต" ของ FE-72/FE-73 โดยไม่ต้องมี entity แยก (แถว inactive = ประวัติ), บังคับ FE-74 (สัตว์เลี้ยงใช้งานได้ตัวเดียว) ด้วย unique partial index บน `PET(student_id)` กรองเฉพาะ `is_active=true`; ล็อกตารางต้นทุนแต้ม/แต้มสเตตัสให้ตรงกับ AC-FE-24-4~10 และ AC-FE-77-* (1/5/10/15/20 สะสม 51, สเตตัสรวม 50/100/150/200/250) ในตาราง `PET.stage` enum และหัวข้อ 4; ออกแบบ `STUDENT_QUIZ_RESULT` เก็บ `points_awarded_total` เพื่อคำนวณ delta แต้มถูกต้องตามกฎ "ยึดคะแนนรอบที่ดีที่สุด" (FE-41, AC-FE-41-1/2) ไม่ใช่บวกแต้มซ้ำทุกรอบ; `POINTS_LEDGER_ENTRY` เป็น append-only audit log กัน double-spend ตาม DF-01; `ARENA_MATCH`/`ARENA_MATCH_PARTICIPANT` ออกแบบให้ไม่มี field ค่าสเตตัส/แต้ม/ขั้นการเติบโตเจือปนเลยเพื่อรองรับกฎ "ซ่อน ไม่ใช่ disable"; บันทึกช่องว่าง G-DB-01 ถึง G-DB-11 (PIN/lockout ตัวเลข, จำนวน/สัดส่วนค่าสเตตัส, พลังพิเศษต่อสาย, สูตร HP/แพ้-ชนะ, ราคาไอเทม, กฎถือครองไอเทม, ช่องโหว่รีเซ็ตคืนแต้มไอเทม, เกณฑ์ไม่เข้าใช้งานนาน, นโยบายส่งคะแนนซ้ำ, รูปแบบไฟล์ดาวน์โหลด, คะแนนดิบ/ปรับสัดส่วนตอนส่งออก) — ไม่เดาตัวเลขใดที่ AC/spec ยังไม่ล็อก
  - **api-spec.md:** ออกรหัส `API-01` ถึง `API-61` (GET 22, POST 32, PUT 5, PATCH 2) ครอบทุก CMP module และทุก FE Must/Should/Could ที่อยู่ในขอบเขต (รวม FE-45 ผ่าน API-35/API-46 ที่เพิ่มเข้ามาหลังตรวจ traceability ซ้ำ); ทุก endpoint มีอย่างน้อย 1 error case พร้อม status code จริง; ยึดค่าตัวเลขจาก acceptance-criteria.md ทุกจุดที่ตรวจสอบได้ (เช่น `fullScore > 0`, `maxAttempts >= 1`, ต้นทุนขั้นแบบ incremental, เงื่อนไข `available_points >= required`) ไม่ได้ตั้งตัวเลขใหม่เอง; endpoint ที่ FE-78 ถึง FE-81 อ้างอิง (ราคาไอเทม, กฎถือครอง, สูตร HP/แพ้-ชนะ) ระบุไว้ชัดว่ายังไม่ล็อกในหัวข้อ Gap แทนการเดา; ทุก endpoint ใต้ `/teacher/...` ระบุกฎซ่อน field แต้ม/ขั้นการเติบโต/ค่าสเตตัส/HP/ไอเทมไว้ในหัวข้อ "กฎทางธุรกิจ" ของแต่ละ endpoint ที่เกี่ยวข้อง (โดยเฉพาะ API-47 ภาพรวมอารีน่าฝั่งครู และ API-55 ภาพรวมชั้นเรียน); บันทึกช่องว่าง G-API-01 ถึง G-API-11 สอดคล้องกับฝั่ง database-schema
  - เทียบตัวเลขข้ามเอกสารด้วยตนเอง (ไม่เชื่อรายงาน agent ตามตัวอักษร): ยืนยันจำนวน ENT ในตาราง 2 ตรงกับหัวข้อ 3 จริง (26 รายการ), นับจำนวน endpoint แยกตาม method จากตารางจริงแล้วพบว่าตัวเลขที่ agent สรุปไว้ครั้งแรก (GET 20/POST 30/PUT 6/PATCH 5) ผิด แก้เป็นตัวเลขที่นับได้จริง (GET 22/POST 32/PUT 5/PATCH 2 รวม 61), และเทียบตารางต้นทุนแต้มในทั้งสองไฟล์กับ AC-FE-24-4~10 ตรงกันครบ
  - อัปเดต `docs/02-design/02-technical/index.md` — เพิ่มลิงก์ไปยัง `database-schema.md` และ `api-spec.md`
  - **ไม่ได้แตะ** `acceptance-criteria.md`/`test-cases/*.md` ตามข้อกำหนด (ปัญหาที่ AC ยังไม่ครอบ FE-78 ถึง FE-81 เป็นหน้าที่ของ `sync-test-plan` รอบถัดไป)
  - ยังไม่ได้ commit/push — รอ user ตรวจสอบก่อนตัดสินใจว่าจะทำ `sync-detailed-design` ต่อหรือปิด Gap ที่ระบุไว้ก่อน (โดยเฉพาะสูตรตัดสินแพ้-ชนะ/HP และราคาไอเทม ซึ่งกระทบ detailed-design ของ CMP-10/CMP-09 โดยตรง)
- สร้างเอกสาร Detailed Design ใหม่ (mode: new) ผ่าน agent `detailed-design-writer`: **[[../02-design/02-technical/detailed-design|detailed-design.md]]** (ไฟล์ hub) + 8 ไฟล์ต่อ flow ใน `docs/02-design/02-technical/detailed-design/` — stack: conceptual (pseudo code ไม่ผูกภาษา/framework), scope: 8 flow ที่มีการคำนวณ/หักแต้ม/เปลี่ยนสถานะตามที่ user ยืนยัน (ซื้อไข่, ปลดล็อกขั้น, รีเซ็ต, ซื้อไอเทม, ส่งคำตอบแบบทดสอบ, ใช้ไอเทมในแมตช์, ประกาศผลรายสัปดาห์, ยืนยันส่งคะแนน)
  - ออกรหัส `DD-01` ถึง `DD-08`, `BR-01` ถึง `BR-19` (กฎธุรกิจร่วม รวม security/error catalog ไว้ที่ hub ตามที่ user สั่ง), `ERR-01` ถึง `ERR-16` — ทุกตัวเลขในกฎอ้างอิง AC-FE-24-4~10/AC-FE-75-*/AC-FE-77-* (ตารางต้นทุนแต้ม 1/5/10/15/20 สะสม 51, สเตตัส 50 ต่อขั้นรวม 250) ตรงกับ database-schema.md/api-spec.md ทุกจุด ไม่มีตัวเลขที่แต่งขึ้นเอง
  - **DD-03 (reset-pet):** ปิดช่องโหว่ "รีเซ็ตแล้วได้แต้มค่าไอเทมคืนวนซ้ำ" ด้วย `BR-09` — สมมติฐานที่ผู้ช่วยเสนอว่าคืนเฉพาะแต้มค่าไข่+ค่าขั้นของ PET เดิม (ตามตาราง BR-01 สูงสุด 51 แต้ม) ไม่คืนแต้มค่าไอเทม — **บันทึกชัดเจนว่าเป็น Open Decision ที่ยังไม่ผ่านการยืนยันจาก user อย่างเป็นทางการ ต้องเปิด requirement ใหม่ก่อนพัฒนาโค้ดจริง** พร้อมระบุผลข้างเคียงว่าจะทำให้สมมติฐานเดิมใน database-schema.md ENT-10/ENT-15 และ test case `TC-FE-26-002` ("available_points กลับมาเท่ากับ total_earned_points พอดีหลังรีเซ็ต") ไม่จริงอีกต่อไปถ้านักเรียนเคยซื้อไอเทม
  - **พบความขัดแย้งใหม่ระหว่างเอกสารต้นทาง (G-DD-02):** database-schema.md ข้อ ENT-15 บอกว่าการรีเซ็ตต้อง insert PET ใหม่ทันที (`stage=egg, stat_total=50`) แต่ api-spec.md API-38 (request/response) และ spec 02 ข้อ ซ.6 ยืนยันตรงกันว่ารีเซ็ต**ไม่สร้าง PET ใหม่ทันที** ต้องไปซื้อไข่แยกต่างหากจ่าย 1 แต้ม — เอกสารนี้ยึดตาม api-spec.md/spec 02 ในการเขียน DD-03 และรายงานความขัดแย้งนี้ให้แก้ที่ database-schema.md ในรอบ `sync-api-db` ถัดไป ไม่ได้แก้ไฟล์นั้นเอง (นอกขอบเขต agent นี้)
  - **DD-07 (announce-weekly-arena-result):** สูตรตัดสินแพ้-ชนะและสูตรคำนวณ HP ยังไม่มีจาก spec ใดๆ — เขียนเฉพาะโครง pseudo code เชิงแนวคิด (เปรียบเทียบค่าสเตตัสรวม ปรับด้วยผลไอเทม ลด HP ทีละรอบจนถึง 0) ไม่มีตัวเลข/สูตรที่แต่งขึ้นเอง บันทึกเป็น Gap G-DD-03 ชัดเจนว่าห้ามพัฒนาโค้ดจริงจนกว่าจะมีสูตร
  - **DD-04/DD-06 (purchase-battle-item, use-item-in-match):** กฎการถือครองไอเทมยังไม่ล็อก — เลือกพฤติกรรม "ใช้แล้วหักจำนวน (หมดไป)" เป็นค่าเริ่มต้นในการเขียน pseudo code เพราะตรงความหมาย "ใช้ได้ 1 ครั้ง" ของ spec 06 มากที่สุด แต่ระบุชัดว่ายังไม่ยืนยันจาก user (Gap G-DD-04); เพิ่ม `ERR-16` (ITEM_NOT_PRICED) เป็นมาตรการป้องกันไม่ให้ซื้อไอเทมด้วยราคา null/0 ก่อนราคาจะถูกล็อกจริง (Gap G-DD-05)
  - AC ที่ flow เหล่านี้ยังไม่มีรหัสอย่างเป็นทางการ (FE-26, FE-78, FE-80, FE-81) อ้างอิง spec 02/spec 06 และ test case `TC-FE-26-*` โดยตรงแทน ตามที่ acceptance-criteria.md เองระบุไว้ใน Coverage Gap G-06/G-08 และ architecture.md ข้อ 9 (AC ปิดก่อน spec 06 จะถูกเพิ่ม)
  - อัปเดต `docs/02-design/02-technical/index.md` — เพิ่มลิงก์ไปยัง `detailed-design.md`
  - ยังไม่ได้ commit/push — รอ user ตัดสินใจปิด Open Decision หลัก 2 เรื่อง (BR-09 การคืนแต้มตอนรีเซ็ต, สูตร HP/แพ้-ชนะ) ก่อนพัฒนาโค้ดจริง และพิจารณาแก้ความขัดแย้งที่พบใน database-schema.md ENT-15 (G-DD-02) ในรอบ `sync-api-db` ถัดไป
- อัปเดตเอกสาร requirement เดิม (mode: update): [[../01-requirements/01-spec/20260823-06-battle-support-items|ไอเทมช่วยในการต่อสู้และการใช้แต้มส่วนเกิน (Battle Support Items & Surplus Points)]] — ปิด Open Question ข้อ 6 (ช่องโหว่รีเซ็ต/เปลี่ยนสายพันธุ์แล้วได้ทั้งไอเทมและแต้มคืนวนซ้ำ) ที่ค้างมาจาก detailed-design (BR-09/Gap G-DD-01) ผู้ใช้ตัดสินใจว่า: รีเซ็ตคืนแต้มทั้งหมดที่เคยใช้กับสัตว์เลี้ยงตัวนั้น รวมแต้มค่าไอเทมด้วย (available_points กลับมาเท่ากับ total_earned_points เต็มจำนวนตามกฎข้อ ข.3 เดิม) แต่ไอเทมทั้งหมดที่ถือครองอยู่ ณ ตอนรีเซ็ตถูกเคลียร์ทิ้งพร้อมกัน ปิดช่องโหว่โดยไม่ให้ได้ทั้งไอเทมและแต้มคืนพร้อมกัน — ข้อความเดิมเก็บไว้แบบขีดฆ่า ไม่ลบทิ้ง; ยังไม่ได้แก้ `docs/02-design/02-technical/detailed-design/*.md` (BR-09, G-DD-01) ให้ตรงกับการตัดสินใจนี้ ต้องทำในรอบ `sync-detailed-design` ถัดไป; backlog.md แถวของเอกสารนี้มีอยู่แล้ว (2026-08-23, Confirmed) ไม่ต้องเพิ่มแถวใหม่
- อัปเดตเอกสาร Database Schema (mode: update, targets: db เท่านั้น) ผ่าน agent `api-db-writer`: **[[../02-design/02-technical/database-schema|database-schema.md]]** — แก้ 2 จุดที่ตรวจพบระหว่าง `sync-detailed-design` (G-DD-02) และปิด Open Question ข้อ 6 ของ spec 06: ไม่แตะรหัส ENT-XX ใดเลย ไม่แก้ api-spec.md
  1. แก้ความขัดแย้งเรื่องการรีเซ็ตสร้าง PET ใหม่ทันทีหรือไม่ — เดิม ENT-15 (หัวข้อ 0 ข้อ 3, field species_id/is_active, กฎความถูกต้องระดับข้อมูล) เขียนว่าการรีเซ็ตต้อง insert แถว PET ใหม่ (stage='egg', stat_total=50) ในทรานแซกชันเดียวกัน ซึ่งขัดกับ api-spec.md (API-38) และ spec 02 ข้อ ซ.6 — แก้ให้ตรงกันทุกจุดว่าการรีเซ็ต (API-38) ทำแค่ปิดใช้งาน (is_active=false) แถว PET ปัจจุบันเท่านั้น ไม่ insert แถวใหม่ในจังหวะเดียวกัน นักเรียนต้องเรียก API-36 (ซื้อไข่) แยกต่างหากอีกครั้งเพื่อสร้าง PET ใหม่ เลือกสายพันธุ์ใหม่ และจ่าย 1 แต้ม
  2. เพิ่มกฎการเคลียร์ไอเทมตอนรีเซ็ต — บันทึกไว้ที่ ENT-15 (กฎความถูกต้องระดับข้อมูล), ENT-18 (กฎความถูกต้องระดับข้อมูล) และหัวข้อ 5 "การเก็บข้อมูลส่วนบุคคลและการลบ": การรีเซ็ตคืนแต้มทั้งหมดที่เคยใช้กับสัตว์เลี้ยงตัวนั้นเต็มจำนวน (รวมแต้มค่าไอเทม) ให้ available_points = total_earned_points พอดี แต่ต้องเคลียร์ STUDENT_ITEM_INVENTORY ของนักเรียนคนนั้นทั้งหมดในทรานแซกชันเดียวกัน โดยตั้ง quantity=0 ทุกแถว (ไม่ลบแถวจริง — ใช้ pattern soft delete เดียวกับทั้งเอกสาร) เพื่อปิดช่องโหว่ไม่ให้ได้ทั้งไอเทมและแต้มคืนพร้อมกัน
  - ปิด Gap G-DB-07 (ทำเครื่องหมาย "ปิดแล้ว (2026-08-23)" คงรหัสไว้ ไม่ลบ) — ตรวจแล้วว่าไม่มีจุดอื่นในไฟล์ที่ยังขัดกับข้อความใหม่ (grep คำว่า "รีเซ็ต"/"STUDENT_ITEM_INVENTORY" ครบทุกจุดในไฟล์)
  - ไม่ได้แตะ docs/02-design/02-technical/api-spec.md, detailed-design/*.md, หรือรหัส ENT-XX/API-XX ใดๆ ตามขอบเขตที่กำหนด — ยังไม่ได้ commit/push
- อัปเดตเอกสาร Detailed Design (mode: update, targets: เฉพาะ `detailed-design.md` แถว BR-09 + Gap G-DD-01/G-DD-02 และ `detailed-design/reset-pet.md` ทั้งไฟล์) ผ่าน agent `detailed-design-writer` — ปิด Open Decision ของ BR-09 ให้ตรงกับการตัดสินใจที่ยืนยันแล้ว (spec 06 Open Question ข้อ 6 ปิดแล้ว 2026-08-23, database-schema.md ENT-15/ENT-18/G-DB-07 แก้แล้วก่อนหน้านี้): ไม่แตะรหัส DD-XX/BR-XX/ERR-XX เดิม ไม่เพิ่มรหัสใหม่ ไม่แตะไฟล์ flow อื่น
  1. **BR-09 (แก้เนื้อหา ไม่เปลี่ยนรหัส):** ลบข้อความ "[Open Decision — สมมติฐานที่ผู้ช่วยเสนอ ยังไม่ยืนยัน]" — เขียนกฎยืนยันแล้วแทน: รีเซ็ตคืนแต้มทั้งหมดที่เคยใช้กับสัตว์เลี้ยงตัวนั้นเต็มจำนวน รวมแต้มค่าไอเทมด้วย ทำให้ `available_points = total_earned_points` พอดี (ตรงกฎเดิมข้อ ข.3 ไม่มีข้อยกเว้น) แลกกับการเคลียร์ `STUDENT_ITEM_INVENTORY` ทั้งหมดของนักเรียนคนนั้นพร้อมกัน (`quantity=0`) ในธุรกรรมเดียว, PET เดิมตั้ง `is_active=false` เท่านั้น ไม่ insert PET ใหม่ทันที — อ้างอิง spec 06 Open Question ข้อ 6 (ปิดแล้ว 2026-08-23) แทนที่การอ้าง "reset-pet หัวข้อ Gap" เดิม
  2. **Gap G-DD-01/G-DD-02:** ปิดสถานะทั้งคู่พร้อมกันเป็น "ปิดแล้ว (2026-08-23)" — ข้อความเดิมเก็บไว้แบบขีดฆ่าไม่ลบทิ้ง ตามธรรมเนียมโปรเจกต์ พร้อมหมายเหตุอ้างอิงว่า database-schema.md ถูกแก้ไปแล้วเช่นกัน (ไม่มีความขัดแย้งข้ามเอกสารอีกต่อไป)
  3. **`detailed-design/reset-pet.md` (DD-03) แก้ทั้งไฟล์:** sequence diagram เพิ่มขั้นตอนเคลียร์ `STUDENT_ITEM_INVENTORY` (ไม่มีขั้นตอน insert PET ใหม่อยู่แล้วเดิม จึงไม่ต้องลบ); pseudo code ขั้นตอน 4.b-4.f เปลี่ยนสูตรเป็น `refundAmount = total_earned_points - available_points ปัจจุบัน` (คืนเต็ม ไม่ใช่คำนวณเฉพาะค่าไข่+ค่าขั้นตาม BR-01 อีกต่อไป), ตั้ง `available_points = total_earned_points`, เพิ่มขั้นตอนเคลียร์ `STUDENT_ITEM_INVENTORY` ทั้งหมด (`quantity=0`) ในธุรกรรมเดียวกัน, คง `POINTS_LEDGER_ENTRY(entry_type='pet_reset_refund')`; ลบคำเตือน "Open Decision — สมมติฐานที่ผู้ช่วยเสนอ" ทั้งไฟล์ (คำนำ, ขั้นตอนที่ 4.b, ตาราง BR ในหัวข้อ 4); เพิ่มแถวกฎ BR-09 ใหม่ในตารางหัวข้อ 4 (ค่าขอบเขต `available_points = total_earned_points` เป๊ะ ไม่ใช่ `<=`, และไอเทมต้อง `quantity=0` ทุกแถว); เพิ่มแถว state transition ของ `STUDENT_ITEM_INVENTORY`; ปรับ edge case กดซ้ำ/race condition ให้ครอบคลุมคลังไอเทมด้วย; หัวข้อ 7 (Gap เฉพาะ flow) ปิด G-DD-01/G-DD-02 ทั้งคู่แบบขีดฆ่า+หมายเหตุปิดแล้ว — ตรวจ Mermaid ทุกจุดยังครอบ double quote ถูกต้องหลังแก้
  - ไม่ได้แตะไฟล์ flow อื่น (DD-01/02/04/05/06/07/08) หรือ error catalog/security ใน `detailed-design.md` — ยังไม่ได้ commit/push
- สร้างเอกสาร Non-Functional Requirement ใหม่ (mode: new, placement: standalone) ผ่าน agent `nfr-writer`: **[[../02-design/02-technical/non-functional-requirement|non-functional-requirement.md]]** — ออกรหัส `NFR-01` ถึง `NFR-13` ครบ 13 ข้อตามที่ user ยืนยันแล้วทั้งหมด ไม่มีข้อใดถูกตัด ครอบ 7 หมวด (Performance 3, Scalability 2, Availability & Reliability 2, Security 2, Usability 1, Maintainability 2, Compatibility 1) — Must 8 ข้อ (NFR-01/02/04/05/07/08/10/13), Should/เลื่อนได้ 5 ข้อ (NFR-03/06/09/11/12)
  - หัวข้อ 1 "บริบทที่ใช้ตั้งเกณฑ์" อธิบายชัดเจนว่า buffer โหลด/ความจุขนาด ~120 คน (เทียบเท่า 4 ห้องเรียน) ของ NFR-03/04/05 **ไม่ใช่การเปิดฟีเจอร์หลายห้องเรียน** — ไม่เพิ่ม `classroom_id`/`school_id` ไม่กระทบ `architecture.md`/`database-schema.md`/`api-spec.md` ที่ commit ไปแล้ว และ `FE-36` ยังคงเป็น Won't ตามเดิม
  - NFR-10 (contrast ≥ 4.5:1, พื้นที่กด ≥ 48px) อ้างอิงค่าที่วัดจริงแล้วใน `DESIGN.md` ข้อ 4.2/4.3 โดยตรง ไม่ได้ตั้งตัวเลขใหม่ซ้อนที่อาจขัดกัน — ตรวจแล้วว่าไม่ขัดกับตัวเลขใน `test-plan.md` (test-plan ระบุไว้แล้วว่า Performance/Load Testing ยังไม่อยู่ในขอบเขตทดสอบรอบนี้ จึงไม่มีตัวเลข performance เดิมให้ขัดกัน)
  - หัวข้อ 5 "รอ user ตัดสินใจ" ว่างเปล่าตามที่ user ยืนยันว่าทั้ง 13 ข้อผ่านการตัดสินใจครบแล้ว ไม่มีข้อใดต้องเดาตัวเลข
  - อัปเดต `docs/02-design/02-technical/index.md` — เพิ่มลิงก์ไปยัง `non-functional-requirement.md`
  - **ไม่ได้แตะ** `architecture.md`, `database-schema.md`, `api-spec.md`, `detailed-design.md`/`detailed-design/*.md` ใดๆ (placement: standalone ตามที่สั่ง ไม่ใช่ inline) — ยังไม่ได้ commit/push
