# Test Cases — teacher-class-overview (หน้าภาพรวมความคืบหน้าของนักเรียนสำหรับครู)

- **วันที่บันทึก:** 2026-08-25
- **ครอบ FE:** FE-57 (สถานะทำ/ไม่ทำรายชุดทั้งห้อง), FE-58 (คะแนนรายคนในหน้าเดียว), FE-59 (เห็นคนไม่เข้าใช้งานนาน), FE-60 (สัญลักษณ์เตือนบนแถว), FE-61 (กดจากแถวไปทำงานต่อ), FE-62 (เลือกวิธีเรียง/กรองเอง)
- **อ้างอิง UJ:** [[../../../01-requirements/user-journey|UJ-02]] (node X — ครูเปิดหน้าภาพรวมนักเรียนทั้งห้องก่อนเตรียมชุดถัดไป), [[../../../01-requirements/user-journey|UJ-03]] (node A0 — ครูเปิดหน้าภาพรวมนักเรียนทั้งห้องก่อนเข้าจัดการแข่งขัน)
- **อ้างอิง Requirement:** [[../../../01-requirements/01-spec/20260821-04-teacher-class-overview|teacher-class-overview]] หัวข้อ ก. (ข้อมูลที่ต้องแสดง), ข. (ข้อมูลที่จงใจไม่แสดง), ค. (การเตือนครูเชิงรุก), ง. (การทำงานต่อจากหน้านี้), จ. (การเรียงลำดับและการกรอง)
- **อ้างอิง AC:** [[../acceptance-criteria|acceptance-criteria]] หมวด H — AC-FE-57-1/2/3, AC-FE-58-1/2/3, AC-FE-59-1/2, AC-FE-60-1/2/3, AC-FE-61-1/2, AC-FE-62-1/2/3
- **Coverage Gap ที่เกี่ยวข้อง:** ไฟล์นี้ถูกสร้างขึ้นเพื่อปิดส่วนหนึ่งของ **G-13** (wikilink ที่เคยชี้ไปยังไฟล์นี้แต่ไม่เคยมีไฟล์จริง) — ยังมี G-13 เหลืออีก 4 ไฟล์ (`student-identity-and-roster`, `teacher-lesson-and-quiz`, `student-learn-and-pet`, `score-export-gradebook`) ที่ยังไม่ถูกสร้างในงานนี้ ไม่อยู่ในขอบเขตของไฟล์นี้ นอกจากนี้ AC-FE-59-1 และ AC-FE-60-1 ยังติด **G-04** (เกณฑ์ "ไม่เข้าใช้งานนาน" และรูปแบบสัญลักษณ์เตือนยังไม่ล็อกใน spec 04) — ทุกเคสที่อ้างถึงเลข 7 วัน หรือป้าย "▲ ควรดูแล" ในไฟล์นี้ใช้ **ค่าสมมติจาก prototype v2** (`MOCK.inactiveDaysThresholdMock` และสัญลักษณ์แบบเดียว) ไม่ใช่ค่าที่ล็อกจริง
- **Prototype ที่ใช้ทดสอบ:** `docs/02-design/01-prototypes/v2-20260821-teacher-classroom-manage/index.html` — หน้าจอ 2 "ภาพรวมชั้นเรียน" (`screen-overview`, หลักของไฟล์นี้), หน้าจอ 3 "จัดการรายชื่อนักเรียน" (`screen-roster`, ใช้ปุ่ม "ปิดการใช้งาน" เพื่อจำลอง state ในบางเคส), หน้าจอ 6 "ตรวจงานและบันทึกคะแนน" (`screen-grading`, ใช้ยืนยัน FE-58/FE-61 ที่ต้องกระโดดข้ามหน้า)
- **Actual Result:** ทุกเคสเว้นว่างช่อง Actual Result ไว้ — ให้บันทึกผลทดสอบจริงที่ [[../../02-test-result/index|docs/03-testing/02-test-result/]] ไม่ใช่ในไฟล์นี้

---

## Test Data ที่ใช้ในไฟล์นี้ (อ้างจาก `MOCK` ใน prototype v2 — เจ้าของเดียวตาม DESIGN.md ข้อ 4.8)

| ค่า | รายละเอียด |
|---|---|
| เกณฑ์ "ไม่เข้าใช้งานนาน" | 7 วัน (`MOCK.inactiveDaysThresholdMock` — **ค่าสมมติ**, ดู Coverage Gap G-04) |
| รูปแบบสัญลักษณ์เตือน | แบบเดียว "▲ ควรดูแล" ใช้ร่วมกันทั้งกรณีไม่เข้าใช้งานนานและยังไม่มีชุดใดทำ/ตรวจเสร็จเลย (**ค่าสมมติ**, ดู Coverage Gap G-04) |
| นักเรียนตัวอย่าง (13 คนทั้งหมด, ปิดใช้งานอยู่แล้ว 1 คน = 4110 → เหลือใช้งานอยู่ 12 คน) | 4101 มะปราง (0 วัน), 4102 บอส (1 วัน), 4103 จิ๋ว (9 วัน, ยังไม่ทำครบทุกชุด), 4104 ฟ้า (2 วัน), 4105 ต้นกล้า (0 วัน), 4106 เต่า (15 วัน, ยังไม่ทำครบทุกชุด), 4107 กล้า (3 วัน), 4108 พลอย (1 วัน), 4109 นุ่น (4 วัน), 4111 เมย์ (6 วัน), 4112 ไอซ์ (2 วัน), 4113 ไม่มีชื่อเล่น (0 วัน) |
| แบบทดสอบเผยแพร่อยู่ 3 ชุด | q1 "แบบทดสอบปรนัย: การคูณเลข" (คะแนนเต็ม 20, ตรวจอัตโนมัติ), q2 "ใบงานคัดลายมือ ก-ฮ" (คะแนนเต็ม 10, ครูตรวจเอง), q3 "แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน" (คะแนนเต็ม 15, ตรวจอัตโนมัติ) |
| นักเรียนที่เข้าเงื่อนไข "ต้องดูแล" ในข้อมูลเริ่มต้น | รหัส 4103 และ 4106 เท่านั้น (2 คนจาก 12 คนที่ใช้งานอยู่) — เข้าเงื่อนไขทั้งสองด้าน (ไม่เข้าใช้งานนาน **และ** ยังไม่มีชุดใดทำ/ตรวจเสร็จเลย) |
| นักเรียนที่มีงานค้างตรวจ (สถานะ "ส่งแล้ว รอครูตรวจ" ในชุดใบงาน) ในข้อมูลเริ่มต้น | รหัส 4106 และ 4109 เท่านั้น |
| คะแนนรวมสูงสุดในกลุ่มนักเรียนที่ใช้งานอยู่ | รหัส 4108 (พลอย) = 19+10+15 = 44 คะแนน |
| ตัวเลือก "เรียงลำดับตาม" (ค่าสมมติ 5 แบบ, ยังไม่ล็อกใน spec 04) | รหัสประจำตัว (น้อย→มาก) / คะแนนรวม (มาก→น้อย) / คะแนนรวม (น้อย→มาก) / เข้าใช้งานล่าสุด (นานสุดก่อน) / คนที่ต้องดูแลไว้บนสุด |
| ตัวเลือก "กรองข้อมูล" (ค่าสมมติ 4 แบบ, ยังไม่ล็อกใน spec 04) | แสดงทั้งหมด (ที่ใช้งานอยู่) / เฉพาะที่ต้องดูแล (มีสัญลักษณ์เตือน) / เฉพาะที่ไม่เข้าใช้งานนาน / เฉพาะที่มีงานค้างตรวจ (ใบงาน/ชิ้นงาน) |

---

## FE-57 — ครูเห็นสถานะทำ/ไม่ทำแบบทดสอบรายชุดของนักเรียนทั้งห้อง

### TC-FE-57-001 — ตารางภาพรวมแสดงสถานะทำ/ไม่ทำครบทุกชุดของทุกคนในหน้าเดียว

**อ้างอิง:** AC-FE-57-1, FE-57, UJ-02 (node X), spec 04 หัวข้อ ก. ข้อ 1
**ประเภท:** Happy path
**Pre-conditions:** ครูล็อกอินสำเร็จแล้ว อยู่ที่หน้า "ภาพรวมชั้นเรียน" มีแบบทดสอบเผยแพร่อยู่ 3 ชุด (q1, q2, q3 ตามตาราง Test Data)
**Test Data:** รหัส 4101 (มะปราง, ทำครบทั้ง 3 ชุดแล้ว) และรหัส 4103 (จิ๋ว, ยังไม่ทำสักชุด)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูหัวตารางของ "ตารางภาพรวม" | หัวตารางแสดงครบ 8 คอลัมน์: "รหัสประจำตัว", "ชื่อเล่น", "แบบทดสอบปรนัย: การคูณเลข", "ใบงานคัดลายมือ ก-ฮ", "แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน", "เข้าใช้งานล่าสุด", "สัญลักษณ์เตือน", "การจัดการ" โดยไม่ต้องเปิดหน้าอื่น |
| 2 | ดูแถวของรหัส 4101 ทั้ง 3 คอลัมน์ชุดแบบทดสอบ | แสดง "ทำแล้ว 18/20", "ตรวจแล้ว 9/10", "ทำแล้ว 14/15" ตามลำดับ |
| 3 | ดูแถวของรหัส 4103 ทั้ง 3 คอลัมน์ชุดแบบทดสอบ | แสดง "ยังไม่ทำ", "ยังไม่ส่ง", "ยังไม่ทำ" ตามลำดับ |
| 4 | นับจำนวนแถวทั้งหมดในตาราง | มีครบ 12 แถว (นักเรียนที่ใช้งานอยู่ทั้งหมด) อยู่ในตารางเดียวโดยไม่ต้องเลื่อนไปหน้าอื่นหรือกดโหลดเพิ่ม |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-57-002 — หน้าภาพรวมไม่แสดงชื่อ-นามสกุลจริงของนักเรียนที่ใดเลย

**อ้างอิง:** AC-FE-57-2, FE-57, UJ-02 (node C/E), spec 04 หัวข้อ ก. ("แสดงตัวนักเรียนด้วยรหัสประจำตัวและชื่อเล่นเท่านั้น")
**ประเภท:** Permission
**Pre-conditions:** ครูอยู่ที่หน้า "จัดการรายชื่อนักเรียน" แล้วกด "อ่านไฟล์ (จำลอง)" ดูตัวอย่างคอลัมน์ที่นำเข้า ซึ่งมีคอลัมน์ "ชื่อ-นามสกุล" อยู่ในไฟล์ต้นทาง (แสดงเป็นค่า "(จะไม่นำเข้าชื่อจริง)" ในพรีวิว)
**Test Data:** ไฟล์รายชื่อตัวอย่างที่มีคอลัมน์ "รหัสนักเรียน" / "ชื่อ-นามสกุล" / "ห้อง"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูตารางพรีวิวไฟล์ที่นำเข้าที่หน้า "จัดการรายชื่อนักเรียน" | คอลัมน์ "ชื่อ-นามสกุล" ของทุกแถวแสดงข้อความ "(จะไม่นำเข้าชื่อจริง)" ไม่ใช่ชื่อจริงจากไฟล์ต้นทาง |
| 2 | กดเมนู "ภาพรวมชั้นเรียน" แล้วเปิด view-source หรือ DevTools > Elements ค้นหาคำว่า "-นามสกุล" หรือชื่อเต็มใดๆ ในโครงสร้าง DOM ทั้งหมดของหน้านี้ | ไม่พบข้อความชื่อ-นามสกุลจริงปรากฏอยู่ในหน้านี้เลย ทุกแถวมีเพียงคอลัมน์ "รหัสประจำตัว" และ "ชื่อเล่น" เท่านั้นสำหรับระบุตัวนักเรียน |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-57-003 — นักเรียนไม่มีทางเข้าหน้าภาพรวมชั้นเรียนของครู

**อ้างอิง:** AC-FE-57-3, FE-57, UJ-01 (ภาพรวมฝั่งนักเรียนทั้งหมด)
**ประเภท:** Permission
**Pre-conditions:** เปิด prototype ฝั่งนักเรียน `docs/02-design/01-prototypes/v1-20260821-student-learn-and-pet/index.html` แล้วล็อกอินด้วยรหัส `3001` / PIN `1234`
**Test Data:** ทุกหน้าจอที่นักเรียนเข้าถึงได้ในไฟล์ v1 (เข้าสู่ระบบ, ทำแบบทดสอบปรนัย, ผลคะแนน, ร้านของแต่งตัว, สัตว์เลี้ยงของฉัน)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ไล่เปิดดูทุกหน้าจอที่นักเรียนเข้าถึงได้ในไฟล์ v1 ทีละหน้า | ไม่มีลิงก์ ปุ่ม หรือเมนูใดที่มีข้อความ "ภาพรวมชั้นเรียน" หรือพาไปยัง `screen-overview` ปรากฏอยู่ในฝั่งนักเรียนแม้แต่จุดเดียว |
| 2 | ลองพิมพ์/แก้ URL หรือ id ของ section ด้วยมือใน DevTools เพื่อพยายามแสดง `screen-overview` (ซึ่งมีอยู่เฉพาะในไฟล์ v2 คนละไฟล์กับ v1) | ไฟล์ v1 ไม่มี element id `screen-overview` อยู่เลยในโครงสร้าง DOM เพราะเป็นคนละไฟล์กับ v2 ยืนยันว่าไม่มีทางเข้าถึงได้จากฝั่งนักเรียน |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-58 — ครูเห็นคะแนนรายคนในหน้าเดียว

### TC-FE-58-001 — คะแนนที่เพิ่งบันทึกจากหน้าตรวจงานไปแสดงในหน้าภาพรวมทันทีโดยไม่ต้องเปิดหน้าอื่นซ้ำ

**อ้างอิง:** AC-FE-58-1, FE-58, FE-61, UJ-02 (node U/W → X), spec 04 หัวข้อ ก. ข้อ 2
**ประเภท:** Happy path
**Pre-conditions:** ครูอยู่ที่หน้า "ภาพรวมชั้นเรียน" รหัส 4109 (นุ่น) มีสถานะชุด "ใบงานคัดลายมือ ก-ฮ" (คะแนนเต็ม 10) เป็น "ส่งแล้ว รอครูตรวจ" ยังไม่มีคะแนน
**Test Data:** กรอกคะแนน 8 คะแนนให้รหัส 4109 ชุด "ใบงานคัดลายมือ ก-ฮ"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูแถวรหัส 4109 คอลัมน์ "ใบงานคัดลายมือ ก-ฮ" ที่หน้าภาพรวม | แสดงป้าย "ส่งแล้ว รอครูตรวจ" ยังไม่มีตัวเลขคะแนน |
| 2 | กดปุ่ม "ตรวจ/บันทึกคะแนน" ที่แถวรหัส 4109 | ระบบพาไปหน้า "ตรวจงานและบันทึกคะแนน" พร้อมข้อความ "กำลังเปิดดูจากหน้าภาพรวมชั้นเรียน: นุ่น — แถวของนักเรียนคนนี้ถูกไฮไลต์ไว้ด้านล่าง" |
| 3 | กรอกคะแนน 8 ในช่องคะแนนของแถวรหัส 4109 กลุ่ม "ใบงานคัดลายมือ ก-ฮ" แล้วกดปุ่ม "บันทึกคะแนน" | Toast แสดงข้อความ "บันทึกคะแนนของ นุ่น ชุด "ใบงานคัดลายมือ ก-ฮ" แล้ว" และแถวเปลี่ยนสถานะเป็น "ตรวจแล้ว" พร้อมช่องคะแนนแสดง 8 |
| 4 | กดเมนู "ภาพรวมชั้นเรียน" กลับไปหน้าเดิม แล้วดูแถวรหัส 4109 คอลัมน์ "ใบงานคัดลายมือ ก-ฮ" อีกครั้ง | แสดงป้าย "ตรวจแล้ว 8/10" ทันที โดยครูไม่ต้องกดปุ่มอื่นเพิ่มเติมนอกจากสลับเมนูกลับมาดู |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-58-002 — หน้าภาพรวมไม่แสดงแต้มสะสมหรือแต้มที่ใช้ได้ของนักเรียนที่ใดเลย

**อ้างอิง:** AC-FE-58-2, FE-58, spec 04 หัวข้อ ข., DESIGN.md ข้อ 4.5
**ประเภท:** Permission
**Pre-conditions:** ครูอยู่ที่หน้า "ภาพรวมชั้นเรียน"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิด DevTools > Elements แล้วค้นหาคำว่า "แต้ม" ในโครงสร้าง DOM ทั้งหมดของหน้านี้ (รวมส่วนสรุปด้านบนและทุกคอลัมน์ของตาราง) | ไม่พบคำว่า "แต้มสะสม" หรือ "แต้มที่ใช้ได้" ปรากฏอยู่ในหน้านี้เลยแม้แต่จุดเดียว |
| 2 | ดูส่วนสรุปด้านบน ("นักเรียนที่ใช้งานอยู่ทั้งหมด", "ต้องดูแล", "ไม่เข้าใช้งานนาน", "มีงานค้างตรวจ") | ไม่มีตัวเลขแต้มสะสมรวมหรือแต้มที่ใช้ได้ของนักเรียนคนใดปรากฏอยู่ในส่วนสรุปนี้เลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-58-003 — หน้าภาพรวมไม่แสดงขั้นการเติบโตหรือค่าสเตตัสของสัตว์เลี้ยงที่ใดเลย

**อ้างอิง:** AC-FE-58-3, FE-58, spec 04 หัวข้อ ข.
**ประเภท:** Permission
**Pre-conditions:** ครูอยู่ที่หน้า "ภาพรวมชั้นเรียน"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิด DevTools > Elements แล้วค้นหาคำว่า "ขั้น", "ค่าสเตตัส", "สัตว์เลี้ยง" ในโครงสร้าง DOM ทั้งหมดของหน้านี้ | ไม่พบคำว่าขั้นการเติบโต ค่าสเตตัส หรือชนิดสัตว์เลี้ยงของนักเรียนคนใดปรากฏอยู่ในหน้านี้เลย |
| 2 | ดูทุกคอลัมน์ของตารางภาพรวมอีกครั้งทีละคอลัมน์ | คอลัมน์ทั้งหมดมีเฉพาะ รหัสประจำตัว, ชื่อเล่น, สถานะ/คะแนนต่อชุด, เข้าใช้งานล่าสุด, สัญลักษณ์เตือน และการจัดการ เท่านั้น ไม่มีข้อมูลสัตว์เลี้ยงปน |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-59 — ครูเห็นนักเรียนที่ไม่เข้าใช้งานระบบนาน

> **หมายเหตุ Coverage Gap G-04:** เกณฑ์ "ไม่เข้าใช้งานนาน" (7 วัน) ในทุกเคสของหมวดนี้เป็นค่าสมมติจาก prototype v2 ยังไม่ได้ล็อกใน spec 04 — ถ้าทีมล็อกเกณฑ์ใหม่ ให้แก้เฉพาะตัวเลขในตาราง Test Data ด้านบนของไฟล์นี้

### TC-FE-59-001 — นักเรียนที่ไม่เข้าใช้งานเกินเกณฑ์ปรากฏในหน้าภาพรวมพร้อมถูกนับในสรุปด้านบน

**อ้างอิง:** AC-FE-59-1, FE-59, UJ-02 (node X), spec 04 หัวข้อ ก. ข้อ 3, Coverage Gap G-04
**ประเภท:** Happy path
**Pre-conditions:** รหัส 4103 (จิ๋ว) ไม่ได้เข้าใช้งานมาแล้ว 9 วัน ซึ่งเกินเกณฑ์สมมติ 7 วัน

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ภาพรวมชั้นเรียน" แล้วดูส่วนสรุปด้านบน | ข้อความ "ไม่เข้าใช้งานนาน: 2 คน" ปรากฏ (นับรวมรหัส 4103 และ 4106) |
| 2 | ดูคอลัมน์ "เข้าใช้งานล่าสุด" ของแถวรหัส 4103 | แสดงข้อความ "9 วันก่อน" ตรงกับจำนวนวันจริง |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-59-002 — นักเรียนที่เข้าใช้งานภายในเกณฑ์ไม่ถูกนับเป็นไม่เข้าใช้งานนาน

**อ้างอิง:** AC-FE-59-2, FE-59, spec 04 หัวข้อ ก. ข้อ 3, Coverage Gap G-04
**ประเภท:** Edge case
**Pre-conditions:** รหัส 4104 (ฟ้า) เข้าใช้งานล่าสุดเมื่อ 2 วันที่ผ่านมา ซึ่งน้อยกว่าเกณฑ์สมมติ 7 วัน

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ภาพรวมชั้นเรียน" แล้วดูคอลัมน์ "เข้าใช้งานล่าสุด" ของแถวรหัส 4104 | แสดงข้อความ "2 วันก่อน" |
| 2 | ดูส่วนสรุป "ไม่เข้าใช้งานนาน" ที่ด้านบนของหน้า | ตัวเลขยังคงเป็น "2 คน" (เฉพาะรหัส 4103 และ 4106) ไม่รวมรหัส 4104 เข้าไปด้วย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-60 — สัญลักษณ์เตือนบนแถวนักเรียนที่ครูควรดูแล

> **หมายเหตุ Coverage Gap G-04:** จำนวนแบบและความหมายของสัญลักษณ์เตือนในทุกเคสของหมวดนี้เป็นค่าสมมติจาก prototype v2 (สัญลักษณ์แบบเดียว "▲ ควรดูแล") ยังไม่ได้ล็อกใน spec 04

### TC-FE-60-001 — แถวนักเรียนที่เข้าเงื่อนไขต้องดูแลมีสัญลักษณ์เตือนปรากฏพร้อมเหตุผล

**อ้างอิง:** AC-FE-60-1, FE-60, spec 04 หัวข้อ ค., Coverage Gap G-04
**ประเภท:** Happy path
**Pre-conditions:** รหัส 4106 (เต่า) ไม่เข้าใช้งานมา 15 วัน (เกินเกณฑ์สมมติ 7 วัน) และทุกชุดยังไม่ทำ/ตรวจเสร็จเลย (q1 ยังไม่ทำ, q2 ส่งแล้วรอครูตรวจ, q3 ยังไม่ทำ)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูคอลัมน์ "สัญลักษณ์เตือน" ของแถวรหัส 4106 ที่หน้าภาพรวม | แสดงป้าย "▲ ควรดูแล" |
| 2 | เอาเมาส์ชี้ค้างที่ป้าย "▲ ควรดูแล" ของแถวรหัส 4106 | ข้อความ tooltip (attribute `title`) แสดงเหตุผล "ไม่เข้าใช้งานมา 15 วัน · ยังไม่มีชุดใดที่ทำ/ตรวจเสร็จเลย" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-60-002 — แถวนักเรียนที่ไม่เข้าเงื่อนไขไม่มีสัญลักษณ์เตือนและไม่ถูกไฮไลต์สีทั้งแถว

**อ้างอิง:** AC-FE-60-2, FE-60, DESIGN.md ข้อ 3.6
**ประเภท:** Negative
**Pre-conditions:** รหัส 4101 (มะปราง) เข้าใช้งานสม่ำเสมอ (เมื่อวันนี้) และทำชุด "แบบทดสอบปรนัย: การคูณเลข" เสร็จแล้ว (ได้ 18/20 คะแนน)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูคอลัมน์ "สัญลักษณ์เตือน" ของแถวรหัส 4101 | แสดงเครื่องหมาย "-" แทนป้ายเตือน |
| 2 | เทียบสีพื้นหลังของทั้งแถวรหัส 4101 กับแถวปกติที่ไม่มีสัญลักษณ์เตือนแถวอื่น | สีพื้นหลังเหมือนกันทุกประการ ไม่มีการไฮไลต์สีทั้งแถว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-60-003 — เมื่อไม่มีนักเรียนคนใดเข้าเงื่อนไขต้องดูแลเลย ไม่มีสัญลักษณ์เตือนแสดงในตารางแม้แต่แถวเดียว

**อ้างอิง:** AC-FE-60-1 (กรณีผกผัน), AC-FE-59-2, FE-60, FE-59, spec 04 หัวข้อ ค.
**ประเภท:** Negative
**Pre-conditions:** ข้อมูลเริ่มต้นมีนักเรียนที่เข้าเงื่อนไขต้องดูแลอยู่ 2 คนเท่านั้นคือรหัส 4103 และ 4106 (ยืนยันจากตาราง Test Data ด้านบน) ครูอยู่ที่หน้า "จัดการรายชื่อนักเรียน" ก่อนเริ่มเคสนี้
**Test Data:** ปิดการใช้งานรหัส 4103 และ 4106 เพื่อจำลองสถานการณ์ที่ไม่มีนักเรียนคนใดในกลุ่มที่ใช้งานอยู่เข้าเงื่อนไขต้องดูแลเลย

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ที่หน้า "จัดการรายชื่อนักเรียน" กดปุ่ม "ปิดการใช้งาน" ที่แถวรหัส 4103 | Toast แสดง "ปิดการใช้งานรหัส 4103 แล้ว ข้อมูลไม่ถูกลบ แต่จะไม่ปรากฏในรายการที่ใช้งานประจำวัน" และสถานะแถวเปลี่ยนเป็น "ปิดใช้งานแล้ว" |
| 2 | กดปุ่ม "ปิดการใช้งาน" ที่แถวรหัส 4106 | สถานะแถวเปลี่ยนเป็น "ปิดใช้งานแล้ว" เช่นกัน |
| 3 | กดเมนู "ภาพรวมชั้นเรียน" | ตารางแสดงนักเรียนที่ใช้งานอยู่เหลือ 10 แถว (ไม่รวมรหัส 4103, 4106, 4110) |
| 4 | ดูส่วนสรุปด้านบนของหน้าภาพรวม | ข้อความ "ต้องดูแล: 0 คน" และ "ไม่เข้าใช้งานนาน: 0 คน" |
| 5 | ดูคอลัมน์ "สัญลักษณ์เตือน" ของทุกแถวที่เหลือทั้ง 10 แถว | ทุกแถวแสดงเครื่องหมาย "-" ไม่มีป้าย "▲ ควรดูแล" ปรากฏที่แถวใดเลยแม้แต่จุดเดียว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-60-004 — ระบบไม่ส่งการแจ้งเตือนออกไปนอกระบบถึงครู สัญลักษณ์ปรากฏเฉพาะเมื่อครูเปิดหน้าเอง

**อ้างอิง:** AC-FE-60-3, FE-60, spec 04 หัวข้อ ค. ("ระบบไม่ส่งการแจ้งเตือนออกไปนอกระบบ")
**ประเภท:** Negative
**Pre-conditions:** ครูล็อกอินสำเร็จแล้วแต่ยังไม่ได้เปิดหน้า "ภาพรวมชั้นเรียน" ด้วยตนเองในเซสชันนี้ (อยู่ที่หน้าอื่น เช่น "ตรวจงานและบันทึกคะแนน") มีรหัส 4106 ที่เข้าเงื่อนไขต้องดูแลอยู่แล้วในระบบ

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิด view-source ของไฟล์ `index.html` แล้วค้นหาคำว่า "Notification", "push", "email" ในซอร์สโค้ดทั้งไฟล์ | ไม่พบการเรียกใช้ Browser Notification API, การจำลองอีเมล หรือกลไก push ใดๆ ในโค้ดทั้งไฟล์ |
| 2 | ใช้งานหน้าอื่นที่ไม่ใช่ "ภาพรวมชั้นเรียน" อยู่สักครู่ สังเกต toast และ popup ทั้งหมดที่ปรากฏบนหน้าจอ | ไม่มี toast, popup, หรือตัวนับสัญลักษณ์เตือนใดปรากฏขึ้นเองนอกหน้า "ภาพรวมชั้นเรียน" แม้จะมีนักเรียนที่เข้าเงื่อนไขต้องดูแลอยู่แล้ว |
| 3 | กดเมนู "ภาพรวมชั้นเรียน" เพื่อเปิดดูเอง | สัญลักษณ์ "▲ ควรดูแล" ที่แถวรหัส 4106 ปรากฏขึ้นทันทีที่หน้านี้ถูกเปิด ยืนยันว่าสัญลักษณ์แสดงเฉพาะเมื่อครูเปิดดูหน้านี้เองเท่านั้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-61 — ครูกดจากแถวนักเรียนไปทำงานต่อได้ทันที

### TC-FE-61-001 — กดปุ่ม "ตรวจ/บันทึกคะแนน" จากแถวแล้วไปหน้าตรวจงานพร้อมไฮไลต์แถวนักเรียนคนนั้น

**อ้างอิง:** AC-FE-61-1, FE-61, UJ-02 (node U/W → X), spec 04 หัวข้อ ง.
**ประเภท:** Happy path
**Pre-conditions:** ครูอยู่ที่หน้า "ภาพรวมชั้นเรียน" เห็นแถวรหัส 4106 (เต่า) ที่มีสถานะชุด "ใบงานคัดลายมือ ก-ฮ" เป็น "ส่งแล้ว รอครูตรวจ" (มีงานค้างตรวจ)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "ตรวจ/บันทึกคะแนน" ที่แถวรหัส 4106 | ระบบพาไปยังหน้า "ตรวจงานและบันทึกคะแนน" ทันที |
| 2 | ดูแถบข้อความด้านบนของหน้า "ตรวจงานและบันทึกคะแนน" | แสดงข้อความ "กำลังเปิดดูจากหน้าภาพรวมชั้นเรียน: เต่า — แถวของนักเรียนคนนี้ถูกไฮไลต์ไว้ด้านล่าง" |
| 3 | เลื่อนไปดูแถวของรหัส 4106 ในตารางกลุ่ม "ใบงานคัดลายมือ ก-ฮ" | แถวของรหัส 4106 มีพื้นหลังไฮไลต์ (class `row-highlighted`) แตกต่างจากแถวอื่นอย่างชัดเจน |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-61-002 — ปุ่มทางลัดจากแถวมีครบอย่างน้อย 2 อย่างตามที่ spec กำหนด และแต่ละปุ่มทำงานได้จริง

**อ้างอิง:** AC-FE-61-2, FE-61, spec 04 หัวข้อ ง. ("อย่างน้อยคือ ตรวจงาน บันทึกคะแนน และรีเซ็ต PIN")
**ประเภท:** Edge case
**Pre-conditions:** ครูอยู่ที่หน้า "ภาพรวมชั้นเรียน" ดูแถวของรหัส 4102 (บอส)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูคอลัมน์ "การจัดการ" ของแถวรหัส 4102 | พบปุ่ม 2 ปุ่มในแถวเดียวกันคือ "ตรวจ/บันทึกคะแนน" และ "รีเซ็ต PIN" |
| 2 | กดปุ่ม "รีเซ็ต PIN" ที่แถวรหัส 4102 | หน้าต่าง modal "รีเซ็ต PIN สำเร็จ" เปิดขึ้น แสดงข้อความ "บอส (รหัส 4102) มี PIN ใหม่คือ [เลข 4 หลัก] — แจ้งให้นักเรียนคนนี้ทราบ" |
| 3 | กดปุ่ม "รับทราบ" ในหน้าต่าง modal | modal ปิดลง และ toast แสดงข้อความ "รีเซ็ต PIN ของ บอส สำเร็จ" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-62 — ครูเลือกวิธีเรียงลำดับและกรองข้อมูลเอง

> **ค่าสมมติ:** ตัวเลือกเรียงลำดับ 5 แบบและตัวกรอง 4 แบบในทุกเคสของหมวดนี้มาจาก prototype v2 (`<select id="overview-sort">` และ `<select id="overview-filter">`) spec 04 ยังไม่ล็อกรายการตัวเลือกที่ต้องมีจริงทั้งหมด (Open Question)

### TC-FE-62-001 — ครูเปลี่ยนวิธีเรียงลำดับเป็น "คะแนนรวม (มาก→น้อย)" แล้วตารางเรียงใหม่ทันที

**อ้างอิง:** AC-FE-62-1, FE-62, spec 04 หัวข้อ จ.
**ประเภท:** Happy path
**Pre-conditions:** ครูอยู่ที่หน้า "ภาพรวมชั้นเรียน" ตัวเลือก "เรียงลำดับตาม" เป็นค่าเริ่มต้น "รหัสประจำตัว (น้อย→มาก)"
**Test Data:** คะแนนรวมของรหัส 4108 (พลอย) = 19+10+15 = 44 คะแนน เป็นคะแนนรวมสูงสุดในกลุ่มนักเรียนที่ใช้งานอยู่ (ดูตาราง Test Data ด้านบน)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปลี่ยน "เรียงลำดับตาม" เป็น "คะแนนรวม (มาก→น้อย)" | ตารางเรียงลำดับใหม่ทันทีโดยไม่ต้องกดปุ่มเพิ่มเติมใดๆ |
| 2 | ดูแถวแรกของตารางหลังเรียงลำดับ | แถวแรกคือรหัส 4108 (คะแนนรวมสูงสุด 44 คะแนน) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-62-002 — ครูเลือกตัวกรอง "เฉพาะที่ต้องดูแล" แล้วตารางแสดงเฉพาะแถวที่ตรงเงื่อนไข

**อ้างอิง:** AC-FE-62-2, FE-62, spec 04 หัวข้อ จ.
**ประเภท:** Happy path
**Pre-conditions:** ข้อมูลเริ่มต้นมีนักเรียนที่มีสัญลักษณ์เตือน 2 คน (รหัส 4103, 4106) จากทั้งหมด 12 คนที่ใช้งานอยู่

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปลี่ยน "กรองข้อมูล" เป็น "เฉพาะที่ต้องดูแล (มีสัญลักษณ์เตือน)" | ตารางแสดงเหลือ 2 แถวทันที คือรหัส 4103 และ 4106 เท่านั้น จำนวนแถวลดลงตามจำนวนคนที่ต้องดูแลจริง |
| 2 | ดูคอลัมน์ "สัญลักษณ์เตือน" ของทั้ง 2 แถวที่เหลือ | ทั้งสองแถวแสดงป้าย "▲ ควรดูแล" ครบทุกแถว ไม่มีแถวอื่นปนอยู่ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-62-003 — ตัวกรองที่ไม่มีนักเรียนตรงเงื่อนไขแสดงตารางว่างแบบเป็นกลาง ไม่ตำหนิครู

**อ้างอิง:** AC-FE-62-3, FE-62, spec 04 หัวข้อ จ., DESIGN.md ข้อ 4.1
**ประเภท:** Edge case
**Pre-conditions:** ครูอยู่ที่หน้า "จัดการรายชื่อนักเรียน" ก่อนเริ่มเคสนี้ ข้อมูลเริ่มต้นมีนักเรียนที่มีงานค้างตรวจ (สถานะ "ส่งแล้ว รอครูตรวจ" ในชุดใบงาน) เพียง 2 คนคือรหัส 4106 และ 4109
**Test Data:** ปิดการใช้งานรหัส 4106 และ 4109 เพื่อจำลองว่าไม่มีนักเรียนคนใดมีงานค้างตรวจในสัปดาห์นี้

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ที่หน้า "จัดการรายชื่อนักเรียน" กดปุ่ม "ปิดการใช้งาน" ที่แถวรหัส 4106 และรหัส 4109 | ทั้งสองแถวมีสถานะเปลี่ยนเป็น "ปิดใช้งานแล้ว" |
| 2 | กดเมนู "ภาพรวมชั้นเรียน" แล้วเปลี่ยน "กรองข้อมูล" เป็น "เฉพาะที่มีงานค้างตรวจ (ใบงาน/ชิ้นงาน)" | ตารางแสดงพื้นที่ว่าง ไม่มีแถวข้อมูลใดปรากฏเลย (0 แถว) และไม่มีข้อความตำหนิหรือถือเป็นความผิดของครู |
| 3 | ดูข้อความที่ปรากฏในพื้นที่ตารางว่าง | คาดหวังข้อความเป็นกลางตาม AC-FE-62-3 เช่น "ไม่มีนักเรียนที่ตรงเงื่อนไขนี้ในขณะนี้" — **หมายเหตุ:** ตรวจสอบซอร์สโค้ด `index.html` แล้ว (2026-08-25) พบว่า prototype v2 ปัจจุบันยังไม่ได้ render ข้อความนี้จริง เมื่อจำนวนแถวเป็น 0 ตัว `tbody` จะว่างเปล่าโดยไม่มีข้อความใดๆ ปรากฏ — ให้บันทึกผลข้อนี้ตามที่เห็นจริงที่ `docs/03-testing/02-test-result/` (คาดว่าจะไม่ผ่านตามที่ AC ระบุ จนกว่า prototype จะเพิ่ม empty-state message) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## สรุปสถานะ prototype

| จำนวน test case | รันบน prototype v2 ได้ครบทุกขั้นตอนวันนี้ | รันได้บางส่วน (มีขั้นตอนที่ implementation ยังไม่ตรงกับ AC) |
|---|---|---|
| 17 | 16 (94%) | 1 (TC-FE-62-003 ขั้นตอนที่ 3 — prototype ยังไม่ render ข้อความ empty-state ตาม AC-FE-62-3) |

ทุกเคสในไฟล์นี้ใช้หน้าจอ ปุ่ม และคอลัมน์ที่มีอยู่จริงใน `v2-20260821-teacher-classroom-manage/index.html` (หน้า "ภาพรวมชั้นเรียน", "จัดการรายชื่อนักเรียน", "ตรวจงานและบันทึกคะแนน") ยกเว้น TC-FE-57-003 ที่ต้องเปิด prototype ฝั่งนักเรียน (`v1-20260821-student-learn-and-pet/index.html`) ร่วมด้วยเพื่อพิสูจน์ว่าไม่มีทางเข้าถึงหน้าครูจากฝั่งนักเรียน — ตัวเลข 7 วัน และป้าย "▲ ควรดูแล" ที่ใช้ในหมวด FE-59/FE-60 ยังคงเป็นค่าสมมติตาม Coverage Gap G-04 จนกว่า spec 04 จะล็อกค่าจริง
