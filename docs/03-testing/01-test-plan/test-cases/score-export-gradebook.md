# Test Cases — score-export-gradebook (การส่งออกคะแนนและสมุดคะแนนของครู)

- **วันที่บันทึก:** 2026-08-25
- **ครอบ FE:** FE-34 (ส่งออกคะแนนเข้าสมุดคะแนนครู), FE-63 (ครูเลือกข้อมูลที่ส่งออกเอง), FE-64 (ส่งออกคะแนนแยกรายชุด), FE-65 (ส่งออกคะแนนรวม), FE-66 (เขียนกลับ Google Sheets ผ่านลิงก์), FE-67 (ดาวน์โหลดเป็นไฟล์), FE-68 (เลือกคอลัมน์ปลายทาง), FE-69 (จับคู่ข้อมูลด้วยรหัสเท่านั้น), FE-70 (ข้ามแถวรหัสไม่ตรง + รายงาน), FE-71 (ครูกดส่งเองเท่านั้น)
- **อ้างอิง UJ:** [[../../../01-requirements/user-journey|UJ-04]] (node A/B/C/D/E/F/G/H/I/J/K/L/M/N — ครูเข้าหน้าส่งออก → เลือกข้อมูล → เลือกช่องทาง → เลือกคอลัมน์ → จับคู่รหัส → ข้ามแถวไม่ตรง → ตรวจรายงาน → กดส่งเอง)
- **อ้างอิง Requirement:** [[../../../01-requirements/01-spec/20260821-05-score-export-and-gradebook|score-export-and-gradebook]]
- **อ้างอิง AC:** [[../acceptance-criteria|acceptance-criteria]] หมวด I — AC-FE-34-1/2, AC-FE-63-1/2/3, AC-FE-64-1/2, AC-FE-65-1/2, AC-FE-66-1/2, AC-FE-67-1/2, AC-FE-68-1/2, AC-FE-69-1/2, AC-FE-70-1/2, AC-FE-71-1/2/3 (ครบทั้ง 22 AC ของหมวด I)
- **Prototype ที่ใช้ทดสอบ:** `docs/02-design/01-prototypes/v2-20260821-teacher-classroom-manage/index.html` หน้า 8 "ส่งออกคะแนน" (`screen-export`) เป็นหลัก — TC-FE-34-002 อ้างอิง `docs/02-design/01-prototypes/v1-20260821-student-learn-and-pet/index.html` (ฝั่งนักเรียน) เพิ่มเติมเพื่อพิสูจน์ว่าไม่มีทางเข้าถึงหน้าส่งออกคะแนนของครู
- **ปิด Coverage Gap G-13 (บางส่วน):** ไฟล์นี้เป็น 1 ใน 5 ไฟล์ test-case ที่ acceptance-criteria.md เคยอ้างด้วย wikilink ไว้ล่วงหน้าแต่ยังไม่เคยถูกสร้างจริง — สร้างขึ้นครั้งแรกในรอบนี้
- **สิ่งที่ตั้งใจไม่เขียน (ตาม G-05):** ไม่มี test case สำหรับ (1) พฤติกรรมส่งคะแนนซ้ำรอบสอง (เขียนทับ/เพิ่มคอลัมน์/ถามครูก่อน) เพราะ UJ-04 ยังไม่ระบุ กระทบ AC-FE-71-* ในอนาคต (2) ขอบเขตสิทธิ์ (scope) ที่ระบบขอจาก Google ตอนเขียนกลับเข้าชีท และกรณีลิงก์ผิดรูปแบบ/ไม่มีสิทธิ์เข้าถึงชีท เพราะ UJ-04 ยังไม่ระบุ กระทบ AC-FE-66-* (3) พฤติกรรมเมื่อครูเปลี่ยนโครงสร้างชีทหลังตั้งค่าคอลัมน์ปลายทางไว้แล้ว กระทบ AC-FE-68-* — ทั้งสามข้อบันทึกไว้ที่ Coverage Gap G-05 ใน `acceptance-criteria.md` แล้ว ไม่ใช่ช่องว่างใหม่ที่พบในไฟล์นี้
- **Actual Result:** ทุกเคสเว้นว่างช่อง Actual Result ไว้ — ให้บันทึกผลทดสอบจริงที่ [[../../02-test-result/index|docs/03-testing/02-test-result/]] ไม่ใช่ในไฟล์นี้

---

## Test Data ที่ใช้ในไฟล์นี้ (อ้างจาก `MOCK` ใน prototype v2 — เจ้าของเดียวตาม DESIGN.md ข้อ 4.8)

| ค่า | รายละเอียด |
|---|---|
| นักเรียนตัวอย่างที่มีคะแนนครบ 3 ชุด | รหัส `4101` — q1 (แบบทดสอบปรนัย: การคูณเลข) = 18, q2 (ใบงานคัดลายมือ ก-ฮ) = 9, q3 (แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน) = 14 |
| จำนวนนักเรียนทั้งหมดในระบบ | 13 คน (รหัส 4101–4113) — **ใช้งานอยู่ (active) 12 คน** เพราะรหัส `4110` ถูกปิดการใช้งานแล้ว (`active: false`) |
| ชุดคะแนน 3 ชุด | q1 "แบบทดสอบปรนัย: การคูณเลข" (เต็ม 20), q2 "ใบงานคัดลายมือ ก-ฮ" (เต็ม 10, ครูตรวจเอง), q3 "แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน" (เต็ม 15) |
| รหัสในชีทปลายทางที่ไม่ตรงกับระบบ (ค่าสมมติ ดูหมายเหตุ FE-70 ใน acceptance-criteria.md) | `5001`, `5002` — เหตุผลที่แสดง: "ไม่พบรหัสนี้ในระบบ (FE-69)" |
| รูปแบบไฟล์ดาวน์โหลด (ค่าสมมติ, spec 05 ยังไม่ล็อก) | "CSV (.csv)" / "Excel (.xlsx)" |
| รายชื่อคอลัมน์ปลายทาง (ค่าสมมติ, spec 05 ยังไม่ล็อก) | คอลัมน์ A ถึง F |
| ลิงก์ Google Sheets ปลายทางตัวอย่าง | `https://docs.google.com/spreadsheets/d/mockid/edit` |

---

## FE-34 — ส่งออกคะแนนเข้าสมุดคะแนนของครู

### TC-FE-34-001 — ครูเข้าหน้า "ส่งออกคะแนน" จากเมนูหลักได้สำเร็จ พร้อมเห็นครบ 5 ขั้นตอน

**อ้างอิง:** AC-FE-34-1, FE-34, UJ-04 (node A), spec 05
**ประเภท:** Happy path
**Pre-conditions:** ครูล็อกอินด้วยบัญชี Google สำเร็จแล้วที่ prototype v2 (หน้า 1 กด "เข้าสู่ระบบด้วยบัญชี Google") อยู่ที่หน้าใดก็ได้ในเมนูหลักของครู
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูเมนูหลักของครู (แถบทึบใต้แถบรีวิว) | เห็นปุ่มเมนู "ส่งออกคะแนน" อยู่ในรายการเมนูหลัก |
| 2 | กดปุ่มเมนู "ส่งออกคะแนน" | ระบบพาไปยังหน้า "ส่งออกคะแนน" (`screen-export`) |
| 3 | ดูเนื้อหาของหน้าจอ | เห็นหัวข้อครบ 5 ขั้นตอนเรียงตามลำดับ: "1. เลือกข้อมูลที่จะส่งออก", "2. เลือกช่องทางส่งออก", "3. เลือกคอลัมน์ปลายทาง", "4. ตรวจสอบข้อมูลก่อนส่ง", "5. ยืนยันส่ง" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-34-002 — นักเรียนไม่มีทางเข้าหน้าส่งออกคะแนนของครูจาก prototype ฝั่งนักเรียนเลย

**อ้างอิง:** AC-FE-34-2, FE-34, UJ-04, spec 05, DESIGN.md ข้อ 4.5
**ประเภท:** Permission
**Pre-conditions:** นักเรียนล็อกอินด้วยรหัส `3001` / PIN `1234` สำเร็จที่ prototype v1 (`v1-20260821-student-learn-and-pet/index.html`)
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดดูทุกหน้าจอทั้ง 8 หน้าที่นักเรียนเข้าถึงได้ (บทเรียนของฉัน, ทำแบบทดสอบปรนัย, ผลคะแนน, สัตว์เลี้ยงของฉัน, ร้านของแต่งตัว, เปลี่ยนชนิดสัตว์เลี้ยง, อารีน่า) | ไม่พบปุ่มหรือลิงก์ที่มีข้อความ "ส่งออกคะแนน" หรือ "สมุดคะแนน" ปรากฏอยู่ในหน้าจอใดเลยแม้แต่จุดเดียว |
| 2 | ตรวจสอบ DOM (view-source / inspector) ของไฟล์ v1 prototype ทั้งไฟล์ | ไม่พบ element ที่มี `id` หรือ `data-target` ชี้ไปยัง `screen-export` หรือฟังก์ชันที่เกี่ยวข้องกับการส่งออกคะแนนของครูเลยแม้แต่จุดเดียว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-63 — ครูเลือกข้อมูลที่จะส่งออกเอง

### TC-FE-63-001 — ครูเลือกส่งออกเฉพาะ 2 จาก 3 ชุดเมื่อเลือก "คะแนนแยกรายชุด"

**อ้างอิง:** AC-FE-63-1, FE-63, UJ-04 (node B), spec 05
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ขั้น "1. เลือกข้อมูลที่จะส่งออก" ตัวเลือก "คะแนนแยกรายชุด" ถูกเลือกอยู่แล้ว (ค่าเริ่มต้น) มีชุดคะแนนให้เลือก 3 ชุด: "แบบทดสอบปรนัย: การคูณเลข" (q1), "ใบงานคัดลายมือ ก-ฮ" (q2), "แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน" (q3)
**Test Data:** ติ๊กเลือกเฉพาะ q1 และ q3 ไม่ติ๊ก q2

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ติ๊กเลือกช่องทำเครื่องหมายหน้า "แบบทดสอบปรนัย: การคูณเลข" และ "แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน" | ทั้งสองช่องแสดงเครื่องหมายถูก ช่องหน้า "ใบงานคัดลายมือ ก-ฮ" ยังไม่ถูกติ๊ก |
| 2 | เลื่อนไปดูขั้น "3. เลือกคอลัมน์ปลายทาง" | เห็นแถวคอลัมน์ปลายทางของ "รหัสประจำตัวนักเรียน (ใช้จับคู่แถว)", "คะแนนชุด: แบบทดสอบปรนัย: การคูณเลข" และ "คะแนนชุด: แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน" เท่านั้น ไม่มีแถวของ "ใบงานคัดลายมือ ก-ฮ" ปรากฏ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-63-002 — ตัวเลือกข้อมูลที่ส่งออกได้มีเพียง "คะแนนแยกรายชุด" กับ "คะแนนรวม" ไม่มีแต้มหรือขั้นการเติบโตให้เลือก

**อ้างอิง:** AC-FE-63-2, FE-63, spec 05 ข้อ ก., DESIGN.md ข้อ 4.5
**ประเภท:** Permission
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ขั้น "1. เลือกข้อมูลที่จะส่งออก"
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูตัวเลือกทั้งหมดในขั้น "1. เลือกข้อมูลที่จะส่งออก" | เห็นตัวเลือกแบบ radio เพียง 2 รายการคือ "คะแนนแยกรายชุด" และ "คะแนนรวม" เท่านั้น |
| 2 | ตรวจสอบ DOM ของส่วนนี้ทั้งหมด | ไม่มีคำว่า "แต้ม", "ขั้น", "สเตตัส" หรือชื่อสายพันธุ์สัตว์เลี้ยงปรากฏอยู่ใน element ใดของขั้นนี้เลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-63-003 — ไม่สามารถตรวจสอบข้อมูลก่อนส่งได้เมื่อยังไม่เลือกชุดใดเลย (ไม่มีข้อมูลให้ส่งออก)

**อ้างอิง:** AC-FE-63-3, FE-63, spec 05
**ประเภท:** Negative
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" เลือก "คะแนนแยกรายชุด" แล้ว
**Test Data:** ยกเลิกติ๊กเลือกทุกชุด จนไม่มีชุดใดถูกเลือกเลย

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ยกเลิกการติ๊กช่องทำเครื่องหมายของทุกชุด จนไม่มีชุดใดถูกเลือกเหลืออยู่ | ไม่มีช่องใดแสดงเครื่องหมายถูกเลย |
| 2 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" | ระบบแสดงข้อความแจ้งเตือน "กรุณาเลือกชุดแบบทดสอบที่จะส่งออกอย่างน้อย 1 ชุด" |
| 3 | สังเกตส่วนผลตรวจสอบและปุ่ม "ยืนยันส่งคะแนน" | ส่วนผลตรวจสอบ (`export-preview-result`) ยังคงซ่อนอยู่ และปุ่ม "ยืนยันส่งคะแนน" ยังอยู่ในสถานะปิดใช้งาน |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-64 — ระบบส่งออกคะแนนแยกรายชุด

### TC-FE-64-001 — ส่งออกคะแนนแยกรายชุดครบตามชุดที่เลือก (q1, q3) ไม่มีคะแนน q2 ที่ไม่ได้เลือกปนอยู่

**อ้างอิง:** AC-FE-64-1, FE-64, UJ-04 (node C), spec 05
**ประเภท:** Happy path
**Pre-conditions:** เลือก "คะแนนแยกรายชุด" และติ๊กเลือกเฉพาะชุด q1 และ q3 (ต่อจาก TC-FE-63-001) เลือกช่องทาง "ดาวน์โหลดเป็นไฟล์" รูปแบบ "CSV (.csv)"
**Test Data:** q1 = "แบบทดสอบปรนัย: การคูณเลข", q3 = "แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูรายการคอลัมน์ปลายทางที่ขั้น "3. เลือกคอลัมน์ปลายทาง" | เห็นเพียง 3 แถว: รหัสประจำตัวนักเรียน, คะแนนชุด q1, คะแนนชุด q3 — ไม่มีแถวคะแนนชุด "ใบงานคัดลายมือ ก-ฮ" (q2) ปรากฏเลย |
| 2 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" แล้วกดปุ่ม "ยืนยันส่งคะแนน" | ระบบแสดงข้อความ "สร้างไฟล์คะแนนให้ดาวน์โหลดสำเร็จ (จำลอง — CSV (.csv))" และไม่มีคอลัมน์คะแนนของ q2 อยู่ในรายการคอลัมน์ปลายทางที่ส่งออกเลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-64-002 — ข้อมูลที่ส่งออกไม่มีชื่อ-นามสกุลจริงของนักเรียนปรากฏอยู่เลย

**อ้างอิง:** AC-FE-64-2, FE-64, spec 05, DESIGN.md ข้อ 4.5
**ประเภท:** Permission
**Pre-conditions:** ส่งออกคะแนนแยกรายชุดสำเร็จแล้ว (ต่อจาก TC-FE-64-001)
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูรายการฟิลด์คอลัมน์ปลายทางทั้งหมดที่ขั้น "3. เลือกคอลัมน์ปลายทาง" | ฟิลด์แรกสุดคือ "รหัสประจำตัวนักเรียน (ใช้จับคู่แถว)" เท่านั้น ไม่มีฟิลด์ชื่อ "ชื่อ-นามสกุล" หรือ "ชื่อจริง" ปรากฏเลยสักฟิลด์ |
| 2 | ตรวจสอบ DOM ทั้งหมดของหน้า "ส่งออกคะแนน" | ไม่พบข้อความชื่อ-นามสกุลจริงของนักเรียนคนใดปรากฏอยู่ในหน้าจอนี้เลย มีเพียงรหัสประจำตัว (เช่น `4101`) และชื่อเล่นที่ใช้อ้างถึงนักเรียนเท่านั้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-65 — ระบบส่งออกคะแนนรวม

### TC-FE-65-001 — ส่งออกคะแนนรวมของนักเรียนแต่ละคนเป็นยอดเดียว (18+9+14 = 41)

**อ้างอิง:** AC-FE-65-1, FE-65, UJ-04 (node D), spec 05
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" นักเรียนรหัส `4101` มีคะแนนจาก 3 ชุดคือ q1=18, q2=9, q3=14 คะแนน
**Test Data:** เลือก "คะแนนรวม" ช่องทาง "ดาวน์โหลดเป็นไฟล์"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เลือกตัวเลือก "คะแนนรวม" ที่ขั้น "1. เลือกข้อมูลที่จะส่งออก" | ช่องทำเครื่องหมายรายชุด (q1/q2/q3) หายไป เหลือเพียงตัวเลือก radio 2 แบบเดิม |
| 2 | ดูรายการคอลัมน์ปลายทางที่ขั้น "3. เลือกคอลัมน์ปลายทาง" | เห็นฟิลด์ "คะแนนรวมทั้งหมด" แทนที่ฟิลด์รายชุดทั้งหมด |
| 3 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" แล้วกดปุ่ม "ยืนยันส่งคะแนน" | ระบบส่งออกสำเร็จ และข้อมูลของนักเรียนรหัส `4101` แสดงยอดรวมเดียวเท่ากับ 41 คะแนน (18+9+14) ไม่แยกเป็นรายชุด |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-65-002 — เลือก "คะแนนแยกรายชุด" และ "คะแนนรวม" พร้อมกันในการส่งออกครั้งเดียวไม่ได้

**อ้างอิง:** AC-FE-65-2, FE-65, spec 05
**ประเภท:** Edge case
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ขั้น "1. เลือกข้อมูลที่จะส่งออก"
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูชนิดของ input ของตัวเลือก "คะแนนแยกรายชุด" และ "คะแนนรวม" | ทั้งสองเป็น `<input type="radio" name="export-scope">` ในกลุ่มชื่อเดียวกัน |
| 2 | กดเลือก "คะแนนรวม" | ตัวเลือก "คะแนนแยกรายชุด" ถูกยกเลิกการเลือกโดยอัตโนมัติ เหลือเพียง "คะแนนรวม" ที่ถูกเลือกอยู่รายการเดียว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-66 — เขียนคะแนนกลับเข้า Google Sheets ผ่านลิงก์

### TC-FE-66-001 — ครูส่งคะแนนกลับเข้า Google Sheets ผ่านลิงก์สำเร็จโดยไม่ต้องดาวน์โหลดไฟล์ก่อน

**อ้างอิง:** AC-FE-66-1, FE-66, UJ-04 (node E/F/M), spec 05
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" เลือกช่องทาง "เขียนกลับเข้า Google Sheets ผ่านลิงก์" (ค่าเริ่มต้น)
**Test Data:** ลิงก์ `https://docs.google.com/spreadsheets/d/mockid/edit`

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | วางลิงก์ Google Sheets ปลายทางในช่อง "ลิงก์ Google Sheets ปลายทาง" | ช่องกรอกแสดงลิงก์ที่วางไว้ครบถ้วน |
| 2 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" แล้วกดปุ่ม "ยืนยันส่งคะแนน" | ระบบแสดงข้อความแจ้งเตือน (toast) "ส่งคะแนนเข้า Google Sheets สำเร็จ (จำลอง)" และการ์ดผลลัพธ์แสดงข้อความ "ส่งคะแนนสำเร็จ" พร้อม "ส่งล่าสุดเมื่อ ..." โดยตลอดขั้นตอนนี้ไม่มีปุ่มหรือขั้นตอนดาวน์โหลดไฟล์ปรากฏขึ้นเลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-66-002 — ไม่สามารถตรวจสอบข้อมูลก่อนส่งได้เมื่อยังไม่วางลิงก์ Google Sheets

**อ้างอิง:** AC-FE-66-2, FE-66, spec 05
**ประเภท:** Negative
**หมายเหตุ:** ครอบเฉพาะกรณีช่องลิงก์ว่างเปล่าตามที่ AC-FE-66-2 ระบุ — ไม่ครอบกรณี "ลิงก์ที่กรอกมีรูปแบบผิด" หรือ "ครูไม่มีสิทธิ์เข้าถึงชีทปลายทาง" เพราะ UJ-04 ยังไม่ระบุขอบเขตสิทธิ์ Google ที่ระบบขอ (ดู Coverage Gap G-05 ข้อ (2) ใน acceptance-criteria.md — ไม่แต่งเงื่อนไขเพิ่มเอง)
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" เลือกช่องทาง "เขียนกลับเข้า Google Sheets ผ่านลิงก์" แต่ยังไม่ได้กรอกอะไรในช่อง "ลิงก์ Google Sheets ปลายทาง" เลย (ค่าว่างเปล่า)
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ตรวจสอบว่าช่องกรอกลิงก์ว่างเปล่า | ช่อง "ลิงก์ Google Sheets ปลายทาง" ไม่มีข้อความใดๆ อยู่เลย |
| 2 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" | ระบบแสดงข้อความแจ้งเตือน "กรุณาวางลิงก์ Google Sheets ปลายทางก่อน" และปุ่ม "ยืนยันส่งคะแนน" ยังคงปิดใช้งานเช่นเดิม |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-67 — ดาวน์โหลดคะแนนเป็นไฟล์

### TC-FE-67-001 — ครูดาวน์โหลดคะแนนเป็นไฟล์ CSV สำเร็จ

**อ้างอิง:** AC-FE-67-1, FE-67, UJ-04 (node E/G/N), spec 05
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" เลือกช่องทาง "ดาวน์โหลดเป็นไฟล์"
**Test Data:** รูปแบบไฟล์ "CSV (.csv)"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เลือกช่องทาง "ดาวน์โหลดเป็นไฟล์" | ช่อง select "รูปแบบไฟล์" ปรากฏขึ้นพร้อมตัวเลือก "CSV (.csv)" และ "Excel (.xlsx)" (มีป้ายกำกับค่าสมมติกำกับอยู่) |
| 2 | เลือกรูปแบบไฟล์ "CSV (.csv)" แล้วกดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" จากนั้นกดปุ่ม "ยืนยันส่งคะแนน" | ระบบแสดงข้อความ "สร้างไฟล์คะแนนให้ดาวน์โหลดสำเร็จ (จำลอง — CSV (.csv))" ระบุรูปแบบไฟล์ "CSV (.csv)" ตรงกับที่เลือกไว้ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-67-002 — เลือกช่องทางส่งออกได้ทีละหนึ่งวิธี และไม่ขอสิทธิ์ Google เมื่อเลือกดาวน์โหลด

**อ้างอิง:** AC-FE-67-2, FE-67, spec 05
**ประเภท:** Edge case
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ขั้น "2. เลือกช่องทางส่งออก"
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูชนิดของ input ของตัวเลือกช่องทางทั้งสอง | ทั้ง "เขียนกลับเข้า Google Sheets ผ่านลิงก์" และ "ดาวน์โหลดเป็นไฟล์" เป็น `<input type="radio" name="export-channel">` ในกลุ่มชื่อเดียวกัน เลือกได้ทีละหนึ่ง |
| 2 | เลือก "ดาวน์โหลดเป็นไฟล์" | ส่วนรายละเอียดช่องทางเปลี่ยนเป็นช่อง select "รูปแบบไฟล์" เท่านั้น ไม่มีข้อความหรือปุ่มใดเกี่ยวกับการขอสิทธิ์ (authorize) บัญชี Google ปรากฏขึ้นในหน้าจอเลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-68 — ครูเลือกคอลัมน์ปลายทางของข้อมูลส่งออก

### TC-FE-68-001 — ครูเลือกคอลัมน์ปลายทางของคะแนนชุด q1 เป็นคอลัมน์ C ได้เองโดยไม่ต้องจัดสมุดคะแนนใหม่

**อ้างอิง:** AC-FE-68-1, FE-68, UJ-04 (node H), spec 05
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ขั้น "3. เลือกคอลัมน์ปลายทาง" เห็นตัวเลือกคอลัมน์ A ถึง F ที่แถวคะแนนชุด "แบบทดสอบปรนัย: การคูณเลข" (q1) ซึ่งค่าเริ่มต้นเป็นคอลัมน์ B
**Test Data:** เปลี่ยนคอลัมน์ปลายทางของ q1 จากคอลัมน์ B เป็นคอลัมน์ C

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิด select คอลัมน์ปลายทางของแถว "คะแนนชุด: แบบทดสอบปรนัย: การคูณเลข" แล้วเลือก "คอลัมน์ C" | select แสดงค่าที่เลือกเป็น "คอลัมน์ C" |
| 2 | เลื่อนดูขั้นตอนอื่นทั้งหมดในหน้าเดียวกัน | ไม่มีข้อความหรือ modal ใดบังคับให้ครูจัดเรียงลำดับคอลัมน์อื่นในสมุดคะแนนใหม่ตามลำดับของระบบ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-68-002 — ไม่สามารถเลือกคอลัมน์ปลายทางเดียวกันให้ข้อมูลสองชุดพร้อมกันได้

**อ้างอิง:** AC-FE-68-2, FE-68, spec 05
**ประเภท:** Negative
**หมายเหตุ:** ตรวจสอบโค้ด `renderExportColumnMap()` ใน prototype v2 เมื่อ 2026-08-25 พบว่า **ยังไม่มีการตรวจสอบคอลัมน์ปลายทางซ้ำ** — ระบบปัจจุบันอนุญาตให้เลือกคอลัมน์เดียวกันซ้ำได้โดยไม่มีคำเตือนใดๆ ดังนั้นเคสนี้คาดว่าจะ **ไม่ผ่าน** เมื่อรันบน prototype v2 ฉบับปัจจุบัน — ให้บันทึกเป็นบั๊กที่ `docs/03-testing/02-test-result/` ตามผลจริง ไม่ใช่การเขียน AC หรือ test case ผิด (AC-FE-68-2 คือกฎธุรกิจที่ยืนยันแล้วจาก spec 05)
**Pre-conditions:** เลือก "คะแนนแยกรายชุด" ติ๊ก q1 และ q3 แล้วเลือกคอลัมน์ปลายทางของ q1 เป็นคอลัมน์ C ไว้ก่อนแล้ว (ต่อจาก TC-FE-68-001)
**Test Data:** พยายามเลือกคอลัมน์ C ให้ q3 ด้วย

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิด select คอลัมน์ปลายทางของแถว "คะแนนชุด: แบบทดสอบปรนัย AI: คำศัพท์ภาษาอังกฤษพื้นฐาน" (q3) แล้วเลือก "คอลัมน์ C" | ระบบแสดงข้อความเตือนว่าคอลัมน์นี้ถูกเลือกไปแล้ว |
| 2 | สังเกตค่าที่เลือกของ select ของ q3 และปุ่ม "ยืนยันส่งคะแนน" หลังพยายามเลือกซ้ำ | select ของ q3 ไม่คงค่าเป็น "คอลัมน์ C" (กลับไปเป็นค่าก่อนหน้าหรือว่างให้เลือกใหม่) และไม่สามารถดำเนินการต่อไปยังขั้นตอนถัดไปได้จนกว่าครูจะเลือกคอลัมน์อื่นให้ q3 |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-69 — ระบบจับคู่ข้อมูลด้วยรหัสประจำตัวเท่านั้น

### TC-FE-69-001 — ระบบจับคู่แถวคะแนนกับนักเรียนด้วยรหัสประจำตัวที่ตรงกันเท่านั้น

**อ้างอิง:** AC-FE-69-1, FE-69, UJ-04 (node I), spec 05
**ประเภท:** Happy path
**หมายเหตุ:** AC-FE-69-1 ใช้ตัวเลขตัวอย่างประกอบ Given ว่า "13 คน" แต่ข้อมูลจำลองจริงใน prototype v2 มีนักเรียนที่ใช้งานอยู่ (active) เพียง **12 คน** จากทั้งหมด 13 คน (รหัส `4110` ถูกปิดการใช้งานแล้ว) — test case นี้ยึดค่าที่วัดได้จริงจาก prototype (12 คน) ตามกฎ "ต้องพิสูจน์จริง ไม่ใช่ตรวจสายตา" ของ CLAUDE.md ไม่ใช่ตัวเลขตัวอย่างใน AC
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ผ่านขั้นตอน 1–3 มาแล้ว (เลือกข้อมูล/ช่องทาง/คอลัมน์ปลายทางเรียบร้อย)
**Test Data:** นักเรียนที่ใช้งานอยู่ (active) 12 คนจากทั้งหมด 13 คนในระบบ

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" | ระบบแสดงข้อความ "จะส่งคะแนนของนักเรียน 12 คน · ข้ามแถวที่รหัสไม่ตรง 2 แถว" |
| 2 | ตรวจสอบว่าจำนวนที่รายงานตรงกับจำนวนนักเรียนที่ใช้งานอยู่จริง | จำนวน "12 คน" ตรงกับจำนวนนักเรียนสถานะ active ทั้งหมดในระบบพอดี ไม่มากหรือน้อยกว่านั้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-69-002 — ระบบไม่ใช้ชื่อหรือลำดับแถวในการจับคู่ข้อมูล — รหัสไม่ตรงถูกข้ามเสมอไม่ว่าจะอยู่ตำแหน่งใด

**อ้างอิง:** AC-FE-69-2, FE-69, spec 05
**ประเภท:** Negative
**หมายเหตุ:** prototype v2 จำลองแถวที่รหัสไม่ตรงด้วยรหัสคงที่ "5001"/"5002" ที่ไม่ตรงกับรหัสนักเรียนคนใดในระบบ (ไม่ได้จำลองไฟล์ Sheets จริงที่มีลำดับแถวให้เทียบตำแหน่ง) — test case นี้จึงยืนยันได้เฉพาะว่า "รหัสที่ไม่ตรงกับระบบไม่ถูกจับคู่ไม่ว่าจะอยู่ที่ใด" แต่ไม่สามารถพิสูจน์ประเด็น "ลำดับแถวตรงกันพอดีแต่รหัสไม่ตรง" ได้ตรงๆ ด้วยกลไก mock ปัจจุบัน (เป็นข้อจำกัดของ prototype ไม่ใช่ Coverage Gap ใหม่ ดูหัวข้อสรุปสถานะ prototype ท้ายไฟล์)
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ผ่านขั้นตอน 1–3 มาแล้ว
**Test Data:** รหัส "5001" และ "5002" ในชีทปลายทางจำลอง (ไม่ตรงกับรหัสนักเรียนคนใดในระบบ)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" | ตารางรายงานแถวที่ถูกข้ามแสดงรหัส "5001" และ "5002" พร้อมคอลัมน์เหตุผล "ไม่พบรหัสนี้ในระบบ (FE-69)" ทั้งสองแถว |
| 2 | ตรวจสอบว่ารหัสทั้งสองไม่ถูกนับรวมอยู่ในจำนวนนักเรียนที่ระบบระบุว่าจะได้รับคะแนน | ข้อความ "จะส่งคะแนนของนักเรียน 12 คน" ไม่รวมรหัส "5001"/"5002" เข้าไปด้วยเลย ยืนยันว่าระบบใช้รหัสประจำตัวเป็นเกณฑ์เดียวในการจับคู่ ไม่ใช่ตำแหน่งแถว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-70 — ระบบข้ามแถวที่รหัสไม่ตรงและรายงานให้ครูเห็น

### TC-FE-70-001 — ระบบข้ามแถวที่รหัสไม่ตรงและรายงานให้ครูเห็นก่อนส่ง

**อ้างอิง:** AC-FE-70-1, FE-70, UJ-04 (node J), spec 05
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ผ่านขั้นตอน 1–3 มาแล้ว ชีทปลายทางจำลองมีรหัส "5001" และ "5002" ที่ไม่ตรงกับรหัสใดในระบบ
**Test Data:** รหัสที่ไม่ตรง 2 รายการ: "5001", "5002"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" | ส่วนผลตรวจสอบแสดงข้อความสรุปที่มีวลี "ข้ามแถวที่รหัสไม่ตรง 2 แถว" |
| 2 | ดูตารางรายงานแถวที่ถูกข้าม | ตารางมี 2 แถว ระบุรหัส "5001" และ "5002" คอลัมน์ "สาเหตุที่ถูกข้าม" แสดงข้อความ "ไม่พบรหัสนี้ในระบบ (FE-69)" ทั้งสองแถว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-70-002 — แถวที่รหัสไม่ตรงไม่ถูกเขียนทับข้อมูลใดๆ ในชีทปลายทาง

**อ้างอิง:** AC-FE-70-2, FE-70, spec 05
**ประเภท:** Negative
**Pre-conditions:** รายงานก่อนส่งแสดงว่ามีแถวที่รหัสไม่ตรง 2 แถว (ต่อจาก TC-FE-70-001)
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | อ่านข้อความหมายเหตุใต้ตารางรายงานแถวที่ถูกข้าม | เห็นข้อความ "ระบบจะไม่เขียนทับแถวที่รหัสไม่ตรงเหล่านี้ — ครูตรวจสอบและแก้ไขเองในชีทของตนได้ตามสะดวก" |
| 2 | กดปุ่ม "ยืนยันส่งคะแนน" | ระบบแสดงข้อความส่งสำเร็จ โดยไม่มีข้อความหรือสัญญาณใดระบุว่ามีการเขียนทับหรือเปลี่ยนแปลงข้อมูลของรหัส "5001" หรือ "5002" เลย (จำนวนแถวที่ถูกข้ามยังคงเป็น 2 แถวเท่าเดิมตามรายงานก่อนหน้า ไม่มีข้อความใดระบุว่าแถวเหล่านี้ถูกเขียนใหม่) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-71 — ครูกดส่งคะแนนเองเท่านั้น

### TC-FE-71-001 — ครูเห็นข้อมูลที่จะส่งก่อนที่ปุ่มยืนยันส่งจะกดได้

**อ้างอิง:** AC-FE-71-1, FE-71, UJ-04 (node K/L), spec 05
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "ส่งออกคะแนน" ผ่านขั้นตอน 1–3 มาแล้ว ยังไม่เคยกดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" เลยตั้งแต่เข้าหน้านี้
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | สังเกตปุ่ม "ยืนยันส่งคะแนน" ก่อนกดตรวจสอบ | ปุ่มอยู่ในสถานะปิดใช้งาน (มี attribute `disabled`) |
| 2 | กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" | ส่วนผลตรวจสอบแสดงข้อความสรุปจำนวนนักเรียนที่จะได้รับคะแนนและจำนวนแถวที่ถูกข้าม เช่น "จะส่งคะแนนของนักเรียน 12 คน · ข้ามแถวที่รหัสไม่ตรง 2 แถว" |
| 3 | สังเกตปุ่ม "ยืนยันส่งคะแนน" ทันทีหลังผลตรวจสอบปรากฏ | ปุ่มเปลี่ยนจากปิดใช้งานเป็นกดได้ในจังหวะเดียวกับที่ผลตรวจสอบแสดงขึ้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-71-002 — ระบบไม่เขียนหรือส่งข้อมูลใดๆ จนกว่าครูจะกดยืนยันส่ง

**อ้างอิง:** AC-FE-71-2, FE-71, spec 05
**ประเภท:** Negative
**หมายเหตุ:** ครอบเฉพาะสิ่งที่ AC-FE-71-2 ระบุ คือ "ไม่มีข้อมูลถูกเขียนจนกว่าจะกดยืนยัน" — ไม่ครอบพฤติกรรมส่งซ้ำรอบสอง (เขียนทับ/เพิ่มคอลัมน์/ถามครูก่อน) เพราะ UJ-04 ยังไม่ระบุ (ดู Coverage Gap G-05 ข้อ (1) ใน acceptance-criteria.md — ไม่แต่งเงื่อนไขเพิ่มเอง)
**Pre-conditions:** ครูตรวจสอบข้อมูลก่อนส่งเสร็จแล้ว (ปุ่ม "ยืนยันส่งคะแนน" กดได้แล้ว) แต่ยังไม่ได้กดปุ่มนั้น
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | สังเกตส่วนผลลัพธ์การส่ง (`export-sent-result`) หลังตรวจสอบข้อมูลเสร็จแต่ยังไม่กดยืนยัน | ส่วนผลลัพธ์การส่งยังคงซ่อนอยู่ ไม่มีข้อความ "ส่งคะแนนสำเร็จ" ปรากฏ |
| 2 | สลับออกจากหน้า "ส่งออกคะแนน" ไปหน้าอื่นในเมนูหลักโดยไม่กดปุ่ม "ยืนยันส่งคะแนน" แล้วกลับมาที่หน้า "ส่งออกคะแนน" อีกครั้ง | ไม่มีข้อความหรือสถานะใดยืนยันว่ามีการส่งคะแนนเกิดขึ้น ("ส่งล่าสุดเมื่อ ..." ยังไม่ปรากฏ) ยืนยันว่าไม่มีข้อมูลคะแนนถูกเขียนเข้า Google Sheets หรือสร้างไฟล์ให้ดาวน์โหลดเกิดขึ้นเลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-71-003 — ปุ่ม "ยืนยันส่งคะแนน" ปิดใช้งานจนกว่าการตรวจสอบข้อมูลก่อนส่งจะสำเร็จ

**อ้างอิง:** AC-FE-71-3, FE-71, spec 05
**ประเภท:** Edge case
**Pre-conditions:** เพิ่งเข้าหน้า "ส่งออกคะแนน" ครั้งแรก ยังไม่ได้กดปุ่ม "ตรวจสอบข้อมูลก่อนส่ง" เลยตั้งแต่เข้าหน้านี้
**Test Data:** -

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เลื่อนหน้าจอไปดูขั้น "5. ยืนยันส่ง" โดยยังไม่กดปุ่มตรวจสอบข้อมูลก่อนส่งเลย | ปุ่ม "ยืนยันส่งคะแนน" อยู่ในสถานะปิดใช้งาน (มี attribute `disabled`) กดไม่ได้ |
| 2 | พยายามกดปุ่มที่ปิดใช้งานนั้น | ไม่มีเหตุการณ์ใดเกิดขึ้น ไม่มีข้อความส่งสำเร็จปรากฏ และส่วนผลลัพธ์การส่งยังคงซ่อนอยู่ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## สรุปสถานะ prototype

| จำนวน test case | รันบน prototype ได้จริงตามที่ระบบทำงานอยู่ในปัจจุบัน | มีข้อจำกัด/คาดว่าจะไม่ผ่าน |
|---|---|---|
| 22 | 20 | 2 |

- **TC-FE-68-002** คาดว่า **ไม่ผ่าน** บน prototype v2 ฉบับปัจจุบัน เพราะ `renderExportColumnMap()` ยังไม่มีการตรวจสอบคอลัมน์ปลายทางซ้ำตามที่ AC-FE-68-2 กำหนดไว้ — ต้องบันทึกเป็นบั๊กที่ `docs/03-testing/02-test-result/` เมื่อรันจริง
- **TC-FE-69-002** รันได้แต่พิสูจน์ได้เพียงบางส่วน เพราะ prototype จำลองรหัสที่ไม่ตรงด้วยค่าคงที่ "5001"/"5002" ไม่ได้จำลองไฟล์ Sheets ที่มีตำแหน่งแถวจริงให้เทียบ — ยังไม่ใช่การพิสูจน์ประเด็น "ตำแหน่งแถวตรงแต่รหัสไม่ตรง" แบบสมบูรณ์
- ที่เหลืออีก 20 เคสใช้หน้าจอ ปุ่ม และข้อความจริงที่มีอยู่ใน `v2-20260821-teacher-classroom-manage/index.html` หน้า "ส่งออกคะแนน" ได้ครบ (ยกเว้น TC-FE-34-002 ที่ใช้ `v1-20260821-student-learn-and-pet/index.html` ฝั่งนักเรียนแทน เพราะเป็นการพิสูจน์ "ไม่มี" ทางเข้าฝั่งครู)

## ประเภท test case แยกตามจำนวน

| ประเภท | จำนวน | รายการ |
|---|---|---|
| Happy path | 10 | TC-FE-34-001, TC-FE-63-001, TC-FE-64-001, TC-FE-65-001, TC-FE-66-001, TC-FE-67-001, TC-FE-68-001, TC-FE-69-001, TC-FE-70-001, TC-FE-71-001 |
| Permission | 3 | TC-FE-34-002, TC-FE-63-002, TC-FE-64-002 |
| Negative | 6 | TC-FE-63-003, TC-FE-66-002, TC-FE-68-002, TC-FE-69-002, TC-FE-70-002, TC-FE-71-002 |
| Edge case | 3 | TC-FE-65-002, TC-FE-67-002, TC-FE-71-003 |
| **รวม** | **22** | ครบทั้ง 22 AC ของหมวด I ใน acceptance-criteria.md (AC-FE-34-1/2, AC-FE-63-1~3, AC-FE-64-1/2, AC-FE-65-1/2, AC-FE-66-1/2, AC-FE-67-1/2, AC-FE-68-1/2, AC-FE-69-1/2, AC-FE-70-1/2, AC-FE-71-1~3) |
