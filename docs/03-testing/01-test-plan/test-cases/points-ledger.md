# Test Cases — points-ledger (ระบบแปลงคะแนนเป็นแต้มและบัญชีแต้ม 2 ยอด)

- **วันที่บันทึก:** 2026-08-22
- **ครอบ FE:** FE-21 (คะแนนเต็มต่อชุด + คำนวณอัตโนมัติ), FE-22 (ยอดแต้มสะสมรวมไม่ลด), FE-23 (ยอดแต้มที่ใช้ได้หักออกได้), FE-25 (ซื้อของแต่งตัวด้วยแต้ม ไม่มีผลต่อค่าสเตตัส — **เลิกใช้ 2026-08-23 แทนที่ด้วย FE-78**)
- **อ้างอิง UJ:** [[../../../01-requirements/user-journey|UJ-01]] (node G/H/I/K/L — ทำแบบทดสอบ → คำนวณคะแนน → แต้มสะสม/ที่ใช้ได้ → ซื้อของแต่งตัว)
- **อ้างอิง Requirement:** [[../../../01-requirements/01-spec/20260821-02-pet-points-and-weekly-arena|pet-points-and-weekly-arena]] หัวข้อ ก. และ ข.
- **อ้างอิง AC:** [[../acceptance-criteria|acceptance-criteria]] — AC-FE-21-1/2, AC-FE-22-1/2, AC-FE-23-1/2, AC-FE-25-1/2
- **Prototype ที่ใช้ทดสอบ:** `docs/02-design/01-prototypes/v1-20260821-student-learn-and-pet/index.html` (หน้าจอ 1 ล็อกอิน, 3 ทำแบบทดสอบ, 4 ผลคะแนน, 6 ร้านของแต่งตัว) — ทุกเคสในไฟล์นี้ **รันบน prototype v1 ได้จริง** เพราะ FE-21/22/23/25 ไม่ผูกกับตัวเลขเศรษฐกิจแต้มที่เพิ่งล็อกใหม่ (ไข่/ร่างที่ 1–4) ซึ่งอยู่ในไฟล์ `pet-growth-economy.md` แทน
- **Actual Result:** ทุกเคสเว้นว่างช่อง Actual Result ไว้ — ให้บันทึกผลทดสอบจริงที่ [[../../02-test-result/index|docs/03-testing/02-test-result/]] ไม่ใช่ในไฟล์นี้

---

## Test Data ที่ใช้ในไฟล์นี้ (อ้างจาก `MOCK` ใน prototype v1 — เจ้าของเดียวตาม DESIGN.md ข้อ 4.8)

| ค่า | รายละเอียด |
|---|---|
| นักเรียนตัวอย่าง | รหัส `3001` / PIN `1234` / ชื่อเล่น `น้องมิว` |
| แบบทดสอบ | "แบบทดสอบปรนัย: การคูณเลข" 5 ข้อ คะแนนเต็มสมมติ 20 คะแนน (`fullScoreMock`) |
| อัตราแปลงคะแนนเป็นแต้ม | สมมติ 1 คะแนน : 1 แต้ม (`pointConversionRatioMock`) |
| ของแต่งตัว | หมวกผ้า 15 แต้ม / ผ้าพันคอ 20 แต้ม / แว่นกลม 25 แต้ม (`MOCK.shopItems`) |

---

## FE-21 — ครูกำหนดคะแนนเต็มต่อชุดและระบบคำนวณคะแนนอัตโนมัติ

### TC-FE-21-001 — ตอบถูกครบทุกข้อได้คะแนนเต็มพอดี

**อ้างอิง:** AC-FE-21-2, FE-21, UJ-01 (node H/I), spec 02 หัวข้อ ก.
**ประเภท:** Happy path
**Pre-conditions:** ล็อกอินด้วยรหัส `3001` / PIN `1234` สำเร็จแล้ว อยู่ที่หน้า "ทำแบบทดสอบปรนัย" ชุด "แบบทดสอบปรนัย: การคูณเลข" (5 ข้อ คะแนนเต็ม 20)
**Test Data:** ตอบถูกทั้ง 5 ข้อ

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เลือกคำตอบที่ถูกต้องให้ครบทั้ง 5 ข้อ | ทุกข้อแสดงว่าถูกเลือกคำตอบแล้ว ปุ่ม "ส่งคำตอบ" กดได้ |
| 2 | กดปุ่ม "ส่งคำตอบ" | หน้าจอแสดงข้อความ "ตอบถูก 5 / 5 ข้อ · ได้คะแนน 20 / 20 (คะแนนเต็มค่าสมมติ)" |
| 3 | กดปุ่ม "ดูผลคะแนน" | หน้า "ผลคะแนน" แสดง "คะแนนที่ได้ 20 / 20" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-21-002 — ตอบถูกบางส่วนได้คะแนนตามสัดส่วนที่ถูกต้อง

**อ้างอิง:** AC-FE-21-1, FE-21, UJ-01 (node H/I), spec 02 หัวข้อ ก.
**ประเภท:** Edge case
**Pre-conditions:** ล็อกอินสำเร็จแล้ว อยู่ที่หน้า "ทำแบบทดสอบปรนัย" ชุดเดียวกัน (5 ข้อ คะแนนเต็ม 20)
**Test Data:** ตอบถูก 3 ข้อจาก 5 ข้อ (ตอบผิดหรือเว้น 2 ข้อ)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เลือกคำตอบที่ถูกต้อง 3 ข้อ และเลือกคำตอบที่ผิดอีก 2 ข้อ | ครบทั้ง 5 ข้อ ปุ่ม "ส่งคำตอบ" กดได้ |
| 2 | กดปุ่ม "ส่งคำตอบ" | หน้าจอแสดงข้อความ "ตอบถูก 3 / 5 ข้อ · ได้คะแนน 12 / 20 (คะแนนเต็มค่าสมมติ)" (คำนวณจาก 3/5 × 20 = 12) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-22 — ยอดแต้มสะสมรวมที่ไม่ลด

### TC-FE-22-001 — ยอดแต้มสะสมรวมเพิ่มขึ้นทุกครั้งที่ได้คะแนนใหม่

**อ้างอิง:** AC-FE-22-1, FE-22, UJ-01 (node K/L), spec 02 หัวข้อ ข.1
**ประเภท:** Happy path
**Pre-conditions:** ล็อกอินสำเร็จแล้ว ยอดแต้มสะสมรวมเริ่มต้นที่ 0 แต้ม (เพิ่งเข้าใช้งานครั้งแรก)
**Test Data:** ทำแบบทดสอบตอบถูกครบ 5/5 ข้อ (ได้ 20 แต้ม)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ทำแบบทดสอบตอบถูกครบ 5 ข้อแล้วกด "ส่งคำตอบ" | ระบบคำนวณคะแนนได้ 20/20 |
| 2 | กด "ดูผลคะแนน" | หน้า "ผลคะแนน" แสดงข้อความ "ยอดสะสมรวมทั้งหมด: 20 แต้ม" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-22-002 — ยอดแต้มสะสมรวมไม่ลดลงแม้ใช้แต้มไปซื้อของ

**อ้างอิง:** AC-FE-22-2, FE-22, UJ-01 (node M/O), spec 02 หัวข้อ ข.1
**ประเภท:** Negative
**Pre-conditions:** นักเรียนมียอดแต้มสะสมรวม 20 แต้ม และยอดแต้มที่ใช้ได้ 20 แต้ม (จาก TC-FE-22-001)
**Test Data:** ซื้อ "หมวกผ้า" ราคา 15 แต้มที่หน้า "ร้านของแต่งตัว"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ร้านของแต่งตัว" แล้วกดปุ่ม "ซื้อ" ที่รายการ "หมวกผ้า" | ยอดแต้มที่ใช้ได้ลดลงเหลือ 5 แต้ม |
| 2 | กลับไปเปิดหน้า "ผลคะแนน" อีกครั้ง | ข้อความ "ยอดสะสมรวมทั้งหมด: 20 แต้ม" ยังคงเท่าเดิม ไม่ลดลงจาก 20 แม้ใช้แต้มไปแล้ว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-23 — ยอดแต้มที่ใช้ได้ซึ่งหักออกได้

### TC-FE-23-001 — ยอดแต้มที่ใช้ได้ถูกหักลดลงเมื่อนำไปซื้อของ

**อ้างอิง:** AC-FE-23-1, FE-23, UJ-01 (node M/O), spec 02 หัวข้อ ข.2
**ประเภท:** Happy path
**Pre-conditions:** นักเรียนมียอดแต้มที่ใช้ได้ 20 แต้ม
**Test Data:** ซื้อ "ผ้าพันคอ" ราคา 20 แต้ม

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ร้านของแต่งตัว" | เห็นรายการ "ผ้าพันคอ" ราคา 20 แต้ม พร้อมปุ่ม "ซื้อ" ที่กดได้ |
| 2 | กดปุ่ม "ซื้อ" ที่รายการ "ผ้าพันคอ" | ยอดแต้มที่ใช้ได้ลดลงเหลือ 0 แต้มพอดี และรายการเปลี่ยนเป็นป้าย "ซื้อแล้ว ✓" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-23-002 — ระบบปฏิเสธการใช้แต้มเมื่อยอดที่ใช้ได้ไม่พอ โดยไม่ตำหนินักเรียน

**อ้างอิง:** AC-FE-23-2, FE-23, UJ-01 (node M/O), DESIGN.md ข้อ 4.1
**ประเภท:** Negative
**Pre-conditions:** นักเรียนมียอดแต้มที่ใช้ได้ 10 แต้ม
**Test Data:** พยายามซื้อ "แว่นกลม" ราคา 25 แต้ม

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ร้านของแต่งตัว" | ปุ่ม "ซื้อ" ของรายการ "แว่นกลม" อยู่ในสถานะปิดใช้งาน (disabled) และมีข้อความ "ยังสะสมแต้มที่ใช้ได้ไม่พอสำหรับของชิ้นนี้" ปรากฏ ไม่มีสีแดงหรือไอคอนกากบาท (ตาม DESIGN.md ข้อ 4.1) |
| 2 | สังเกตยอดแต้มที่ใช้ได้ | ยอดแต้มที่ใช้ได้ยังคงเป็น 10 แต้ม ไม่ถูกหักออกแต่อย่างใด |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-25 — ~~ซื้อของแต่งตัวด้วยแต้ม (ไม่มีผลต่อค่าสเตตัส)~~ **[เลิกใช้ 2026-08-23 — หมวดของแต่งตัวถูกยกเลิกทั้งหมด แทนที่ด้วย FE-78]**

> **หมายเหตุเลิกใช้ (เพิ่ม 2026-08-25 — ปิด Coverage Gap G-15):** FE-25 เลิกใช้ตั้งแต่ 2026-08-23 ตาม [[../../../01-requirements/feature-list|feature-list]] เพราะผู้ใช้เปลี่ยนแนวคิดร้านค้าจากของแต่งตัวเพื่อความสวยงามเป็นไอเทมช่วยลดความซวยในการต่อสู้ทั้งหมด แทนที่ด้วย **FE-78 (ร้านไอเทมช่วยที่ซื้อด้วยแต้ม)** ดู [[../../../01-requirements/01-spec/20260823-06-battle-support-items|battle-support-items]] — TC-FE-25-001 ถึง TC-FE-25-004 ด้านล่าง**ยังคงไว้ในไฟล์เพื่อรักษาประวัติ ไม่ลบทิ้ง** ตามข้อตกลง `00-archived` ของโปรเจกต์ แต่**ไม่ใช่ test case ที่ต้องรันทดสอบซ้ำอีกต่อไป** — test case สำหรับร้านค้าไอเทมช่วยรอบนี้ (FE-78/79/80/81) อยู่ที่ `test-cases/weekly-arena.md` แทน

### TC-FE-25-001 — ซื้อของแต่งตัวสำเร็จโดยไม่กระทบค่าสเตตัสของสัตว์เลี้ยง

**อ้างอิง:** AC-FE-25-1, FE-25, UJ-01 (node M/O), spec 02 หัวข้อ ค.
**ประเภท:** Happy path
**Pre-conditions:** นักเรียนมียอดแต้มที่ใช้ได้ 20 แต้ม สัตว์เลี้ยงมีค่าพลัง (ค่าสเตตัสตามป้ายเดิมของ prototype v1) เท่ากับ X ก่อนซื้อ
**Test Data:** ซื้อ "หมวกผ้า" ราคา 15 แต้ม

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | จดค่าพลัง/ค่าสเตตัสของสัตว์เลี้ยงที่หน้า "สัตว์เลี้ยงของฉัน" ไว้ก่อนซื้อ | เห็นค่า X ปัจจุบัน |
| 2 | เปิดหน้า "ร้านของแต่งตัว" แล้วกดปุ่ม "ซื้อ" ที่รายการ "หมวกผ้า" | ยอดแต้มที่ใช้ได้ลดลงเหลือ 5 แต้ม และรายการเปลี่ยนเป็น "ซื้อแล้ว ✓" |
| 3 | กลับไปหน้า "สัตว์เลี้ยงของฉัน" แล้วดูค่าพลัง/ค่าสเตตัส และส่วน "ของแต่งตัวที่ใส่อยู่" | ค่าพลัง/ค่าสเตตัสยังเท่ากับ X เดิมทุกประการ ไม่เปลี่ยนแปลง และเห็น "หมวกผ้า" ปรากฏในรายการของแต่งตัวที่ใส่อยู่ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-25-002 — ไม่สามารถซื้อของแต่งตัวได้เมื่อแต้มที่ใช้ได้ไม่พอ

**อ้างอิง:** AC-FE-25-2, FE-25, UJ-01 (node M/O)
**ประเภท:** Negative
**Pre-conditions:** นักเรียนมียอดแต้มที่ใช้ได้ 10 แต้ม
**Test Data:** พยายามซื้อ "แว่นกลม" ราคา 25 แต้ม

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ร้านของแต่งตัว" | ปุ่ม "ซื้อ" ของ "แว่นกลม" อยู่ในสถานะปิดใช้งาน |
| 2 | พยายามกดปุ่มที่ปิดใช้งานนั้น | ไม่มีการหักแต้มเกิดขึ้น ยอดแต้มที่ใช้ได้ยังคงเป็น 10 แต้ม ไม่มีของใหม่ปรากฏในรายการ "ของแต่งตัวที่ใส่อยู่" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-25-003 — ซื้อของแต่งตัวด้วยยอดแต้มที่ใช้ได้เท่ากับราคาพอดี (ขอบเขต)

**อ้างอิง:** AC-FE-25-1, FE-25, UJ-01 (node M/O)
**ประเภท:** Boundary
**Pre-conditions:** นักเรียนมียอดแต้มที่ใช้ได้ 15 แต้มพอดี
**Test Data:** ซื้อ "หมวกผ้า" ราคา 15 แต้ม

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ร้านของแต่งตัว" | ปุ่ม "ซื้อ" ของ "หมวกผ้า" กดได้ (ไม่ปิดใช้งาน) แม้ยอดแต้มที่ใช้ได้เท่ากับราคาพอดี |
| 2 | กดปุ่ม "ซื้อ" | ซื้อสำเร็จ ยอดแต้มที่ใช้ได้ลดลงเหลือ 0 แต้มพอดี ยืนยันว่าเงื่อนไขใช้ `>=` ไม่ใช่ `>` |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-25-004 — ของแต่งตัวที่ซื้อแล้วไม่สามารถซื้อซ้ำได้อีก

**อ้างอิง:** AC-FE-25-1 (ผลข้างเคียงของความสำเร็จในการซื้อ), FE-25
**ประเภท:** Edge case
**Pre-conditions:** นักเรียนซื้อ "หมวกผ้า" สำเร็จไปแล้วก่อนหน้านี้
**Test Data:** เปิดหน้า "ร้านของแต่งตัว" อีกครั้ง

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูรายการ "หมวกผ้า" ในหน้าร้านของแต่งตัว | แสดงป้าย "ซื้อแล้ว ✓" แทนปุ่ม "ซื้อ" ไม่มีทางกดซื้อซ้ำได้อีก |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## สรุปสถานะ prototype

| จำนวน test case | รันบน prototype v1 ได้จริงวันนี้ | ยังรันไม่ได้ |
|---|---|---|
| 10 | 10 (100%) | 0 |

ทุกเคสในไฟล์นี้ใช้หน้าจอและปุ่มที่มีอยู่จริงใน `v1-20260821-student-learn-and-pet/index.html` (หน้า "ทำแบบทดสอบปรนัย", "ผลคะแนน", "ร้านของแต่งตัว") และไม่ผูกกับตัวเลขเศรษฐกิจแต้มชุดใหม่ (ไข่/ร่างที่ 1–4) จึงไม่ติด Coverage Gap G-11 เหมือนไฟล์ `pet-growth-economy.md` และ `pet-species-and-egg.md`
