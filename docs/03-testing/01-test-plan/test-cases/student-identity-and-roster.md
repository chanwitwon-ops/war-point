# Test Cases — student-identity-and-roster (ระบบตัวตนและการจัดการรายชื่อนักเรียน)

- **วันที่บันทึก:** 2026-08-25
- **ครอบ FE (17 FE, หมวด A ของ acceptance-criteria.md):** FE-01 (นักเรียนล็อกอินด้วยรหัส+PIN), FE-02 (ครูรีเซ็ต PIN), FE-03 (ครูล็อกอิน Google), FE-04 (นำเข้ารายชื่อจากไฟล์), FE-05 (เพิ่มนักเรียนทีละคน), FE-06 (สร้าง PIN เริ่มต้นอัตโนมัติ), FE-07 (เก็บข้อมูลแบบไม่มีชื่อจริง), FE-47 (บังคับเปลี่ยน PIN ครั้งแรก), FE-48 (ล็อคชั่วคราวเมื่อ PIN ผิดหลายครั้ง), FE-49 (ครูล็อกอิน Google ส่วนตัว), FE-50 (นำเข้ารายชื่อจากลิงก์ Google Sheets), FE-51 (ครูเลือกคอลัมน์รหัสเอง), FE-52 (เก็บเวลาเข้าใช้งานล่าสุด), FE-53 (นักเรียนตั้งชื่อเล่นเอง), FE-54 (กรองคำหยาบชื่อเล่นอัตโนมัติ), FE-55 (ครูล้างชื่อเล่นได้), FE-56 (ปิดการใช้งานนักเรียนที่ย้ายออก)
- **อ้างอิง UJ:** [[../../../01-requirements/user-journey|UJ-01]] (node A — นักเรียนล็อกอิน), [[../../../01-requirements/user-journey|UJ-02]] (node A–E — ครูล็อกอิน, นำเข้ารายชื่อ, รีเซ็ต PIN)
- **อ้างอิง Requirement:** [[../../../01-requirements/01-spec/20260821-03-student-identity-and-roster|student-identity-and-roster]] หัวข้อ ก. ถึง ฉ.
- **อ้างอิง AC:** [[../acceptance-criteria|acceptance-criteria]] หมวด A — AC-FE-01-1~4, AC-FE-02-1/2, AC-FE-03-1/2, AC-FE-04-1/2, AC-FE-05-1~3, AC-FE-06-1/2, AC-FE-07-1/2, AC-FE-47-1/2, AC-FE-48-1~3, AC-FE-49-1/2, AC-FE-50-1/2, AC-FE-51-1/2, AC-FE-52-1/2, AC-FE-53-1/2, AC-FE-54-1/2, AC-FE-55-1/2, AC-FE-56-1~3
- **Prototype ที่ใช้ทดสอบ:**
  - `docs/02-design/01-prototypes/v1-20260821-student-learn-and-pet/index.html` — หน้าจอ 1 "ล็อกอินนักเรียน" (`screen-login`) — ใช้กับ FE-01
  - `docs/02-design/01-prototypes/v2-20260821-teacher-classroom-manage/index.html` — หน้าจอ 1 "ล็อกอินครู" (`screen-login`), หน้าจอ 2 "ภาพรวมชั้นเรียน" (`screen-overview`), หน้าจอ 3 "จัดการรายชื่อนักเรียน" (`screen-roster`), และ modal "รีเซ็ต PIN สำเร็จ" (`pin-modal`) — ใช้กับ FE-02, FE-03, FE-04, FE-05, FE-06, FE-07, FE-49, FE-50, FE-51, FE-52, FE-55, FE-56
  - **FE-47, FE-48, FE-53, FE-54 ไม่มีหน้าจอจริงใน prototype v1/v2 เลย** — FE-47/48/53 ถูกบันทึกไว้แล้วใน Coverage Gap **G-02** ของ `acceptance-criteria.md` ส่วน **FE-54 ยังไม่ถูกบันทึกไว้ใน G-02** (ตรวจสอบแล้วไม่พบคำว่า "คำหยาบ"/"ไม่เหมาะสม" ในไฟล์ `index.html` ทั้งสองเวอร์ชัน) — รายงานเพิ่มเติมไว้ท้ายไฟล์นี้ ทุก test case ของ 4 FE นี้เขียน test step จากข้อความ AC โดยตรงตามกฎของงานนี้ และมีหมายเหตุกำกับไว้ในแต่ละเคส
- **Actual Result:** ทุกเคสเว้นว่างช่อง Actual Result ไว้ — ให้บันทึกผลทดสอบจริงที่ [[../../02-test-result/index|docs/03-testing/02-test-result/]] ไม่ใช่ในไฟล์นี้

---

## Test Data ที่ใช้ในไฟล์นี้ (อ้างจาก `MOCK` ใน prototype v1/v2 — เจ้าของเดียวตาม DESIGN.md ข้อ 4.8)

| ค่า | รายละเอียด | มาจากไฟล์ |
|---|---|---|
| นักเรียนตัวอย่างฝั่งล็อกอิน | รหัส `3001` / PIN `1234` / ชื่อเล่น `น้องมิว` | v1 `MOCK.student` |
| รายชื่อนักเรียนตัวอย่างฝั่งครู (13 คน) | รหัส `4101`–`4113` พร้อมชื่อเล่น, PIN, สถานะ active/inactive, `lastActiveDaysAgoMock` — เช่น `4103` (จิ๋ว, active, 9 วันก่อน), `4106` (เต่า, active, 15 วันก่อน), `4110` (ดิว, **inactive**, 21 วันก่อน), `4113` (**ไม่มีชื่อเล่น**, active, วันนี้) | v2 `MOCK.students` |
| ชุดนำเข้าจากไฟล์ (Excel/CSV) | รหัส `4114`, `4115`, `4116` (3 คน) — คอลัมน์ตัวอย่างในพรีวิว: "รหัสนักเรียน" / "ชื่อ-นามสกุล" (แสดง "(จะไม่นำเข้าชื่อจริง)" ทุกแถว) / "ห้อง" | v2 `MOCK.fileImportBatchMock` |
| ชุดนำเข้าจากลิงก์ Google Sheets | รหัส `4119`, `4120` (2 คน) — คอลัมน์ตัวอย่างในพรีวิว: `student_id` / `full_name` (แสดง "(จะไม่นำเข้าชื่อจริง)" ทุกแถว) / `note` | v2 `MOCK.sheetImportBatchMock` |
| จำนวนหลักของ PIN เริ่มต้น | 4 หลัก (ค่าสมมติ — spec 03 ยังไม่ล็อกจำนวนหลักจริง) | v2 `MOCK.pinDigitsMock` |
| เกณฑ์ "ไม่เข้าใช้งานนาน" | 7 วัน (ค่าสมมติ — spec 04 ยังไม่ล็อก) | v2 `MOCK.inactiveDaysThresholdMock` |

---

## FE-01 — นักเรียนล็อกอินด้วยรหัสประจำตัว + PIN

### TC-FE-01-001 — ล็อกอินสำเร็จด้วยรหัสประจำตัวและ PIN ที่ถูกต้อง

**อ้างอิง:** AC-FE-01-1, FE-01, UJ-01 (node A), spec 03 หัวข้อ ก.
**ประเภท:** Happy path
**Pre-conditions:** เปิด prototype v1 อยู่ที่หน้า "เข้าสู่ระบบ War Point" (`screen-login`) ยังไม่ได้ล็อกอิน
**Test Data:** รหัสประจำตัว `3001` / PIN `1234`

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กรอกรหัสประจำตัว `3001` ในช่อง "รหัสประจำตัว" | ค่าที่กรอกปรากฏในช่องถูกต้อง |
| 2 | กรอก PIN `1234` ในช่อง "PIN (ตัวเลข)" | ค่าที่กรอกปรากฏในช่องถูกต้อง |
| 3 | กดปุ่ม "เข้าสู่ระบบ" | ระบบแสดงข้อความ "เข้าสู่ระบบสำเร็จ ยินดีต้อนรับ น้องมิว" และพาไปยังหน้า "บทเรียนของฉัน" (`screen-lessons`) ทันที |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-01-002 — กรอกรหัสประจำตัวหรือ PIN ผิดแสดงข้อความเป็นกลาง

**อ้างอิง:** AC-FE-01-2, FE-01, UJ-01 (node A), DESIGN.md ข้อ 4.1
**ประเภท:** Negative
**Pre-conditions:** เปิด prototype v1 อยู่ที่หน้า "เข้าสู่ระบบ War Point" ยังไม่ได้ล็อกอิน
**Test Data:** รหัสประจำตัว `9999` / PIN `0000` (ไม่ตรงกับข้อมูลตัวอย่าง)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กรอกรหัสประจำตัว `9999` และ PIN `0000` | ค่าที่กรอกปรากฏในช่องถูกต้อง |
| 2 | กดปุ่ม "เข้าสู่ระบบ" | ข้อความใต้ฟอร์มแสดง "รหัสประจำตัวหรือ PIN ยังไม่ตรงกับข้อมูลตัวอย่าง ลองใหม่อีกครั้ง (ดูคำแนะนำด้านล่างฟอร์ม)" ไม่มีสีแดง ไม่มีไอคอนกากบาท ไม่มีถ้อยคำตำหนิ และหน้าจอยังคงอยู่ที่ `screen-login` |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-01-003 — ฟอร์มล็อกอินไม่มีช่องกรอกอีเมล

**อ้างอิง:** AC-FE-01-3, FE-01, UJ-01 (node A)
**ประเภท:** Edge case
**Pre-conditions:** เปิด prototype v1 อยู่ที่หน้า "เข้าสู่ระบบ War Point"
**Test Data:** ไม่ต้องกรอกข้อมูลใด — ตรวจสอบ DOM เท่านั้น

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิด DevTools ตรวจสอบ DOM ของ `#login-form` ทั้งหมด | พบเฉพาะ `<label for="login-id">รหัสประจำตัว</label>` และ `<label for="login-pin">PIN (ตัวเลข)</label>` เท่านั้น |
| 2 | ค้นหา `input[type="email"]` หรือ label ที่มีคำว่า "อีเมล" ในหน้านี้ | ไม่พบ element ใดที่เกี่ยวกับการกรอกอีเมลอยู่ใน DOM ของหน้าเลยแม้แต่จุดเดียว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-01-004 — ไม่มีทางเข้าระบบสำหรับผู้ปกครองที่ใดในระบบ

**อ้างอิง:** AC-FE-01-4, FE-01, DESIGN.md ข้อ 4.10
**ประเภท:** Negative
**Pre-conditions:** เปิดทั้ง prototype v1 (หน้า `screen-login` ฝั่งนักเรียน) และ v2 (หน้า `screen-login` ฝั่งครู)
**Test Data:** ไม่ต้องกรอกข้อมูล — ตรวจสอบเนื้อหาทุกหน้าจอ

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ที่หน้าล็อกอิน v1 ค้นหาข้อความ/ปุ่มที่เกี่ยวกับ "ผู้ปกครอง" ด้วย Ctrl+F | ไม่พบข้อความหรือปุ่มใดที่เกี่ยวกับการล็อกอิน/สมัครบัญชีผู้ปกครองในหน้านี้ |
| 2 | ที่หน้าล็อกอิน v2 (ครู) ค้นหาข้อความ/ปุ่มที่เกี่ยวกับ "ผู้ปกครอง" ด้วย Ctrl+F | ไม่พบข้อความหรือปุ่มใดที่เกี่ยวกับการล็อกอิน/สมัครบัญชีผู้ปกครองในหน้านี้เช่นกัน มีเพียงทางเข้าของนักเรียน (รหัส+PIN) และของครู (บัญชี Google) เท่านั้นทั้งระบบ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-02 — ครูรีเซ็ต PIN ให้นักเรียน

### TC-FE-02-001 — ครูรีเซ็ต PIN ให้นักเรียนรายคนสำเร็จ

**อ้างอิง:** AC-FE-02-1, FE-02, UJ-02 (node F/G), spec 03 หัวข้อ ก.
**ประเภท:** Happy path
**Pre-conditions:** ล็อกอินฝั่งครู (v2) สำเร็จแล้ว อยู่ที่หน้า "จัดการรายชื่อนักเรียน" (`screen-roster`)
**Test Data:** นักเรียนรหัส `4103` (ชื่อเล่น "จิ๋ว")

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ที่แถวรหัส `4103` กดปุ่ม "รีเซ็ต PIN" | modal เปิดขึ้น หัวข้อ "รีเซ็ต PIN สำเร็จ" แสดงข้อความ "จิ๋ว (รหัส 4103) มี PIN ใหม่คือ <PIN ใหม่ 4 หลัก> — แจ้งให้นักเรียนคนนี้ทราบ" พร้อมกันนั้นมี toast แสดง "รีเซ็ต PIN ของ จิ๋ว สำเร็จ" |
| 2 | กดปุ่ม "รับทราบ" เพื่อปิด modal | modal ปิดลง และคอลัมน์ "PIN ปัจจุบัน" ของแถวรหัส `4103` ในตารางเปลี่ยนเป็น PIN ใหม่ที่เพิ่งได้รับ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-02-002 — นักเรียนไม่มีทางรีเซ็ต PIN ของตัวเองหรือของเพื่อน

**อ้างอิง:** AC-FE-02-2, FE-02, DESIGN.md ข้อ 4.5
**ประเภท:** Permission
**Pre-conditions:** ล็อกอินฝั่งนักเรียน (v1) สำเร็จแล้ว ด้วยรหัส `3001` / PIN `1234`
**Test Data:** ไม่ต้องกรอกข้อมูลเพิ่ม — ตรวจสอบ DOM ของทุกหน้าจอฝั่งนักเรียน

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ไล่เปิดทุกหน้าจอฝั่งนักเรียน (บทเรียนของฉัน, ทำแบบทดสอบ, ผลคะแนน, สัตว์เลี้ยงของฉัน, ร้านของแต่งตัว, เปลี่ยนชนิดสัตว์เลี้ยง, อารีน่า) แล้วเปิด DevTools ค้นหาคำว่า "รีเซ็ต PIN" ใน DOM | ไม่พบปุ่มหรือองค์ประกอบใดที่มีข้อความ "รีเซ็ต PIN" ปรากฏอยู่ใน DOM ของหน้าใดเลยแม้แต่จุดเดียว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-03 — ครูล็อกอินด้วยบัญชี Google

### TC-FE-03-001 — ครูล็อกอินด้วยบัญชี Google สำเร็จ

**อ้างอิง:** AC-FE-03-1, FE-03, UJ-02 (node A), spec 03 หัวข้อ ข.
**ประเภท:** Happy path
**Pre-conditions:** เปิด prototype v2 อยู่ที่หน้า "ล็อกอินครู" (`screen-login`) ยังไม่ได้ล็อกอิน
**Test Data:** ไม่ต้องกรอกข้อมูลใด

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "เข้าสู่ระบบด้วยบัญชี Google" | ระบบแสดง toast "เข้าสู่ระบบด้วยบัญชี Google สำเร็จ (จำลอง)" และพาไปยังหน้า "ภาพรวมชั้นเรียน" (`screen-overview`) ทันทีโดยไม่ต้องกรอกข้อมูลอื่นเพิ่ม |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-03-002 — หน้าล็อกอินของครูไม่มีช่องกรอกรหัสผ่าน

**อ้างอิง:** AC-FE-03-2, FE-03, UJ-02 (node A)
**ประเภท:** Edge case
**Pre-conditions:** เปิด prototype v2 อยู่ที่หน้า "ล็อกอินครู"
**Test Data:** ไม่ต้องกรอกข้อมูล — ตรวจสอบ DOM เท่านั้น

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ตรวจสอบ DOM ของหน้า `screen-login` (ฝั่งครู) ทั้งหมดด้วย DevTools | พบเฉพาะปุ่ม `#btn-google-login` ("เข้าสู่ระบบด้วยบัญชี Google") และข้อความ "จำลองปุ่มเท่านั้น — prototype นี้ไม่เชื่อมต่อ Google จริง..." ไม่มี `input[type="password"]` หรือ label ที่มีคำว่า "รหัสผ่าน" ปรากฏอยู่ในหน้านี้เลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-04 — ครูนำเข้ารายชื่อนักเรียนทั้งห้องจากไฟล์

### TC-FE-04-001 — นำเข้ารายชื่อจากไฟล์สำเร็จ

**อ้างอิง:** AC-FE-04-1, FE-04, UJ-02 (node B/C), spec 03 หัวข้อ ค.
**ประเภท:** Happy path
**Pre-conditions:** ล็อกอินฝั่งครูสำเร็จแล้ว อยู่ที่หน้า "จัดการรายชื่อนักเรียน" การ์ด "นำเข้าจากไฟล์ (Excel/CSV)"
**Test Data:** ไฟล์ตัวอย่าง (ไม่จำเป็นต้องเลือกไฟล์จริง) — ชุดนำเข้า mock 3 คน (รหัส 4114, 4115, 4116)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "อ่านไฟล์ (จำลอง)" | ตารางตัวอย่างคอลัมน์ปรากฏขึ้น ("รหัสนักเรียน" / "ชื่อ-นามสกุล" / "ห้อง") พร้อม toast "อ่านไฟล์แล้ว (จำลอง) — เลือกคอลัมน์รหัสประจำตัวก่อนนำเข้า" |
| 2 | เลือก "คอลัมน์ที่ 1: รหัสนักเรียน" จาก dropdown "เลือกคอลัมน์ที่เป็นรหัสประจำตัว" | ค่าที่เลือกปรากฏใน dropdown ถูกต้อง |
| 3 | กดปุ่ม "นำเข้ารายชื่อจากไฟล์" | ระบบแสดง toast "นำเข้ารายชื่อจากไฟล์สำเร็จ 3 คน (ใช้คอลัมน์ที่เลือกเป็นรหัสประจำตัว ไม่นำเข้าชื่อจริง)" และตาราง "รายชื่อนักเรียนทั้งหมด" มีแถวใหม่ 3 แถว (รหัส 4114, 4115, 4116) เพิ่มขึ้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-04-002 — นำเข้าไม่สำเร็จเมื่อยังไม่เลือกคอลัมน์รหัสประจำตัว

**อ้างอิง:** AC-FE-04-2, FE-04, UJ-02 (node B/C)
**ประเภท:** Negative
**Pre-conditions:** ครูกดปุ่ม "อ่านไฟล์ (จำลอง)" แล้วเห็นพรีวิวคอลัมน์แล้ว แต่ยังไม่ได้เลือกคอลัมน์ในช่อง "เลือกคอลัมน์ที่เป็นรหัสประจำตัว" (ยังเป็น "— เลือกคอลัมน์ —")
**Test Data:** ไม่เลือกคอลัมน์ใด

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "นำเข้ารายชื่อจากไฟล์" ทันทีโดยไม่เลือกคอลัมน์ | ระบบแสดง toast "กรุณาเลือกคอลัมน์ที่เป็นรหัสประจำตัวก่อน" และไม่มีแถวนักเรียนใหม่ถูกเพิ่มเข้าตาราง "รายชื่อนักเรียนทั้งหมด" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-05 — ครูเพิ่มนักเรียนทีละคน

### TC-FE-05-001 — เพิ่มนักเรียนทีละคนสำเร็จ

**อ้างอิง:** AC-FE-05-1, FE-05, UJ-02 (node B/D), spec 03 หัวข้อ ค.
**ประเภท:** Happy path
**Pre-conditions:** ล็อกอินฝั่งครูสำเร็จแล้ว อยู่ที่หน้า "จัดการรายชื่อนักเรียน" ส่วน "เพิ่มนักเรียนทีละคน"
**Test Data:** รหัสประจำตัวใหม่ `4121` (ยังไม่มีในระบบ)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กรอก `4121` ในช่อง "รหัสประจำตัว (รหัสของโรงเรียน)" | ค่าที่กรอกปรากฏในช่องถูกต้อง |
| 2 | กดปุ่ม "เพิ่มนักเรียน" | ข้อความใต้ฟอร์มแสดง "เพิ่มนักเรียนรหัส 4121 สำเร็จ PIN เริ่มต้นคือ <PIN 4 หลัก>" พร้อม toast "เพิ่มนักเรียนรหัส 4121 สำเร็จ" และตาราง "รายชื่อนักเรียนทั้งหมด" มีแถวใหม่ของรหัส `4121` ปรากฏ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-05-002 — ไม่สามารถเพิ่มนักเรียนได้เมื่อไม่กรอกรหัสประจำตัว

**อ้างอิง:** AC-FE-05-2, FE-05, UJ-02 (node B/D)
**ประเภท:** Negative
**Pre-conditions:** อยู่ที่ส่วน "เพิ่มนักเรียนทีละคน" ปล่อยช่อง "รหัสประจำตัว (รหัสของโรงเรียน)" ว่างไว้
**Test Data:** ช่องรหัสประจำตัวว่างเปล่า

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "เพิ่มนักเรียน" โดยไม่กรอกข้อมูลใด | ข้อความใต้ฟอร์มแสดง "กรุณากรอกรหัสประจำตัว" และไม่มีนักเรียนใหม่ถูกเพิ่มเข้าตาราง |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-05-003 — ป้องกันการเพิ่มรหัสประจำตัวที่ซ้ำกับที่มีอยู่แล้ว

**อ้างอิง:** AC-FE-05-3, FE-05, UJ-02 (node B/D)
**ประเภท:** Negative
**Pre-conditions:** ในระบบมีนักเรียนรหัสประจำตัว `4103` อยู่แล้ว (ตาม Test Data ต้นไฟล์)
**Test Data:** กรอกรหัสประจำตัว `4103` ซ้ำ

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กรอก `4103` ในช่อง "รหัสประจำตัว (รหัสของโรงเรียน)" แล้วกดปุ่ม "เพิ่มนักเรียน" | ข้อความใต้ฟอร์มแสดง "รหัสประจำตัวนี้มีอยู่ในระบบแล้ว" และไม่มีแถวนักเรียนซ้ำถูกเพิ่มเข้าตาราง (ตารางยังคงมีแถวรหัส `4103` เพียงแถวเดียว) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-06 — ระบบสร้าง PIN เริ่มต้นให้อัตโนมัติ

### TC-FE-06-001 — ระบบสร้าง PIN เริ่มต้นให้เองเมื่อเพิ่มนักเรียนใหม่

**อ้างอิง:** AC-FE-06-1, FE-06, UJ-02 (node E), spec 03 หัวข้อ ค.
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่ส่วน "เพิ่มนักเรียนทีละคน" ของหน้า "จัดการรายชื่อนักเรียน"
**Test Data:** รหัสประจำตัวใหม่ `4122`

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กรอก `4122` ในช่องรหัสประจำตัว แล้วกดปุ่ม "เพิ่มนักเรียน" โดยไม่กรอก PIN ใดๆ (เพราะไม่มีช่องให้กรอก) | ข้อความใต้ฟอร์มแสดง "เพิ่มนักเรียนรหัส 4122 สำเร็จ PIN เริ่มต้นคือ <PIN 4 หลักที่ระบบสร้างเอง>" — ครูเห็น PIN ทันทีโดยไม่ได้พิมพ์เอง |
| 2 | ดูคอลัมน์ "PIN ปัจจุบัน" ของแถวรหัส `4122` ในตาราง | ค่า PIN ตรงกับที่แสดงในข้อความยืนยันขั้นตอนที่ 1 |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-06-002 — ฟอร์มเพิ่มนักเรียนไม่มีช่องให้ครูกรอก PIN เอง

**อ้างอิง:** AC-FE-06-2, FE-06, UJ-02 (node E)
**ประเภท:** Edge case
**Pre-conditions:** อยู่ที่ส่วน "เพิ่มนักเรียนทีละคน"
**Test Data:** ไม่ต้องกรอกข้อมูล — ตรวจสอบ DOM เท่านั้น

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ตรวจสอบ DOM ของการ์ด "เพิ่มนักเรียนทีละคน" ทั้งหมดด้วย DevTools | พบเฉพาะ `<label for="manual-add-id">รหัสประจำตัว (รหัสของโรงเรียน)</label>` และปุ่ม "เพิ่มนักเรียน" ไม่มี input หรือ label ใดที่เกี่ยวกับการกรอก PIN ปรากฏอยู่เลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-07 — เก็บข้อมูลนักเรียนแบบไม่มีชื่อจริง (รหัส + ชื่อเล่นเท่านั้น)

### TC-FE-07-001 — นำเข้าไฟล์ที่มีชื่อจริงแต่ระบบเก็บเฉพาะรหัสประจำตัว

**อ้างอิง:** AC-FE-07-1, FE-07, UJ-02 (node E), spec 03 หัวข้อ ง.
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "จัดการรายชื่อนักเรียน" การ์ด "นำเข้าจากไฟล์"
**Test Data:** พรีวิวไฟล์ mock มีคอลัมน์ "ชื่อ-นามสกุล" อยู่ด้วย (แสดง "(จะไม่นำเข้าชื่อจริง)" ทุกแถว)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "อ่านไฟล์ (จำลอง)" | ตารางพรีวิวแสดง 3 คอลัมน์ "รหัสนักเรียน" / "ชื่อ-นามสกุล" / "ห้อง" — คอลัมน์ "ชื่อ-นามสกุล" แสดงข้อความ "(จะไม่นำเข้าชื่อจริง)" กำกับไว้ทุกแถว |
| 2 | เลือก "คอลัมน์ที่ 1: รหัสนักเรียน" แล้วกดปุ่ม "นำเข้ารายชื่อจากไฟล์" | ระบบแสดง toast ยืนยันว่า "ไม่นำเข้าชื่อจริง" |
| 3 | ดูแถวใหม่ (รหัส 4114–4116) ในตาราง "รายชื่อนักเรียนทั้งหมด" | แต่ละแถวแสดงเฉพาะคอลัมน์ "รหัสประจำตัว" และ "ชื่อเล่น" (เป็น "ยังไม่ตั้งชื่อเล่น") เท่านั้น ไม่มีคอลัมน์หรือข้อความชื่อ-นามสกุลจริงถูกบันทึกเข้าตารางเลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-07-002 — ไม่มีชื่อ-นามสกุลจริงปรากฏในหน้าจอใดของระบบ

**อ้างอิง:** AC-FE-07-2, FE-07, DESIGN.md ข้อ 4.5
**ประเภท:** Permission
**Pre-conditions:** ระบบมีนักเรียนที่นำเข้าจากไฟล์ (รหัส 4114–4116) ซึ่งไฟล์ต้นทางมีคอลัมน์ชื่อจริงอยู่ (จาก TC-FE-07-001)
**Test Data:** ไม่ต้องกรอกข้อมูลเพิ่ม — ตรวจสอบ DOM

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ตรวจสอบ DOM ของหน้า "จัดการรายชื่อนักเรียน" และ "ภาพรวมชั้นเรียน" ทั้งหมดด้วย DevTools | ไม่พบข้อความชื่อ-นามสกุลจริงของนักเรียนคนใดปรากฏอยู่ในหน้าใดเลย มีเพียงรหัสประจำตัวและชื่อเล่น (หรือ "ยังไม่ตั้งชื่อเล่น") เท่านั้น |
| 2 | เปิด modal "รีเซ็ต PIN สำเร็จ" ของนักเรียนรหัส `4114` (ยังไม่มีชื่อเล่น) แล้วอ่านข้อความใน modal | ข้อความอ้างถึงนักเรียนด้วย "รหัส 4114" เท่านั้น ไม่มีชื่อ-นามสกุลจริงปรากฏใน modal เลย |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-47 — นักเรียนบังคับเปลี่ยน PIN ในการล็อกอินครั้งแรก

> **หมายเหตุ:** ยังไม่มีหน้าจอนี้ใน prototype v1/v2 (ดู Coverage Gap **G-02** ใน `acceptance-criteria.md`) — test case ทั้ง 2 ข้อด้านล่างเขียน step จากข้อความ AC-FE-47-1/2 โดยตรง ยังไม่ผ่านการพิสูจน์ด้วยของจริง

### TC-FE-47-001 — บังคับให้ตั้ง PIN ใหม่ในการล็อกอินครั้งแรก

**อ้างอิง:** AC-FE-47-1, FE-47, UJ-01 (ต่อจาก node A), spec 03 หัวข้อ ก.
**ประเภท:** Happy path
**Pre-conditions:** นักเรียนได้รับ PIN เริ่มต้นที่ระบบสร้างให้ (ยังไม่เคยเปลี่ยนมาก่อน) และกำลังจะล็อกอินเป็นครั้งแรก
**Test Data:** รหัสประจำตัวและ PIN เริ่มต้นของนักเรียนที่เพิ่งถูกเพิ่มเข้าระบบ (สมมติ — ไม่มีตัวอย่างจริงใน prototype)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | นักเรียนกรอกรหัสประจำตัวและ PIN เริ่มต้นแล้วกด "เข้าสู่ระบบ" สำเร็จเป็นครั้งแรก | ระบบพานักเรียนไปยังหน้าจอ "ตั้ง PIN ใหม่" ทันที ก่อนเข้าใช้งานฟีเจอร์อื่นใดของระบบ (ไม่ผ่านหน้า "บทเรียนของฉัน" หรือหน้าอื่นก่อน) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-47-002 — นักเรียนข้ามขั้นตอนเปลี่ยน PIN ครั้งแรกไม่ได้

**อ้างอิง:** AC-FE-47-2, FE-47, UJ-01 (ต่อจาก node A)
**ประเภท:** Negative
**Pre-conditions:** นักเรียนอยู่ที่หน้าจอ "ตั้ง PIN ใหม่" หลังล็อกอินครั้งแรก (สมมติ — ไม่มีหน้าจอจริงเทียบ)
**Test Data:** ไม่กรอก PIN ใหม่

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | พยายามกดปุ่มย้อนกลับหรือปิดหน้าจอ "ตั้ง PIN ใหม่" โดยไม่กรอก PIN ใหม่ | ระบบไม่พาไปหน้าอื่นใด ยังคงค้างอยู่ที่หน้า "ตั้ง PIN ใหม่" |
| 2 | มองหาปุ่ม "ข้าม" หรือ "ทีหลัง" บนหน้าจอนี้ | ไม่พบปุ่ม "ข้าม" หรือ "ทีหลัง" ปรากฏอยู่บนหน้าจอนี้เลยแม้แต่จุดเดียว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-48 — ระบบล็อคชั่วคราวเมื่อกรอก PIN ผิดหลายครั้ง และครูปลดล็อกได้

> **หมายเหตุ:** ยังไม่มีหน้าจอนี้ใน prototype v1/v2 (Coverage Gap **G-02**) และจำนวนครั้งที่กรอกผิดได้/ระยะเวลาล็อคยังไม่ล็อกใน spec 03 (Coverage Gap **G-01**) — test case ด้านล่างเขียนโดยไม่ผูกตัวเลขที่ยังไม่ล็อก ตามข้อความ AC-FE-48-1~3 โดยตรง

### TC-FE-48-001 — ระบบล็อคการล็อกอินชั่วคราวเมื่อกรอก PIN ผิดครบจำนวนครั้งที่ระบบกำหนด

**อ้างอิง:** AC-FE-48-1, FE-48, UJ-01 (ต่อจาก node A), DESIGN.md ข้อ 4.1
**ประเภท:** Edge case
**Pre-conditions:** นักเรียนกรอกรหัสประจำตัวถูกต้องแต่กรอก PIN ผิดติดต่อกันครบจำนวนครั้งที่ระบบกำหนด (ยังไม่ล็อกจำนวน — G-01)
**Test Data:** รหัสประจำตัวที่ถูกต้อง + PIN ที่ผิดซ้ำจนครบจำนวนครั้งที่ระบบกำหนด (จำนวนยังไม่ล็อก)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กรอกรหัสประจำตัวถูกต้องและ PIN ผิดซ้ำจนครบจำนวนครั้งที่ระบบกำหนด แล้วพยายามกรอกอีกครั้ง | ระบบแสดงข้อความเป็นกลางว่าบัญชีถูกล็อคชั่วคราวและให้ติดต่อครู ไม่ใช้สีแดง ไม่มีไอคอนกากบาท และไม่มีถ้อยคำตำหนิ (ตาม DESIGN.md ข้อ 4.1) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-48-002 — บัญชีที่ถูกล็อคเข้าสู่ระบบไม่ได้แม้กรอก PIN ถูกต้อง

**อ้างอิง:** AC-FE-48-2, FE-48, UJ-01 (ต่อจาก node A)
**ประเภท:** Negative
**Pre-conditions:** บัญชีของนักเรียนอยู่ในสถานะถูกล็อคชั่วคราวแล้ว (ต่อจาก TC-FE-48-001)
**Test Data:** รหัสประจำตัวและ PIN ที่ถูกต้อง

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กรอกรหัสประจำตัวและ PIN ที่ถูกต้องอีกครั้งขณะบัญชียังถูกล็อคอยู่ | ระบบยังคงปฏิเสธการล็อกอินและแจ้งว่าบัญชีถูกล็อคชั่วคราวอยู่ ไม่อนุญาตให้เข้าใช้งานจนกว่าครูจะปลดล็อก |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-48-003 — ครูปลดล็อกบัญชีที่ถูกล็อคชั่วคราวได้

**อ้างอิง:** AC-FE-48-3, FE-48, UJ-02 (node E)
**ประเภท:** Happy path
**Pre-conditions:** ครูเห็นนักเรียนคนหนึ่งอยู่ในสถานะถูกล็อคชั่วคราวในหน้าจัดการรายชื่อ (สมมติ — ตาราง `screen-roster` ของ v2 ยังมีเพียงสถานะ "ใช้งานอยู่/ปิดการใช้งาน" ไม่มีสถานะ "ถูกล็อคชั่วคราว" จริงในรอบนี้)
**Test Data:** นักเรียนที่ถูกล็อคชั่วคราว (สมมติ)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ครูกดปุ่มปลดล็อกที่แถวของนักเรียนคนนั้น | สถานะของนักเรียนเปลี่ยนกลับเป็นล็อกอินได้ตามปกติทันที |
| 2 | นักเรียนกรอกรหัสประจำตัวและ PIN เดิมอีกครั้ง | ล็อกอินสำเร็จตามปกติเหมือนบัญชีที่ไม่เคยถูกล็อค |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-49 — ครูล็อกอินด้วยบัญชี Google ส่วนตัวได้

### TC-FE-49-001 — ครูใช้บัญชี Google ส่วนตัวล็อกอินได้โดยไม่ต้องมีบัญชีที่โรงเรียนออกให้

**อ้างอิง:** AC-FE-49-1, FE-49, UJ-02 (node A), spec 03 หัวข้อ ข.
**ประเภท:** Happy path
**Pre-conditions:** ครูไม่มีบัญชี Google ที่โรงเรียนออกให้ มีเพียงบัญชี Google ส่วนตัว (สมมติ) อยู่ที่หน้า "ล็อกอินครู" (v2)
**Test Data:** ไม่ต้องกรอกข้อมูล — ปุ่มล็อกอินเป็นปุ่มเดียวกันสำหรับทุกบัญชี

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "เข้าสู่ระบบด้วยบัญชี Google" (สมมติว่าเลือกใช้บัญชี Google ส่วนตัวของครู) | ระบบแสดง toast "เข้าสู่ระบบด้วยบัญชี Google สำเร็จ (จำลอง)" และพาไปยังหน้า "ภาพรวมชั้นเรียน" ได้สำเร็จเช่นเดียวกับกรณีใช้บัญชีที่โรงเรียนออกให้ ไม่มีเงื่อนไขบล็อกบัญชีส่วนตัว |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-49-002 — หน้าล็อกอินไม่มีการตรวจสอบโดเมนอีเมลของโรงเรียน

**อ้างอิง:** AC-FE-49-2, FE-49, UJ-02 (node A)
**ประเภท:** Edge case
**Pre-conditions:** อยู่ที่หน้า "ล็อกอินครู"
**Test Data:** ไม่ต้องกรอกข้อมูล — ตรวจสอบ DOM และผลของการกดปุ่ม

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ตรวจสอบ DOM ของหน้าล็อกอินครู | ไม่มีช่องกรอกโดเมนอีเมลหรือข้อความจำกัดโดเมนใดๆ ปรากฏอยู่ในหน้านี้ |
| 2 | กดปุ่ม "เข้าสู่ระบบด้วยบัญชี Google" (สมมติว่าเป็นบัญชีที่ไม่ใช่โดเมนของโรงเรียน เช่น gmail.com ทั่วไป) | ระบบไม่แสดงข้อความปฏิเสธหรือข้อจำกัดใดๆ เกี่ยวกับโดเมนอีเมล และเข้าสู่หน้า "ภาพรวมชั้นเรียน" สำเร็จตามปกติ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-50 — ครูนำเข้ารายชื่อนักเรียนจากลิงก์ Google Sheets

### TC-FE-50-001 — นำเข้ารายชื่อจากลิงก์ Google Sheets สำเร็จ

**อ้างอิง:** AC-FE-50-1, FE-50, UJ-02 (node B/C), spec 03 หัวข้อ ค.
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "จัดการรายชื่อนักเรียน" การ์ด "นำเข้าจากลิงก์ Google Sheets"
**Test Data:** ลิงก์ตัวอย่าง `https://docs.google.com/spreadsheets/d/mock-example` — ชุดนำเข้า mock 2 คน (รหัส 4119, 4120)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | วางลิงก์ตัวอย่างในช่อง "ลิงก์ Google Sheets" แล้วกด "ดึงข้อมูลจากลิงก์ (จำลอง)" | ตารางพรีวิวปรากฏขึ้น ("student_id" / "full_name" / "note") พร้อม toast "ดึงข้อมูลจากลิงก์แล้ว (จำลอง) — เลือกคอลัมน์รหัสประจำตัวก่อนนำเข้า" |
| 2 | เลือก "คอลัมน์ที่ 1: student_id" แล้วกด "นำเข้ารายชื่อจากลิงก์" | ระบบแสดง toast "นำเข้ารายชื่อจากลิงก์ Google Sheets สำเร็จ 2 คน (ไม่นำเข้าชื่อจริง)" และตารางรายชื่อมีแถวใหม่ 2 แถว (รหัส 4119, 4120) เพิ่มขึ้น โดยไม่ต้องดาวน์โหลดไฟล์ก่อน |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-50-002 — นำเข้าไม่สำเร็จเมื่อยังไม่วางลิงก์ Google Sheets

**อ้างอิง:** AC-FE-50-2, FE-50, UJ-02 (node B/C)
**ประเภท:** Negative
**Pre-conditions:** อยู่ที่การ์ด "นำเข้าจากลิงก์ Google Sheets" ช่องกรอกลิงก์ว่างเปล่า
**Test Data:** ช่องลิงก์ว่างเปล่า

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดปุ่ม "ดึงข้อมูลจากลิงก์ (จำลอง)" โดยไม่วางลิงก์ใดๆ | ระบบแสดง toast "กรุณาวางลิงก์ Google Sheets ก่อน" ตารางพรีวิวไม่ปรากฏ และไม่มีการนำเข้าข้อมูลใดๆ เกิดขึ้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-51 — ครูเลือกคอลัมน์รหัสประจำตัวเอง

### TC-FE-51-001 — ครูเลือกคอลัมน์ที่เป็นรหัสประจำตัวได้เองจากตัวเลือกที่ระบบแสดง

**อ้างอิง:** AC-FE-51-1, FE-51, UJ-02 (node B/C), spec 03 หัวข้อ ค.
**ประเภท:** Happy path
**Pre-conditions:** อ่านไฟล์ (จำลอง) แล้วเห็นพรีวิว 3 คอลัมน์ ("รหัสนักเรียน" / "ชื่อ-นามสกุล" / "ห้อง") — ครูเป็นผู้เลือกเองว่าคอลัมน์ไหนคือรหัสประจำตัวจาก dropdown โดยไม่ถูกบังคับจัดเรียงไฟล์ใหม่
**Test Data:** เลือก "คอลัมน์ที่ 1: รหัสนักเรียน"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิด dropdown "เลือกคอลัมน์ที่เป็นรหัสประจำตัว (FE-51)" | เห็นตัวเลือกครบทั้ง 3 คอลัมน์ตามที่ปรากฏในไฟล์ต้นทาง ("คอลัมน์ที่ 1: รหัสนักเรียน", "คอลัมน์ที่ 2: ชื่อ-นามสกุล", "คอลัมน์ที่ 3: ห้อง") โดยไม่บังคับให้จัดเรียงไฟล์ใหม่ก่อน |
| 2 | เลือก "คอลัมน์ที่ 1: รหัสนักเรียน" แล้วกด "นำเข้ารายชื่อจากไฟล์" | ระบบใช้คอลัมน์ที่ครูเลือกเป็นรหัสประจำตัวในการนำเข้าและนำเข้าสำเร็จ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-51-002 — คอลัมน์อื่นที่ไม่ได้ถูกเลือกไม่ถูกบันทึกเข้าระบบ

**อ้างอิง:** AC-FE-51-2, FE-51, UJ-02 (node B/C)
**ประเภท:** Edge case
**Pre-conditions:** นำเข้ารายชื่อจากไฟล์สำเร็จแล้วโดยเลือกเฉพาะคอลัมน์ "รหัสนักเรียน" (จาก TC-FE-51-001) ไฟล์ต้นทางมีคอลัมน์ "ชื่อ-นามสกุล" และ "ห้อง" อยู่ด้วย
**Test Data:** รหัสนักเรียนที่นำเข้าใหม่ (4114–4116)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ตรวจสอบตาราง "รายชื่อนักเรียนทั้งหมด" และ DOM ของหน้าทั้งหมดด้วย DevTools | ไม่พบข้อมูลจากคอลัมน์ "ชื่อ-นามสกุล" หรือ "ห้อง" ที่ไม่ได้ถูกเลือกปรากฏอยู่ในตารางรายชื่อนักเรียนหรือที่ใดในระบบเลย มีเพียงคอลัมน์ "รหัสประจำตัว" และ "ชื่อเล่น" เท่านั้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-52 — ระบบเก็บเวลาเข้าใช้งานล่าสุดของนักเรียน

> **หมายเหตุ:** prototype v1 (ฝั่งนักเรียน) และ v2 (ฝั่งครู) เป็นคนละไฟล์ที่มี mock state แยกกันคนละชุด ไม่เชื่อมต่อกัน จึงไม่มีกลไกใดใน prototype ที่สาธิต "นักเรียนล็อกอินแล้วครูเห็นเวลาอัปเดตทันที" แบบ end-to-end ได้จริงในรอบนี้ — TC-FE-52-001 เขียน step หลักจากข้อความ AC-FE-52-1 โดยตรง และใช้ค่าที่แสดงอยู่แล้วในหน้า "ภาพรวมชั้นเรียน" (v2) เป็นหลักฐานว่าหน้าจอมีกลไกแสดงผลค่านี้พร้อมใช้งานจริง คล้ายลักษณะของ FE-47/FE-48/FE-53 ใน Coverage Gap G-02

### TC-FE-52-001 — ระบบอัปเดตเวลาเข้าใช้งานล่าสุดทุกครั้งที่นักเรียนล็อกอินสำเร็จ

**อ้างอิง:** AC-FE-52-1, FE-52, UJ-01 (node A), UJ-02 (node E), spec 03 หัวข้อ ง.
**ประเภท:** Happy path
**Pre-conditions:** นักเรียนรหัส `4106` มีเวลาเข้าใช้งานล่าสุดที่บันทึกไว้ก่อนหน้าแสดงเป็น "15 วันก่อน" ในหน้า "ภาพรวมชั้นเรียน" ของ v2 (ค่าจาก mock `lastActiveDaysAgoMock: 15`)
**Test Data:** นักเรียนรหัส `4106`

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ภาพรวมชั้นเรียน" (v2) แล้วดูคอลัมน์ "เข้าใช้งานล่าสุด" ของแถวรหัส `4106` | เห็นค่าปัจจุบันตาม mock คือ "15 วันก่อน" ยืนยันว่าหน้าจอมีคอลัมน์นี้แสดงผลจริงพร้อมใช้งาน |
| 2 | (ตามข้อความ AC-FE-52-1) เมื่อนักเรียนรหัส `4106` ล็อกอินสำเร็จอีกครั้งในเวลาใหม่ | ค่า "เวลาเข้าใช้งานล่าสุด" ที่ระบบเก็บไว้ของนักเรียนรหัส `4106` ต้องเปลี่ยนเป็นเวลาของการล็อกอินครั้งใหม่นี้ — **ขั้นตอนนี้ยังพิสูจน์ด้วยการคลิกจริงใน prototype ไม่ได้ในรอบนี้ เพราะ v1/v2 ไม่เชื่อมสถานะกัน (ดูหมายเหตุด้านบน)** |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-52-002 — ระบบไม่เก็บประวัติการเข้าใช้งานทั้งหมด เก็บเฉพาะค่าล่าสุดค่าเดียว

**อ้างอิง:** AC-FE-52-2, FE-52, UJ-02 (node E)
**ประเภท:** Edge case
**Pre-conditions:** อยู่ที่หน้า "ภาพรวมชั้นเรียน" และ "จัดการรายชื่อนักเรียน" (v2)
**Test Data:** นักเรียนรหัส `4103` (แสดง "9 วันก่อน")

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูคอลัมน์ "เข้าใช้งานล่าสุด" ของแถวรหัส `4103` ในหน้า "ภาพรวมชั้นเรียน" | เห็นค่าเดียวคือ "9 วันก่อน" ไม่มีลิงก์หรือปุ่มใดในแถวนั้นที่นำไปสู่ "ประวัติการเข้าใช้งาน" |
| 2 | ตรวจสอบ DOM ทั้งหมดของหน้า "ภาพรวมชั้นเรียน" และ "จัดการรายชื่อนักเรียน" ด้วย DevTools | ไม่พบ element ใดที่แสดงรายการประวัติการเข้าใช้งานย้อนหลังของนักเรียนคนใดเลย มีเพียงคอลัมน์ค่าล่าสุดค่าเดียวต่อคนเท่านั้น |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-53 — นักเรียนตั้งชื่อเล่นเอง

> **หมายเหตุ:** ยังไม่มีหน้าจอนี้ใน prototype v1 (Coverage Gap **G-02**) — prototype v1 ใช้ชื่อเล่นสมมติคงที่ `น้องมิว` เท่านั้น test case ทั้ง 2 ข้อด้านล่างเขียน step จากข้อความ AC-FE-53-1/2 โดยตรง

### TC-FE-53-001 — นักเรียนตั้งชื่อเล่นของตัวเองได้สำเร็จ

**อ้างอิง:** AC-FE-53-1, FE-53, UJ-01 (ต่อจาก node A), spec 03 หัวข้อ จ.
**ประเภท:** Happy path
**Pre-conditions:** นักเรียนล็อกอินเข้าระบบและยังไม่เคยตั้งชื่อเล่น (สถานะ "ยังไม่ตั้งชื่อเล่น")
**Test Data:** ชื่อเล่นที่เหมาะสม เช่น "ปูเป้"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | พิมพ์ชื่อเล่น "ปูเป้" ในช่องตั้งชื่อเล่นแล้วกดยืนยัน | ชื่อเล่น "ปูเป้" ถูกบันทึกและปรากฏแทนที่ข้อความ "ยังไม่ตั้งชื่อเล่น" ในทุกจุดที่ระบบอ้างถึงนักเรียนคนนี้ (ตาราง, modal, หน้าอารีน่า) |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-53-002 — ไม่สามารถบันทึกชื่อเล่นที่เป็นค่าว่างได้

**อ้างอิง:** AC-FE-53-2, FE-53, UJ-01 (ต่อจาก node A)
**ประเภท:** Negative
**Pre-conditions:** นักเรียนเปิดหน้าตั้งชื่อเล่นและปล่อยช่องกรอกว่างไว้
**Test Data:** ช่องกรอกชื่อเล่นว่างเปล่า

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | กดยืนยันโดยไม่กรอกข้อความใดๆ ในช่องชื่อเล่น | ระบบไม่บันทึกชื่อเล่นและแจ้งว่าต้องกรอกชื่อเล่นก่อน สถานะยังคงเป็น "ยังไม่ตั้งชื่อเล่น" เช่นเดิม |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-54 — ระบบกรองคำหยาบของชื่อเล่นอัตโนมัติ

> **หมายเหตุ:** ตรวจสอบไฟล์ `index.html` ของทั้ง prototype v1 และ v2 แล้วไม่พบคำว่า "คำหยาบ" หรือ "ไม่เหมาะสม" ปรากฏอยู่เลย — **ไม่มีหน้าจอหรือกลไกกรองคำหยาบใน prototype ทั้งสองเวอร์ชัน** จุดนี้มีลักษณะเดียวกับ FE-47/FE-48/FE-53 แต่**ยังไม่ถูกบันทึกไว้ใน Coverage Gap G-02 ของ `acceptance-criteria.md`** (G-02 ระบุเฉพาะ FE-47, FE-48, FE-53, FE-41) — รายงานเพิ่มเติมไว้ในสรุปท้ายไฟล์นี้เพื่อให้ผู้ดูแลเอกสารพิจารณาเสริมเข้า G-02 ในรอบตรวจสอบถัดไป test case ด้านล่างเขียน step จากข้อความ AC-FE-54-1/2 โดยตรง

### TC-FE-54-001 — ระบบปฏิเสธชื่อเล่นที่ไม่เหมาะสม

**อ้างอิง:** AC-FE-54-1, FE-54, UJ-01 (ต่อจาก node A), DESIGN.md ข้อ 4.1
**ประเภท:** Negative
**Pre-conditions:** นักเรียนอยู่ที่หน้าตั้งชื่อเล่น (สมมติ — ไม่มีหน้าจอจริงเทียบ)
**Test Data:** ชื่อเล่นที่อยู่ในรายการคำที่ไม่เหมาะสมของระบบ (ตัวอย่างสมมติ — รายการคำจริงยังไม่ถูกล็อกใน spec)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | พิมพ์ชื่อเล่นที่อยู่ในรายการคำที่ไม่เหมาะสมของระบบแล้วกดยืนยัน | ระบบปฏิเสธการบันทึกชื่อเล่นนั้นและแสดงข้อความเป็นกลางให้ลองตั้งชื่ออื่น ไม่ใช้ถ้อยคำตำหนิ (ตาม DESIGN.md ข้อ 4.1) และไม่มีชื่อเล่นนั้นถูกบันทึกเข้าระบบ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-54-002 — ชื่อเล่นที่เหมาะสมผ่านการตรวจและบันทึกได้ตามปกติ

**อ้างอิง:** AC-FE-54-2, FE-54, UJ-01 (ต่อจาก node A)
**ประเภท:** Happy path
**Pre-conditions:** นักเรียนอยู่ที่หน้าตั้งชื่อเล่น (สมมติ — ไม่มีหน้าจอจริงเทียบ)
**Test Data:** ชื่อเล่นที่ไม่อยู่ในรายการคำไม่เหมาะสม เช่น "ปูเป้"

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | พิมพ์ชื่อเล่น "ปูเป้" (ไม่อยู่ในรายการคำไม่เหมาะสม) แล้วกดยืนยัน | ระบบบันทึกชื่อเล่นนั้นสำเร็จโดยไม่มีการปฏิเสธหรือแจ้งเตือนใดๆ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-55 — ครูล้างชื่อเล่นของนักเรียนได้

### TC-FE-55-001 — ครูล้างชื่อเล่นของนักเรียนสำเร็จ

**อ้างอิง:** AC-FE-55-1, FE-55, UJ-02 (node E), spec 03 หัวข้อ จ.
**ประเภท:** Happy path
**Pre-conditions:** ล็อกอินฝั่งครูสำเร็จแล้ว อยู่ที่หน้า "จัดการรายชื่อนักเรียน" นักเรียนรหัส `4101` มีชื่อเล่น "มะปราง" ปรากฏอยู่ในตาราง
**Test Data:** นักเรียนรหัส `4101` (ชื่อเล่น "มะปราง")

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ที่แถวรหัส `4101` กดปุ่ม "ล้างชื่อเล่น" | ระบบแสดง toast "ล้างชื่อเล่นของรหัส 4101 แล้ว" และคอลัมน์ "ชื่อเล่น" ของแถวรหัส `4101` เปลี่ยนจาก "มะปราง" เป็น "ยังไม่ตั้งชื่อเล่น" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-55-002 — ปุ่มล้างชื่อเล่นถูกปิดใช้งานเมื่อนักเรียนยังไม่มีชื่อเล่น

**อ้างอิง:** AC-FE-55-2, FE-55, UJ-02 (node E)
**ประเภท:** Edge case
**Pre-conditions:** อยู่ที่หน้า "จัดการรายชื่อนักเรียน" นักเรียนรหัส `4113` ยังไม่เคยตั้งชื่อเล่น (สถานะ "ยังไม่ตั้งชื่อเล่น")
**Test Data:** นักเรียนรหัส `4113`

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ดูปุ่ม "ล้างชื่อเล่น" ที่แถวรหัส `4113` ในตารางจัดการรายชื่อ | ปุ่ม "ล้างชื่อเล่น" ที่แถวนั้นอยู่ในสถานะปิดใช้งาน (attribute `disabled`) เพราะไม่มีชื่อเล่นให้ล้าง |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## FE-56 — ปิดการใช้งานนักเรียนที่ย้ายออกโดยไม่ลบข้อมูล

### TC-FE-56-001 — ครูปิดการใช้งานบัญชีนักเรียนที่ย้ายออกได้สำเร็จ

**อ้างอิง:** AC-FE-56-1, FE-56, UJ-02 (node E), spec 03 หัวข้อ ฉ.
**ประเภท:** Happy path
**Pre-conditions:** อยู่ที่หน้า "จัดการรายชื่อนักเรียน" นักเรียนรหัส `4111` (ชื่อเล่น "เมย์") มีสถานะ "ใช้งานอยู่"
**Test Data:** นักเรียนรหัส `4111`

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ที่แถวรหัส `4111` กดปุ่ม "ปิดการใช้งาน" | ระบบแสดง toast "ปิดการใช้งานรหัส 4111 แล้ว ข้อมูลไม่ถูกลบ แต่จะไม่ปรากฏในรายการที่ใช้งานประจำวัน" และปุ่มของแถวนั้นเปลี่ยนข้อความเป็น "เปิดการใช้งาน" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-56-002 — นักเรียนที่ถูกปิดการใช้งานไม่ปรากฏในหน้าภาพรวมที่ครูใช้งานประจำวัน

**อ้างอิง:** AC-FE-56-2, FE-56, UJ-02 (node E)
**ประเภท:** Edge case
**Pre-conditions:** นักเรียนรหัส `4111` ถูกครูปิดการใช้งานแล้ว (จาก TC-FE-56-001) — หรือใช้นักเรียนรหัส `4110` ที่ mock ไว้เป็นสถานะปิดใช้งานอยู่แล้วตั้งแต่ต้น
**Test Data:** นักเรียนรหัส `4110` (สถานะปิดใช้งานตั้งแต่ต้น ตาม Test Data mock)

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | เปิดหน้า "ภาพรวมชั้นเรียน" (v2) แล้วค้นหาแถวรหัส `4110` | แถวของนักเรียนรหัส `4110` ไม่ปรากฏอยู่ในตารางภาพรวมชั้นเรียนเลย |
| 2 | กลับไปที่หน้า "จัดการรายชื่อนักเรียน" แล้วค้นหาแถวรหัส `4110` | แถวของนักเรียนรหัส `4110` ยังคงปรากฏอยู่ในตาราง "รายชื่อนักเรียนทั้งหมด" ครบถ้วน (ข้อมูลไม่ถูกลบ) พร้อมปุ่ม "เปิดการใช้งาน" |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

### TC-FE-56-003 — ระบบไม่รองรับการจัดการมากกว่าหนึ่งห้องเรียนหรือหนึ่งโรงเรียน

**อ้างอิง:** AC-FE-56-3, FE-56, DESIGN.md ข้อ 4.10
**ประเภท:** Negative
**Pre-conditions:** ล็อกอินฝั่งครูสำเร็จแล้ว อยู่ที่หน้าใดก็ได้ทั้ง 8 หน้าของ v2
**Test Data:** ไม่ต้องกรอกข้อมูล — ตรวจสอบทุกหน้าจอ

| # | ขั้นตอน | Expected Result |
|---|---|---|
| 1 | ไล่เปิดทั้ง 8 หน้าจอของ v2 (ล็อกอิน, ภาพรวมชั้นเรียน, จัดการรายชื่อ, ลงบทเรียน, สร้างแบบทดสอบ, ตรวจงาน, จัดการแข่งขัน, ส่งออกคะแนน) แล้วค้นหาตัวเลือก "สลับห้องเรียน" / "สลับโรงเรียน" / "ดูรายชื่อห้องอื่น" ด้วย Ctrl+F | ไม่พบตัวเลือกดังกล่าวในหน้าจอใดเลย ระบบแสดงและจัดการรายชื่อของห้องเรียนเดียว (13 คนตาม mock) เท่านั้นตลอดทั้งระบบ |

**Actual Result:** _(ว่าง — บันทึกที่ docs/03-testing/02-test-result/)_

---

## สรุปสถานะ prototype

| กลุ่ม FE | จำนวน test case | รันบน prototype ได้จริงวันนี้ |
|---|---|---|
| FE-01, FE-02, FE-03, FE-04, FE-05, FE-06, FE-07, FE-49, FE-50, FE-51, FE-55, FE-56 | 28 | 28 (100%) — ใช้หน้าจอ/ปุ่ม/ข้อความจริงจาก `v1-20260821-student-learn-and-pet/index.html` และ `v2-20260821-teacher-classroom-manage/index.html` |
| FE-52 | 2 | 1 เต็ม (TC-FE-52-002) + 1 บางส่วน (TC-FE-52-001 ขั้นตอนที่ 2 พิสูจน์ด้วยของจริงไม่ได้ — v1/v2 ไม่เชื่อมสถานะกัน) |
| FE-47, FE-48, FE-53, FE-54 | 9 | 0 — ไม่มีหน้าจอจริงใน prototype เลย เขียนจากข้อความ AC โดยตรง (FE-47/48/53 บันทึกไว้แล้วใน Coverage Gap G-02, ส่วน FE-54 ยังไม่ถูกบันทึกไว้ — ดูหมายเหตุด้านล่าง) |
| **รวม** | **39** | **28 เต็ม + 1 บางส่วน + 10 รอ prototype รอบถัดไป** |

**สรุปจำนวนตามประเภท:** Happy path 17, Negative 11, Edge case 9, Permission 2 (รวม 39 — ครบทุก AC ในหมวด A ของ `acceptance-criteria.md` อย่างน้อย AC ละ 1 test case ตามระดับความละเอียดที่ตกลงไว้)

**หมายเหตุเพิ่มเติมที่พบระหว่างเขียนไฟล์นี้ (รายงานไว้ให้ผู้ดูแลเอกสารพิจารณา ไม่ได้แก้ `acceptance-criteria.md` เพราะอยู่นอกขอบเขตงานนี้):**

- FE-54 (กรองคำหยาบชื่อเล่นอัตโนมัติ) ไม่มีหน้าจอ/กลไกใน prototype v1 หรือ v2 เลย เหมือนกรณี FE-47/FE-48/FE-53 แต่ Coverage Gap **G-02** ของ `acceptance-criteria.md` ยังไม่ได้ระบุ FE-54 ไว้ (ระบุเฉพาะ FE-47, FE-48, FE-53, FE-41) — ควรพิจารณาเพิ่ม FE-54 เข้า G-02 ในรอบตรวจสอบถัดไป
