# ACL.md — ตารางสิทธิ์การเข้าถึงตามบทบาท (war-point)

> สถานะปัจจุบัน (การบ้านที่ 2 — เทียบสัปดาห์ที่ 7 ของ leaveeasy): มีแค่ **security rule ขั้นต่ำ "ต้องล็อกอินก่อน"** เท่านั้น (ดู [firestore.rules](firestore.rules))
> ตารางด้านล่างคือ **สิทธิ์ตามที่ระบบควรเป็น** — ช่องที่มีเครื่องหมาย ⏳ คือสิ่งที่ยังไม่บังคับจริงในโค้ดวันนี้ รอ Security Rules รายห้องในการบ้านที่ 3
>
> ⚠️ **หมายเหตุขอบเขต:** login/logout ในการบ้านนี้ใช้ **Firebase Authentication แบบอีเมล/รหัสผ่าน** เป็นตัวจำลองง่ายๆ ตามแบบที่สอนกับ LeaveEasy เท่านั้น **ไม่ใช่** ระบบ "รหัสประจำตัว + PIN" (นักเรียน) / "Google Login" (ครู) ที่ออกแบบไว้จริงใน [test-cases/student-identity-and-roster.md](docs/03-testing/01-test-plan/test-cases/student-identity-and-roster.md) — ของจริงอยู่นอกขอบเขตการบ้าน Module 2 ตาม [SCOPE.md](SCOPE.md)

| บทบาท (role) | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **student** (นักเรียน) | ส่งงาน/ทำแบบทดสอบใหม่ (บันทึก `studentId` เป็น uid ตัวเอง) · ดูรายการการส่งงานทั้งหมด · ดูรายละเอียดการส่งงาน 1 รายการ | **ให้คะแนน/เขียนรีวิวการส่งงานของตัวเองหรือของใครก็ตาม** (ไม่มีปุ่มนี้เลยไม่ว่ารายการใคร) · แก้ไข/เพิ่ม/ลบ `quizSets` · เปิดดูการส่งงานของนักเรียนคนอื่น ⏳ |
| ✅ **teacher** (ครู) | ทุกอย่างที่ student ทำได้ · ให้คะแนน/เขียนรีวิวการส่งงานที่ `status='pending_review'` (เปลี่ยนเป็น `graded` + สร้างเอกสารใน `reviews` subcollection) · เพิ่ม/แก้/ลบ `quizSets` ⏳ | เปลี่ยนสถานะการส่งงานที่ตรวจแล้ว (`graded`) กลับเป็น `pending_review` — สถานะปลายทางเปลี่ยนต่อไม่ได้ |

## ⏳ = ยังไม่บังคับจริงในโค้ดวันนี้ (รอการบ้านที่ 3)

- **student เปิดดูการส่งงานของนักเรียนคนอื่นไม่ได้** — วันนี้ทุกคนที่ล็อกอินแล้วยังเห็นการส่งงานทุกรายการในหน้ารายการ (หน้ารายการยังไม่กรองตาม `studentId`)
- **การจัดการ `quizSets` (เพิ่ม/แก้/ลบชุดแบบทดสอบ) ของ teacher** — ยังไม่มีหน้าจอสำหรับสิ่งนี้ในการบ้านตอนนี้ (นอกขอบเขตการบ้านที่ 2)

ข้อแรกเป็นแค่เรื่องหน้ารายการยังไม่กรองข้อมูล ไม่ใช่ Security Rules จริง — คนที่รู้วิธียิง request ตรงไปที่ Firestore ยังข้ามข้อจำกัดนี้ได้อยู่ในตอนนี้ เพราะกฎ [firestore.rules](firestore.rules) วันนี้เช็คแค่ "ล็อกอินหรือยัง" เท่านั้น

## บังคับจริงแล้ววันนี้ (ระดับโค้ด)

- ทุกหน้าจอ (ยกเว้น `login.html` / `register.html` / `seed.html`) ต้องล็อกอินก่อนถึงจะเข้าได้ (auth guard, ดู [js/auth-guard.js](js/auth-guard.js))
- Firestore rule บังคับ `request.auth != null` กับทุก collection (ดู [firestore.rules](firestore.rules)) — ยังไม่แยก role
- สมัครสมาชิกใหม่ทุกบัญชีเริ่มต้นเป็น `role: "student"` เสมอ (ดู [js/register.js](js/register.js)) — บัญชี teacher ต้องยกระดับเองผ่าน Firebase Console (แก้ช่อง `role` ในเอกสาร `users/{uid}`)

(ดูโครงสร้างข้อมูลเต็มที่ [SCOPE.md](SCOPE.md))
